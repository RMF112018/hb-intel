# Fresh Session Prompt — <Objective>

You are working in the live `hb-intel` repository.

## Objective

<Concrete objective.>

## Non-Negotiable Repo-Truth Requirement

Inspect the current repo files before making claims or changes. Current files, manifests, exports, tests, configs, closeouts, active prompt packages, and governing docs outrank historical summaries.

Do not re-read files that are still in your current context unless they may have changed, line-level verification is needed, final validation requires proof, or scope expanded.

## Required Starting Sources

- `<path>`
- `<path>`

## Authorized Scope

- `<authorized file/path/workstream>`

## Forbidden Scope

- Do not broaden into adjacent cleanup.
- Do not stage unrelated files.
- Do not push unless explicitly instructed.
- Do not run destructive commands.
- Do not run tenant mutation, live Graph/PnP, Procore, app catalog, CI/CD, dependency install, package version, manifest version, hosted smoke-test, or deployment commands unless explicitly authorized.

## Required Work

<Step-by-step objective.>

## Validation

Run the smallest credible validation set and explain why it is sufficient. If a validation command fails, classify the failure as new, pre-existing, environmental, flaky, or ambiguous.

## Closeout

Return only:

- files inspected;
- files modified;
- validation commands and results;
- guardrails preserved;
- known gaps or uncertainty;
- commit summary;
- commit description.
