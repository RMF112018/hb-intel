# Spotlight Remediation Pass 2 — Flagship Rebuild

## Context
Pass 1 shifted the failure shape (no-image kicker, always-visible history preview, tonal band) without reaching flagship bar. Hosted output still reads as a text-heavy, low-density, timid slab. Pass 2 rebuilds for flagship authority.

## Six structural moves
1. **Progress Ring anchor** — SVG donut as flagship visual signal in featured essentials.
2. **Authored No-Image Band** — SVG pattern plate + large glyph + progress ring + display title + visible kicker + visible summary + premium button.
3. **Editorial material** — constellation SVG at 3% bg in featured band for texture without cards.
4. **Disclosure elision wide/medium** — details promoted to always-visible depth; compact/minimal keep disclosure.
5. **History identity upgrade** — count badge pill, sector-tinted row accents, tighter density.
6. **Density compression** — ~100 px shorter first view on wide.

## Files
layout-mode.ts, FeaturedSlot.tsx, HistoryDisclosure.tsx, SupportingRail.tsx, FeaturedMedia.tsx, project-spotlight-surface.module.css, associated tests.

## Verification
Vitest + tsc + eslint + sppkg rebuild + closure artifact.

## Version
HbHomepageWebPart 1.1.63.0 (both manifest + package-solution).
