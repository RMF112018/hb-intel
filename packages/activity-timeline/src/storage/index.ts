/**
 * SF28-T03 scope — Storage adapter interfaces and append-only persistence.
 *
 * Will contain: IActivityStorageAdapter interface, SharePoint MVP adapter,
 * Azure event store migration seam, append/query/retention operations.
 */
