/**
 * Form input shape for creating or editing a Go/No-Go scorecard.
 */
export interface IScorecardFormData {
    /** Associated project identifier. */
    projectId: string;
    /** Computed overall score (0–100). */
    overallScore: number;
    /** Final recommendation. */
    recommendation: string;
}
//# sourceMappingURL=IScorecardFormData.d.ts.map