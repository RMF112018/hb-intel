/**
 * Shared constants for pagination defaults.
 *
 * @module shared/constants
 */
/** Default number of items per page when not specified by the consumer. */
export declare const DEFAULT_PAGE_SIZE = 25;
/** Maximum allowed page size to prevent excessive payloads. */
export declare const MAX_PAGE_SIZE = 100;
//# sourceMappingURL=constants.d.ts.map