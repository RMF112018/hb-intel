/**
 * Hook to detect first visit for a module context.
 * Stub implementation — returns true. Will be expanded in T04.
 */
export function useFirstVisit(_moduleId: string): boolean {
  return true;
}
