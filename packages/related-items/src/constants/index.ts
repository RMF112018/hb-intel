/**
 * Related Items constants — D-SF14-T01, D-01
 *
 * Default configuration values for the relationship panel and tiles.
 */

/** Default panel title displayed in the sidebar. */
export const RELATED_ITEMS_PANEL_TITLE = 'Related Items';

/** Default priority for relationships without explicit governance metadata. */
export const DEFAULT_RELATIONSHIP_PRIORITY = 50;

/** Maximum number of items shown in the compact canvas tile view. */
export const MAX_TILE_ITEMS = 3;
