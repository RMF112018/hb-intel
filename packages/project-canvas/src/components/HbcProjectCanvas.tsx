/**
 * HbcProjectCanvas — D-SF13-T01, D-07 (SPFx constraints)
 *
 * Placeholder component for the main project canvas surface. Full implementation in T05.
 */
export function HbcProjectCanvas(): React.ReactElement {
  return <div data-testid="hbc-project-canvas">HbcProjectCanvas placeholder</div>;
}
