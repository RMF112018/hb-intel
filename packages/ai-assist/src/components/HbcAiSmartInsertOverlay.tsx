/**
 * HbcAiSmartInsertOverlay — D-SF15-T01 scaffold
 *
 * Inline acceptance overlay for AI-generated results with schema-driven
 * auto-mapping, per-field accept, and 'ai-assisted' commit flow.
 * Full implementation in SF15-T06.
 */
import type { FC } from 'react';

/** Props for the Smart Insert overlay component. */
export interface HbcAiSmartInsertOverlayProps {
  readonly actionId: string;
}

/** Scaffold placeholder — full implementation in SF15-T06. */
export const HbcAiSmartInsertOverlay: FC<HbcAiSmartInsertOverlayProps> = () => null;

HbcAiSmartInsertOverlay.displayName = 'HbcAiSmartInsertOverlay';
