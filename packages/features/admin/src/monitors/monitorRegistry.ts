/**
 * Central registry for Admin Intelligence monitors.
 * Monitors register themselves here to be discovered by the dashboard.
 *
 * @placeholder SF17-T01 scaffold — implementation in SF17-T03
 */
export const monitorRegistry = {
  /** Registered monitor count (placeholder) */
  get size(): number {
    return 0;
  },
} as const;
