// Placeholder stub — populated by T05
/** Immediate unread count + real-time polling */
export function useNotificationBadge(): { unreadCount: number; isLoading: boolean } {
  return { unreadCount: 0, isLoading: false };
}
