# Prompt 04 — Wire the Live Project Hub Home to `@hbc/project-canvas`

```text
You are acting as a senior Project Hub runtime, canvas-governance, shell-composition, route-aware operating workspace, repo-truth, and implementation engineer for HB Intel.

## Objective
Wire the live Project Hub home to `@hbc/project-canvas` in accordance with the validated audit findings, the governed UI doctrine, and the role/device-aware default view system.

## Critical Instruction
Do not re-read files that are already in your active context window or memory. Only open additional files when needed to close a concrete evidence gap.

## Required Context
Assume Prompt 01, Prompt 02, and Prompt 03 have already been completed.
Honor their validated findings and implemented governance.

## Core Runtime Goal
The live Project Hub home must stop behaving like a summary-card scaffold / launcher grid and start behaving like a true operating surface.
`@hbc/project-canvas` must become the governing runtime for the live home wherever a canvas-capable profile applies.

## Required Architectural Intent
Implement the Project Hub home as:
- a governed shell profile,
- backed by one shared project operating runtime,
- using `@hbc/project-canvas` for canvas-capable profiles,
- with mandatory operational tiles / regions enforced in code,
- with role/device-aware defaults resolved by the profile system.

## Operational Profiles To Honor
- `hybrid-operating-layer`
- `canvas-first-operating-layer`
- `next-move-hub`
- `executive-cockpit`
- `field-tablet-split-pane`

## Wiring Requirements
1. Replace page-authored summary/scaffold behavior with governed canvas/runtime composition.
2. Make the project-scoped home resolve through the profile system.
3. For canvas-capable profiles, render through `@hbc/project-canvas` rather than handcrafted summary-card layout.
4. Enforce mandatory operational tiles / regions by profile.
5. Feed the home from canonical shared spines and project operating contracts rather than page-local summary math.

## Mandatory Operational Surface Requirements
Implement and/or wire the required operational surfaces as applicable by profile:
- truth header / project context
- health posture
- work queue / next moves
- related items / cross-module continuity
- activity / timeline continuity
- reporting / review posture as appropriate
- command rail / posture rail / watchlist / quick action surfaces where the profile requires them

## Profile-Specific Requirements

### `hybrid-operating-layer`
Must honor the intended structure:
- project truth header
- posture-aware command rail
- operating canvas
- persistent next-move/work queue rail
- activity/timeline strip

### `canvas-first-operating-layer`
Must honor:
- project header
- dominant operating canvas
- supporting context rail
- timeline/activity strip

### `next-move-hub`
Must honor:
- project header
- work mode tabs
- priority queue as dominant region
- context/impact rail
- modules access band

### `executive-cockpit`
Must honor:
- executive/portfolio/project context header
- watchlist panel
- risk/exposure canvas
- intervention rail
- trend/timeline zone

### `field-tablet-split-pane`
Must honor:
- field header
- area/sheet pane
- action stack pane
- quick action bar
- sync/capture status zone

## Important Constraints
- Do not flatten all profiles into one generic layout.
- Do not allow the canvas to become a decorative KPI dashboard.
- Do not leave mandatory operational surfaces as optional tiles.
- Do not duplicate business logic per profile when a shared runtime can serve multiple shells.
- Do not break project context continuity, role awareness, or runtime honesty.

## Required Evidence
Show:
- exact files changed,
- how the live Project Hub home now resolves through the profile system,
- where `@hbc/project-canvas` is now the governing runtime,
- where mandatory tile / region policy is enforced,
- how the role/device default views are wired into the live experience.

## Acceptance Criteria
This prompt is complete only when:
- the live Project Hub home is no longer primarily a summary-card scaffold,
- canvas-capable profiles run through `@hbc/project-canvas`,
- role/device-aware default views are active in the live Project Hub home,
- mandatory operational surfaces are enforced,
- the resulting home behaves like a project operating surface rather than a passive dashboard.
```
