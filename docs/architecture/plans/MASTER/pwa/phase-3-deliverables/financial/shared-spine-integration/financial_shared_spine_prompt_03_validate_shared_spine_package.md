# Financial Shared Spine Integration — Prompt 03
## Objective
Validate that the Financial shared-spine integration package is now coherent, cross-referenced, and implementation-safe after completion of the shared-spine contract and rule-definition work.

## Context
Prompts 01 and 02 should already be complete.
This is a validation and closure pass.
Do not expand scope into runtime implementation, UI implementation, or package wiring unless a tiny documentation-adjacent cleanup is absolutely required to complete normalization.

## Critical Guardrails
- Validate against actual changed files, not memory alone.
- Do not reopen broad architecture debates unless a real contradiction still exists.
- Do not overclaim completion.
- Do not re-read files that are already in your active context or current memory unless needed to verify a contradiction or retrieve exact evidence.
- Prefer concise corrections over speculative rewrites.

## Validation Scope
Validate that the updated doctrine package now gives a developer clear answers to the following:

### Package entry and precedence
- Is there one obvious entry point for Financial shared-spine integration?
- Is file precedence clear?
- Is the developer start path explicit?

### Spine coverage
- Does each required shared spine have a canonical doctrine home?
- Are publish/consume obligations explicit?
- Are any spine obligations still fragmented or ambiguous?

### Tool integration coverage
- Does each major Financial tool have an explicit spine integration posture?
- Are mandatory versus conditional publications clear?
- Are consumption obligations clear where applicable?

### Implementation safety
- Can a local code agent now implement downstream shared-spine wiring without guessing where doctrine truth lives?
- Are unresolved contradictions explicitly identified instead of hidden?
- Are any files still stale or conceptually weak enough to cause implementation drift?

### Acceptance readiness
- Is it now clear what evidence will be needed to prove shared-spine integration for Financial?
- Are Project Hub home / canvas / operating-layer obligations explicit enough for later validation?

## Required Actions
1. Re-read the changed doctrine files as needed for validation.
2. Perform a coherence pass across:
   - the Financial doctrine index
   - the Financial shared-spine integration control document
   - any tool-integration matrix or appendix
   - any touched README / acceptance / lane / route references
3. Fix any final cross-reference, wording, or precedence problems.
4. Produce a final shared-spine integration closure summary.
5. Produce a recommended next-step list for downstream work, limited to:
   - shared-spine implementation tasks
   - Project Hub operating-layer wiring tasks
   - acceptance/readiness evidence tasks
   - end-to-end validation tasks

## Deliverables
1. final changed-files summary
2. shared-spine package coherence verdict
3. implementation-safety verdict
4. remaining doctrine gaps, if any
5. recommended next task category sequence

## Definition of Done
This prompt is complete only when:
- the shared-spine package is coherent and developer-navigable,
- spine-by-spine obligations are explicit and non-contradictory,
- remaining doctrine risk is minimal and explicitly documented,
- and the package is ready for downstream implementation and validation work.

## Output Format
Return:
1. objective completed
2. files changed
3. coherence verdict
4. implementation-safety verdict
5. remaining doctrine gaps
6. recommended next-step sequence
