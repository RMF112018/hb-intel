# Phase 3 Deliverables — Project Hub and Project Context

**Doc Classification:** Active Deliverable Index — **Phase 3 primary deliverable production is complete (Workstreams A–H).** 31 of 31 primary deliverables authored. Workstream I (Shared Feature Infrastructure) added 2026-03-23. Workstream J (Documents Enabling Seams) added 2026-03-24. P3-E14 (Project Warranty Module) added 2026-03-24.

---

## Planning Status Summary

**Phase 3 deliverable production is complete for Workstreams A–H.** 31 of 31 primary deliverables are authored (P3-A1, P3-A2, P3-A3, P3-B1, P3-C1, P3-C2, P3-C3, P3-D1, P3-D2, P3-D3, P3-D4, P3-E1, P3-E2, P3-E3, P3-E4, P3-E5, P3-E6, P3-E7, P3-E8, P3-E9, P3-E10, P3-E11, P3-E12, P3-E13, P3-E14, P3-F1, P3-G1, P3-G2, P3-G3, P3-H1, P3-J1). All workstreams (A–H) are complete. 28 deliverables are active locked artifacts (Contract, Specification, or Note). 1 deliverable (P3-E12) is retained as a Superseded Reference for historical context only. 1 deliverable (P3-H1) is an Active Reference that evolves during implementation. 1 deliverable (P3-J1) is a Specification not yet started. **P3-E14** (Project Warranty Module Field Specification — master index + T01–T10) was added 2026-03-24; Warranty is now a first-class Phase 3 operating surface (Layer 1: coverage registry, case lifecycle, SLA, subcontractor coordination) with Layer 2 external collaborative workspace explicitly deferred. Phase 3 is now execution-ready per [Phase 3 Plan §20](../04_Phase-3_Project-Hub-and-Project-Context-Plan.md). **Workstream I** (Shared Feature Infrastructure) has been added 2026-03-23, incorporating 6 shared feature plan families (SF23, SF24, SF25, SF26, SF27, SF28) as Phase 3 Stage 5. Governing plan files are in `docs/architecture/plans/shared-features/`. The Implementation Guide has been updated: Stage 5 is new Shared Feature Infrastructure; former Stages 5–11 are renumbered Stages 6–12. **Workstream J** (Documents Enabling Seams) has been added 2026-03-24 as Phase 3 Stage 13. Governing deliverable is P3-J1; governing plan is `04_Phase-3_Unified-Documents-Enabling-Plan.md`. Full Documents implementation remains Phase 5 scope.

---

## Status Legend

| Status | Meaning |
|---|---|
| **Contract** | Locked contract artifact; changes require Architecture-lead approval |
| **Governance Policy** | Locked governance artifact; changes require Architecture-lead approval |
| **Specification** | Locked technical specification; changes require Experience-lead approval |
| **Note** | Locked behavioral guidance; lighter weight than Policy or Spec |
| **Superseded Reference** | Retained for historical context; a later deliverable now governs |
| **Active Reference** | Living document updated as implementation progresses |
| **Not Started** | Planned but not yet begun |

---

## Deliverables Index

### Workstream A — Shared-canonical Project Hub contracts

| Doc ID | Title | Status |
|---|---|---|
| P3-A1 | [Project Registry and Activation Contract](P3-A1-Project-Registry-and-Activation-Contract.md) | Contract |
| P3-A2 | [Membership / Role Authority Contract](P3-A2-Membership-Role-Authority-Contract.md) | Contract |
| P3-A3 | [Shared Spine Publication Contract Set](P3-A3-Shared-Spine-Publication-Contract-Set.md) | Contract |

### Workstream B — Project context continuity and switching

| Doc ID | Title | Status |
|---|---|---|
| P3-B1 | [Project Context Continuity and Switching Contract](P3-B1-Project-Context-Continuity-and-Switching-Contract.md) | Contract |
| ~~P3-B2~~ | ~~Project Switching Behavior Note~~ — consolidated into P3-B1 | — |
| ~~P3-B3~~ | ~~Context Restoration and Mismatch-Reconciliation Rules~~ — consolidated into P3-B1 | — |

### Workstream C — Canvas-first Project Home

| Doc ID | Title | Status |
|---|---|---|
| P3-C1 | [Project Canvas Governance Note](P3-C1-Project-Canvas-Governance-Note.md) | Note |
| P3-C2 | [Mandatory Core Tile Family Definition](P3-C2-Mandatory-Core-Tile-Family-Definition.md) | Specification |
| P3-C3 | [Lane-Aware Home/Canvas Capability Matrix](P3-C3-Lane-Aware-Home-Canvas-Capability-Matrix.md) | Specification |

### Workstream D — Shared project spines

| Doc ID | Title | Status |
|---|---|---|
| P3-D1 | [Project Activity Contract](P3-D1-Project-Activity-Contract.md) | Contract |
| P3-D2 | [Project Health Contract](P3-D2-Project-Health-Contract.md) | Contract |
| P3-D3 | [Project Work Queue Contract](P3-D3-Project-Work-Queue-Contract.md) | Contract |
| P3-D4 | [Related-Items Registry / Presentation Contract](P3-D4-Related-Items-Registry-Presentation-Contract.md) | Contract |

### Workstream E — Always-on core module delivery and PH7 reconciliation

| Doc ID | Title | Status |
|---|---|---|
| P3-E1 | [Phase 3 Module Classification Matrix](P3-E1-Phase-3-Module-Classification-Matrix.md) | Specification |
| P3-E2 | [Module Source-of-Truth / Action-Boundary Matrix](P3-E2-Module-Source-of-Truth-Action-Boundary-Matrix.md) | Specification |
| P3-E3 | [Spreadsheet/Document Replacement Reference Note Set](P3-E3-Spreadsheet-Document-Replacement-Reference-Note-Set.md) | Note |
| P3-E4 | [Financial Module Field Specification](P3-E4-Financial-Module-Field-Specification.md) *(master index + T01–T09 detail files)* | Specification |
| P3-E5 | [Schedule Module Field Specification](P3-E5-Schedule-Module-Field-Specification.md) *(master index + T01–T11 detail files)* | Specification |
| P3-E6 | [Constraints Module Field Specification](P3-E6-Constraints-Module-Field-Specification.md) *(master index + T01–T08 detail files)* | Specification |
| P3-E7 | [Permits Module Field Specification](P3-E7-Permits-Module-Field-Specification.md) *(master index + T01–T08 detail files)* | Specification |
| P3-E8 | [Safety Module Field Specification](P3-E8-Safety-Module-Field-Specification.md) *(master index + T01–T10 detail files)* | Specification |
| P3-E9 | [Reports Module Field Specification](P3-E9-Reports-Module-Field-Specification.md) *(master index + T01–T10 detail files)* | Specification |
| P3-E10 | [Project Closeout Module Field Specification](P3-E10-Project-Closeout-Module-Field-Specification.md) *(master index + T01–T11 detail files)* | Specification |
| P3-E11 | [Project Startup Module Field Specification](P3-E11-Project-Startup-Module-Field-Specification.md) | Specification |
| P3-E12 | [Subcontract Compliance Module Field Specification](P3-E12-Subcontract-Compliance-Module-Field-Specification.md) | Superseded Reference |
| P3-E13 | [Subcontract Execution Readiness Module Field Specification](P3-E13-Subcontract-Execution-Readiness-Module-Field-Specification.md) *(master index + T01–T08 detail files)* | Specification |
| P3-E14 | [Project Warranty Module Field Specification](P3-E14-Project-Warranty-Module-Field-Specification.md) *(master index + T01–T10 detail files)* | Specification |

### Workstream F — Governed reporting system

| Doc ID | Title | Status |
|---|---|---|
| P3-F1 | [Reports Workspace / Definition / Run / Release Contract Package](P3-F1-Reports-Workspace-Definition-Run-Release-Contract-Package.md) | Contract |

### Workstream G — Dual-lane capability and coexistence

| Doc ID | Title | Status |
|---|---|---|
| P3-G1 | [Lane Capability Matrix (PWA / SPFx)](P3-G1-Lane-Capability-Matrix.md) | Specification |
| P3-G2 | [Cross-Lane Navigation and Handoff Map](P3-G2-Cross-Lane-Navigation-and-Handoff-Map.md) | Specification |
| P3-G3 | [Lane-Specific Acceptance Matrix](P3-G3-Lane-Specific-Acceptance-Matrix.md) | Specification |

### Workstream H — Validation, staging, and acceptance

| Doc ID | Title | Status |
|---|---|---|
| P3-H1 | [Acceptance, Staging, and Release-Readiness Checklist](P3-H1-Acceptance-Staging-Release-Readiness-Checklist.md) | Active Reference |

### Workstream I — Shared Feature Infrastructure

Shared feature plan files live in `docs/architecture/plans/shared-features/`. These plan families govern the six Addendum A packages incorporated into Phase 3 as Stage 5.

| SF Plan | Title | Status |
|---|---|---|
| [SF28-Activity-Timeline.md](../../shared-features/SF28-Activity-Timeline.md) | Activity Timeline (`@hbc/activity-timeline`) | Specification |
| [SF24-Export-Runtime.md](../../shared-features/SF24-Export-Runtime.md) | Export Runtime (`@hbc/export-runtime`) | Specification |
| [SF23-Record-Form.md](../../shared-features/SF23-Record-Form.md) | Record Form (`@hbc/record-form`) | Specification |
| [SF26-Saved-Views.md](../../shared-features/SF26-Saved-Views.md) | Saved Views (`@hbc/saved-views`) | Specification |
| [SF25-Publish-Workflow.md](../../shared-features/SF25-Publish-Workflow.md) | Publish Workflow (`@hbc/publish-workflow`) | Specification |
| [SF27-Bulk-Actions.md](../../shared-features/SF27-Bulk-Actions.md) | Bulk Actions (`@hbc/bulk-actions`) | Specification |

### Workstream J — Documents Enabling Seams

Phase 3 document enablement delivers the seams and contracts that make the unified Documents feature implementable in Phase 5 without architectural rework. Full Documents implementation (global shell, smart views, platform-wide search) is explicitly Phase 5 scope. The governing background plan is `04_Phase-3_Unified-Documents-Enabling-Plan.md`.

| Doc ID | Title | Status |
|---|---|---|
| P3-J1 | [Documents Enabling Seams and Contracts](P3-J1-Documents-Enabling-Seams-and-Contracts.md) | Specification |

---

## Implementation Guide

This guide provides a comprehensive, ordered task list for a full implementation of the Phase 3 Project Hub as defined by the plan files in this directory. Tasks are organized into stages that reflect dependency order. Within a stage, tasks may proceed in parallel unless otherwise noted.

---

### Stage 0 — Pre-Implementation Evaluation (Blocking)

Evaluate all blocker dependencies before committing Phase 3 to active development. Proceeding without resolving these risks mid-sprint blockers.

**0.1 — `@hbc/field-annotations` anchor gap assessment**
Determine whether `@hbc/field-annotations` supports section-level and block-level anchor targeting, not just classic form-field anchors. This is a hard blocker for the executive review annotation layer on Financial, Schedule, Constraints, Permits, Project Closeout, Project Startup, and Subcontract Execution Readiness surfaces. If the gap exists, a remediation plan must be agreed before Phase 3 executive review work begins.
Governing: P3-E1 §9.2, P3-E2 §3.4, §14.4, §15.4, §16.4
Status: **Assessed 2026-03-22 — gap confirmed, remediation delivered in v0.2.0.** Section/block anchor support added via `AnchorType` discriminator (`'field' | 'section' | 'block'`) and `HbcAnnotationAnchor` wrapper component. Existing field-level annotations unaffected. Stage 8 unblocked.

**0.2 — `@hbc/auth` AccessControlOverrideRecord readiness**
Confirm that `@hbc/auth` can issue, store, and revoke time-bounded AccessControlOverrideRecords for out-of-scope PER grants. If this record type does not yet exist, it must be added to `@hbc/auth` before the PER authority model can be implemented.
Governing: P3-A2 §2.3, §2.4, §6.1, §11.8
Status: **Assessed 2026-03-22 — gap confirmed, remediation delivered in v0.4.0.** `AccessControlOverrideRecord` extended with `overrideType`, `projectIds`, `department` fields and `approverScope` on approval metadata. PER-specific helpers added (`createPerOverrideRequest`, `isPerOverride`, `getPerOverridesForUser`, `getPerOverridesForProject`, `suspendPerOverridesForDepartmentChange`). Existing override records unaffected. Stage 1 unblocked.

**0.3 — `@hbc/my-work-feed` work item type readiness**
Confirm that `@hbc/my-work-feed` (ADR-0115) supports structured typed work items with provenance fields (originRole, originReviewRunId, originAnnotationId, pushTimestamp) and a closure-loop response model. The Push-to-Project-Team feature requires this. If the work item model is narrower, agree on a work item type extension before executive review work begins.
Governing: P3-D3 §5, P3-F1 §8.5
Status: **Assessed 2026-03-22 — gap confirmed, remediation delivered in v0.0.37.** `IMyWorkSourceMeta` extended with `originRole`, `originAnnotationId`, `originReviewRunId`, and `pushTimestamp` provenance fields. Push-to-Project-Team helpers added (`createPushToTeamSourceMeta`, `isPushToTeamItem`, `getPushProvenance`). Closure-loop state machine already supported. Existing work items and adapters unaffected. Stage 3 unblocked.

**0.4 — SPFx shell cross-lane escalation affordance**
Confirm that the SPFx shell can surface a Launch-to-PWA affordance and pass deep-link parameters including `projectId`, `module`, `action`, and `reviewArtifactId`. This is required for all SPFx-to-PWA escalation scenarios.
Governing: P3-G2 §3, §8.8
Status: **Assessed 2026-03-22 — gap confirmed, remediation delivered in `@hbc/shell` v0.0.5.** `buildPwaDeepLink` URL builder and `useLaunchToPwa` hook added. Supports `projectId`, `module`, `action`, `view`, `reviewArtifactId`, `returnTo`, `source` parameters per P3-G2 §7. All SPFx-to-PWA escalation scenarios unblocked.

---

### Stage 1 — Project Registry and Identity Foundation

All subsequent stages depend on a functioning project registry and canonical identity resolution.

**1.1 — Project registry record shape**
Implement the project registry record with all required fields: `projectId` (UUID, canonical), `projectNumber` (legacy reference key), `siteUrl`, `status`, `department`, `activatedAt`, and `activationSource`. The `department` field is the authoritative scope key for leadership authority calculations.
Governing: P3-A1 §2–§3
Status: **Implemented 2026-03-22 in `@hbc/models` v0.4.0.** `IProjectRegistryRecord` (24 fields), `ISiteAssociation`, `ProjectLifecycleStatus`, and `toActiveProject` backward-compatible mapper added. Existing `IActiveProject` and `ProjectStatus` preserved for backward compatibility.

**1.2 — Dual-key inbound routing normalization**
Implement the transitional inbound routing layer that accepts both `projectId` (UUID) and `projectNumber` (legacy string). All inbound `projectNumber` references must resolve to `projectId` via a registry lookup before any internal processing. All internal state, links, and cross-lane handoffs use `projectId` exclusively. Redirect `projectNumber`-based routes to canonical `projectId`-based routes.
Governing: P3-A1 §3.4–§3.6, P3-B1 §2.2
Status: **Implemented 2026-03-22 in `@hbc/data-access` v0.5.0.** `getProjectByNumber` added to `IProjectRepository`. `normalizeProjectIdentifier` utility detects identifier kind (UUID vs `##-###-##`), resolves `projectNumber` to canonical `projectId` via registry lookup, and signals redirect requirement. Mock and proxy adapters updated.

**1.3 — Project activation transaction — setup seam**
Implement the setup seam activation path: new project setup flow produces a valid project registry entry with `projectId`, `siteUrl`, and `status: 'active'`. Incomplete activations must be rejected atomically — no orphaned records.
Governing: P3-A1 §4
Status: **Implemented 2026-03-22 in `@hbc/provisioning` v0.3.0.** `validateActivationPreconditions` (8 precondition checks per P3-A1 §5.2) and `buildRegistryRecord` (pure function producing all 24 fields with UUID v4 projectId, primary site association, audit trail) added. Handoff config `onAcknowledged` updated to use activation pipeline. Full runtime wiring (repository injection, auth context) is Phase 3 integration scope.

**1.4 — Project activation transaction — handoff seam**
Implement the handoff seam activation path for projects pre-existing in other systems. Validate that handoff data satisfies all required registry fields before committing the registry entry.
Governing: P3-A1 §4
Status: **Implemented 2026-03-22 in `@hbc/provisioning` v0.3.1.** `HandoffActivationInput` (provisioning-independent), `validateHandoffActivationPreconditions`, and `buildRegistryRecordFromHandoff` added. Produces same `IProjectRegistryRecord` as setup seam. Both activation paths now complete.

**1.5 — Registry lookup service**
Implement the internal lookup service used by the dual-key routing layer, cross-lane handoff construction, and authority scoping. This service is the single resolution point for `projectNumber → projectId` normalization.
Governing: P3-A1 §3.5
Status: **Implemented 2026-03-22 in `@hbc/data-access` v0.6.0.** `IProjectRegistryService` with `getByProjectId`, `getByProjectNumber`, `getBySiteUrl`, `resolveProjectIdentity`, and `listByDepartment`. Returns `IProjectRegistryRecord`. Mock implementation and factory function added. Stage 1 complete.

**1.6 — Governed department reclassification flow**
Implement the department change workflow: Manager of OpEx approval, audit record creation, downstream visibility recalculation (all role-scoped authority re-evaluated for the new department value), and suspension of any active PER override records for the project until re-grant.
Governing: P3-A1 §3.6, P3-A2 §7
Status: **Implemented 2026-03-22 in `@hbc/data-access` v0.7.0.** `validateReclassificationAuthority` and `executeDepartmentReclassification` added. Enforces Manager of OpEx approval, rejects no-ops, updates department atomically, suspends PER overrides via `suspendPerOverridesForDepartmentChange`, records audit timestamp. `IProjectRegistryService.reclassifyDepartment` added. Stage 1 fully complete.

---

### Stage 2 — Authority, Membership, and Role Resolution

Must follow Stage 1 (requires `projectId` and `department` from registry).

**2.1 — Three-tier authority model implementation**
Implement role resolution for all three tiers: Leadership (department-scoped, all projects in department), Portfolio Executive Reviewer (department-scoped, non-membership, review-layer only), and Project Executive (project-scoped, full operational authority). Resolve authority against `department` from the project registry record, not against projectType or any other field.
Governing: P3-A2 §3
Status: **Implemented 2026-03-22 in `@hbc/models` v0.4.1 + `@hbc/auth` v0.5.0.** `ProjectRole` type (8 roles) with tier classification, precedence, labels, and membership flags added to models. `resolveProjectRole` pipeline (9 eligibility paths per P3-A2 §6.1) and `resolvePerEligibility` (department scope, C-suite, override) added to auth. Pure functions — caller provides all inputs.

**2.2 — Membership enforcement**
Implement project membership validation. Membership is role-based and project-scoped. Implement the membership resolver used by both lanes (PWA route guards and SPFx web part initialization). The resolver must distinguish between membership-based access and PER oversight access — these are separate access paths.
Governing: P3-A2 §6
Status: **Implemented 2026-03-22 in `@hbc/auth` v0.5.1.** `validateProjectAccess` enforcement wrapper with typed denial reasons (`no-eligibility-path`, `project-not-found`, `external-member-expired`). `ProjectMembershipGate` React component for project-scoped route/component enforcement. Wraps `resolveProjectRole` from 2.1.

**2.3 — PER non-membership scoping**
Implement the PER access path: a PER user reaches a project through department scope, not project membership. PER access grants read and annotation rights on review-capable surfaces only. PER access does not grant any project membership, operational write, or source-of-truth mutation rights. Implement the scope check that validates PER access without a membership record.
Governing: P3-A2 §3.2, §6.4
Status: **Implemented 2026-03-22 in `@hbc/auth` v0.6.0.** Module visibility matrix (`getModuleVisibility`) with per-role access levels. PER scope helpers (`isPerRole`, `canPerAnnotate`, `canPerPushToTeam`, `getPerRestrictions`). Safety annotation excluded per P3-E1 §9.3.

**2.4 — AccessControlOverrideRecord for out-of-scope PER grants**
Implement time-bounded override records in `@hbc/auth` that grant a PER user access to a project outside their normal department scope. Records must include: grantedBy, expiresAt, scope, and reason. Implement revocation on expiry and on department reclassification.
Governing: P3-A2 §5.3
Status: **Implemented 2026-03-22 in `@hbc/auth` v0.6.1.** Full PER override infrastructure delivered across 0.2 (type extensions, helpers), 1.6 (department reclassification revocation), 2.1 (resolvePerEligibility integrates overrides). `getActivePerOverrides` added for expiry-aware filtering via `resolveOverrideLifecycleStatus`.

**2.5 — Role visibility rules**
Implement the role-based visibility layer used by the canvas, module surfaces, and spine components. Visibility is computed from resolved authority tier and project membership state. PER visibility rules differ from membership-based visibility rules.
Governing: P3-A2 §7, P3-C2 §8
Status: **Implemented 2026-03-22 in `@hbc/auth` v0.7.0.** `getTileVisibility`, `getVisibleTileKeys`, and `getSpineVisibility` added for canvas tile and spine component role-based visibility per P3-C2 §8. PER/Viewer/External tile restrictions enforced. Stage 2 (Authority & Membership) complete.

---

### Stage 3 — Shared Spine Publication Infrastructure

Must follow Stage 2 (role context required for spine publication filtering).

**3.1 — Spine adapter interface definitions**
Define the four spine adapter interfaces (Activity, Health, Work Queue, Related Items) with their publication contracts. All module adapters must implement these interfaces. No module should bypass the adapter interface to write directly to a spine.
Governing: P3-A3 §4–§7
Status: **Implemented 2026-03-22 in `@hbc/models` v0.5.0.** Activity spine types added: `IProjectActivityEvent`, `IActivitySourceAdapter`, `IActivitySourceRegistration`, `IActivityQuery`, `ActivityCategory` (7 values), `ActivitySignificance` (3 tiers). Health (SF21), Work Queue (SF29/ADR-0115), and Related Items (SF14) adapter interfaces already mature — no changes needed.

**3.2 — Activity spine (`@hbc/features-project-hub` activity integration)**
Implement the activity publication adapter. Module events publish to the activity spine via the adapter. Activity spine renders the PWA full-timeline view and the SPFx tile view. Spine data is consistent for the same `projectId` in both lanes.
Governing: P3-A3 §7.1, P3-D1
Status: **Implemented 2026-03-22 in `@hbc/features-project-hub` v0.1.0 + `@hbc/models` v0.5.1.** `ProjectActivityRegistry` singleton (freeze-on-write, module-keyed), `aggregateActivityFeed` pipeline (multi-adapter load, dedup, sort, filter, count), and `IActivityFeedResult` type added. Module-specific adapters are Stage 4+ scope.

**3.3 — Health spine (`@hbc/features-project-hub` health-pulse, ADR-0110)**
Implement the health spine. Health metrics are computed from module contributions. The health-pulse component exposes full detail + explainability in PWA and a shared component in SPFx. The executive review annotation layer does not write to health spine metrics.
Governing: P3-A3 §7.2, P3-D2, ADR-0110
Status: **Compliant — already implemented at production maturity (SF21, ADR-0110).** Per P3-D2 §1: 13 types, 5 computor pipeline stages, 7 UI components, 7 integration adapter families, BIC/notification registrations, governance validation — all live. Module-specific metric adapters are controlled-evolution (Stage 4+). Annotation isolation enforced via Stage 2.3 module visibility (`review-layer` for PER on health). No new implementation required.

**3.4 — Work Queue spine (`@hbc/my-work-feed`, ADR-0115)**
Implement the work queue spine. Modules publish actionable items to the work queue. Implement the work item type model with provenance fields required by Push-to-Project-Team. Implement the closure-loop model (PER push → PM response → PER confirmation). Work queue renders as full feed+panel+team view in PWA and tile+panel in SPFx.
Governing: P3-A3 §7.3, P3-D3, ADR-0115

**3.5 — Related Items registry and presentation**
Implement the related items registry. Modules register cross-module relationships by publishing relationship records. PER is a registered role in `visibleToRoles`. Review circle restriction must be enforced: a project team member cannot push to review circle if a PER push is already open on the same item. Related items renders as full panel + AI suggestions in PWA and compact panel in SPFx.
Governing: P3-A3 §7.4, P3-D4
Status: **Compliant — already implemented at production maturity (SF14, ADR-0103).** Per P3-D4 §1: `RelationshipRegistry` singleton, `RelatedItemsApi` (batched, BIC-enriched, AI suggestions), `useRelatedItems` hook, Panel/Tile/Card components, reference registrations (BD ↔ Estimating), AI suggestion hook, testing exports — all live. `emitGovernanceEvent` Activity spine integration is cross-spine scope. No new implementation required.

---

### Stage 4 — Project Context Continuity and Cross-Lane Routing

Must follow Stage 1 (registry) and Stage 2 (auth). Can proceed in parallel with Stage 3.

**4.1 — PWA routing shell integration**
Integrate project context into the PWA routing shell. Routes are `projectId`-based. The context header resolves project identity from the URL and hydrates module context. Smart project switching operates in-app without page reload.
Governing: P3-B1 §2.1, §4
Status: **Implemented 2026-03-23 in `@hbc/shell` v0.1.0.** `resolveProjectRouteContext` for route-entry normalization + access validation (composes `normalizeProjectIdentifier` from 1.2). Per-project return memory (`saveReturnMemory`, `getReturnMemory`, `clearReturnMemory`, `pruneExpiredReturnMemory`) with 7-day TTL and 50-entry LRU in localStorage per P3-B1 §4. PWA route parameterization is app-level integration consuming this infrastructure.

**4.2 — SPFx host-aware context resolution**
Implement SPFx-side project identity resolution: `siteUrl` → registry lookup → `projectId`. The SharePoint web part must initialize with a valid `projectId` before rendering any module surface or spine component.
Governing: P3-B1 §2.3, §3
Status: **Implemented 2026-03-23 in `@hbc/shell` v0.1.1.** `resolveSpfxProjectContext` accepts pre-fetched registry record from `getBySiteUrl()` (from 1.5), returns canonical `projectId`/`projectName`/`projectNumber` or not-found state. Never fabricates context per P3-B1 §2.3.

**4.3 — Smart project switching**
Implement the project-switching mechanism: PWA uses in-app context switcher with return-memory; SPFx uses host-aware navigation with fallbacks. Both lanes must reset module context on project switch and produce no stale state.
Governing: P3-B1 §5
Status: **Implemented 2026-03-23 in `@hbc/shell` v0.2.0.** `resolveProjectSwitch` implementing P3-B1 §5 same-page model: same-page → return-memory → project-home precedence. Saves return memory for departing project, extracts module path for same-page resolution.

**4.4 — Deep-link handler and cross-lane handoff**
Implement the deep-link handler that processes incoming cross-lane arrivals in PWA. Handler must extract `projectId`, `module`, `action`, and optional `reviewArtifactId` from the landing URL, normalize any `projectNumber` via registry, and route to the correct context. All outbound deep links from SPFx must use `projectId` — `projectNumber` must be normalized before link construction.
Governing: P3-B1 §6, P3-G2 §5, §8.8
Status: **Implemented 2026-03-23 in `@hbc/shell` v0.2.1.** `parseDeepLinkParams` and `buildTargetPathFromDeepLink` for inbound SPFx-to-PWA deep links. Extracts projectId, module, action, view, reviewArtifactId, returnTo, source. Outbound construction already delivered in `buildPwaDeepLink` (Stage 0.4).

**4.5 — Context restoration and mismatch reconciliation**
Implement context restoration for users returning to a project after a session break or cross-lane handoff. Implement mismatch reconciliation when the resolved context does not match the user's current membership or authority state.
Governing: P3-B1 §7
Status: **Implemented 2026-03-23 in `@hbc/shell` v0.3.0.** `reconcileProjectContext` implementing P3-B1 §7 route-wins rule: detects store/route mismatch, returns authoritative projectId, signals store sync needed, preserves previousProjectId for return-memory. Stage 4 (Project Context Continuity) complete.

---

### Stage 5 — Shared Feature Infrastructure Completion

Can begin after Stage 1 (project registry context available for adapter configuration) and proceeds in parallel with Stages 2–4. Must complete before Stage 7 (core module implementation begins). Items 5.1–5.4 may proceed in parallel; 5.6 (Bulk Actions) must follow 5.4 (Saved Views).
Governing: Workstream I plan files in `docs/architecture/plans/shared-features/`; P3-E1 §13 per-module integration contracts.

**5.1 — Activity Timeline runtime (`@hbc/activity-timeline`, SF28)**
Deliver the shared activity-timeline package: normalized event contracts, emitter helpers, append-only storage adapters (SharePoint MVP), query hooks, and `@hbc/ui-kit` timeline surfaces. This is the runtime implementation of the Activity Spine contract defined in P3-D1. All 9 Phase 3 modules depend on this package for activity spine emission. Module-level adapter wiring is Stage 7 scope.
Governing: P3-D1; `docs/architecture/plans/shared-features/SF28-Activity-Timeline.md`
**SF28-T01 scaffold:** **Implemented 2026-03-23 in `@hbc/activity-timeline` v0.0.1.** Package scaffold with types (re-exporting P3-D1 contracts from `@hbc/models`), model, formatters, storage, adapters, hooks, telemetry barrels, testing subpath, and README per SF28 locked decisions L-01/L-04/L-08/L-09. Layer 8 shared-feature primitive.
**SF28-T02 contracts:** **Implemented 2026-03-23 in `@hbc/activity-timeline` v0.0.2.** Full type layer: `IActivityEvent` normalized model, `IActivityActorAttribution` (initiatedBy/executedBy/onBehalfOf/serviceIdentity), `IActivityObjectRef`/`IActivityRelatedRef`, `IActivityDiffEntry`, `IActivityContextStamp`, `IActivityDedupeState`, confidence/sync states, `IActivityTimelineQuery`/`IActivityFilterState`, `IActivityTimelinePage`/`IActivityEventGroup`, `IActivityEmissionInput`, `IActivityStorageRecord`, `IActivitySourceHealthState`, 5 reason-code enums, 4 locked constants. Per L-02/L-05/L-06/L-07/L-10.
**SF28-T03 normalization+storage:** **Implemented 2026-03-23 in `@hbc/activity-timeline` v0.0.3.** `normalizeActivityEvent` (emission → normalized event with UUID, actor completion, confidence/sync state), `applyDeduplication` (projection-only, `rawEvidenceRetained: true`), `IActivityStorageAdapter` interface, `InMemoryStorageAdapter` (dev/test). Per L-02/L-07.
**SF28-T04 hooks:** **Implemented 2026-03-23 in `@hbc/activity-timeline` v0.0.4.** 8 hooks: `useActivityTimeline` (core query orchestration + dedup), `useActivityFilters` (filter state), `useActivityEmitter` (normalize wrapper), `useActivityDiff` (expansion state), 3 mode projections (workspace/record/related), `useActivitySourceHealth` (degradation). Query key factory `activityTimelineKeys`.
**SF28-T05 UI surfaces:** **Implemented 2026-03-23 in `@hbc/ui-kit` v2.2.73.** `HbcActivityTimeline` (grouped reverse-chronological feed with loading/empty/degraded states), `ActivityEventRow` (event renderer with icon/actor/summary/timestamp/diff affordance/context badges), `ActivityEventIcon` (18 event-type icon mapping with user/system visual distinction). Per L-08.
**SF28-T06 filter/diff/empty:** **Implemented 2026-03-23 in `@hbc/ui-kit` v2.2.74.** `ActivityFilterBar` (event type chips, system-event toggle, reset), `ActivityDiffPopover` (field-level from/to with suppression reasons), `ActivityEmptyState` (4 variants: no-activity, no-results, degraded, loading). Per L-08.
**SF28-T07 emitter+adapters:** **Implemented 2026-03-23 in `@hbc/activity-timeline` v0.0.5.** `ActivitySourceRegistry` singleton (freeze-on-write, duplicate rejection, getAll/getByModule/getEnabled), `emitActivityEvent` non-React emission helper. All emission through canonical contract — no free-form payloads.
**SF28-T08 testing:** **Implemented 2026-03-23 in `@hbc/activity-timeline` v0.0.6.** `@hbc/activity-timeline/testing` subpath with `createMockActivityEvent`, `createMockEmissionInput`, `createMockSourceAdapter`, `createMockSourceRegistration`, and `mockActivityScenarios` (10 canonical fixtures covering user/system/workflow/service actors, diffs, correlation, replay, dedup, degradation, workspace projection).
**SF28-T09 closure:** **Complete 2026-03-23. `@hbc/activity-timeline` v0.1.0 (feature-complete).** Full SF28 delivered: T01 scaffold, T02 contracts (14 interfaces + 5 enums + 4 constants), T03 normalization + storage, T04 hooks (8 + query keys), T05 ui-kit timeline/row/icon, T06 ui-kit filter/diff/empty, T07 registry + emitter, T08 testing factories. 72 tests passing. ADR-0114, adoption guide, and API reference are documentation scope.
**Status: 5.1 Activity Timeline — COMPLETE.**

**5.2 — Export Runtime (`@hbc/export-runtime`, SF24)**
Deliver the shared export runtime package: CSV/XLSX/PDF render pipeline, export receipt model, artifact provenance stamping, offline replay, and module adapter scaffold for Phase 3 consumers. The saved-view context handoff (`IExportRequest.savedViewContext`) contract is established during Stage 5.4 but the export primitive is scaffolded here. Module-level adapter wiring is Stage 7 scope.
Governing: `docs/architecture/plans/shared-features/SF24-Export-Runtime.md`
**SF24-T01 scaffold:** **Implemented 2026-03-23 in `@hbc/export-runtime` v0.0.1.** Package scaffold with types (export truth vocabulary, receipt states, artifact confidence, review steps, next recommended action, failure/retry diagnostics), model, api, hooks, components, composers, renderers, templates barrels, testing subpath, and README per SF24 locked decisions L-01/L-03/L-04/L-06. Layer 8 shared-feature primitive. Feature adapter seams in `@hbc/features-business-development` and `@hbc/features-estimating`.
**SF24-T02 contracts:** **Implemented 2026-03-23 in `@hbc/export-runtime` v0.0.2.** Canonical TypeScript contracts: `IExportTruthState`, `ITableExportPayload`/`IReportExportPayload` (discriminated `ExportPayload` union), `IExportBicStepConfig` (L-02 BIC ownership), `IExportVersionRef` with `ExportVersionTag` (L-06 provenance), `IExportTelemetryState` (5 UX KPIs), `IExportSuppressedFormatState`, `IExportContextDeltaState`. `IExportRequest` expanded with payload, truth, bicSteps, versionRef, telemetry, suppressedFormats, contextDelta. T02 constants locked. Per L-01 through L-06.
**SF24-T03 lifecycle+storage:** **Implemented 2026-03-23 in `@hbc/export-runtime` v0.0.3.** Export lifecycle state machine (`createExportRequest`, `transitionExportStatus`, `VALID_TRANSITIONS` with monotonic enforcement), confidence computation (`computeArtifactConfidence`, `detectContextDelta`), deterministic file naming (`generateExportFileName`), governance audit trail (`createAuditEntry`, `IExportAuditEntry`), storage adapter interface (`IExportStorageAdapter`, `IExportStorageRecord`), and `InMemoryExportStorageAdapter` (dev/test). Per L-01, L-02, L-04, L-06.
**SF24-T04 hooks:** **Implemented 2026-03-23 in `@hbc/export-runtime` v0.0.4.** Three primitive hooks: `useExportRuntimeState` (query orchestration, derived explainability, top recommended export, queue count), `useExportCompositionState` (report section drafts, toggle/reorder/reset, validation), `useExportQueue` (offline pending list, replay transitions, receipt restoration). Query key factory `exportRuntimeKeys`. Stable return shapes, model-layer composition, no TanStack Query wiring. Per L-01 through L-06.
**SF24-T05 menu+picker:** **Implemented 2026-03-23 in `@hbc/ui-kit` v2.2.75 + `@hbc/export-runtime` v0.0.5.** `ExportActionMenu` and `ExportFormatPicker` in `@hbc/ui-kit` (data-in/callbacks-out, complexity tier rendering, recommended export badge, suppressed format hints, BIC avatar projection, context stamp preview, duplicate guard, offline status). Composition shells `ExportActionMenuShell` and `ExportFormatPickerShell` in `@hbc/export-runtime` wiring hooks to ui-kit. Per L-02, L-03, L-05, L-06.
**SF24-T06 progress+receipt:** **Implemented 2026-03-23 in `@hbc/ui-kit` v2.2.76 + `@hbc/export-runtime` v0.0.6.** `ExportProgressToast` in `@hbc/ui-kit` (lifecycle state projection, explainability messages, retry guidance, artifact link, guarded dismiss). `ExportReceiptCard` in `@hbc/ui-kit` (immutable context stamps, trust state with degraded/restored warnings, BIC review owners, related deep-links, download/re-export actions). Composition shells `ExportProgressToastShell` and `ExportReceiptCardShell` in `@hbc/export-runtime`. Per L-03, L-04, L-05, L-06.
**SF24-T07 integrations:** **Implemented 2026-03-23 in `@hbc/export-runtime` v0.0.7.** `ExportModuleRegistry` singleton (additive, freeze-on-write, duplicate rejection), `IExportModuleRegistration` and `IExportModuleTruthProvider` contracts, `createModuleExportRequest` helper. Reference adapters in `@hbc/features-business-development` (standard tier, csv/xlsx/pdf) and `@hbc/features-estimating` (essential tier, csv/xlsx). Per L-01 through L-06.
**SF24-T08 testing:** **Implemented 2026-03-23 in `@hbc/export-runtime` v0.0.8.** `@hbc/export-runtime/testing` subpath with `createMockExportRequest`, `createMockReceiptState`, `createMockTruthState`, `createMockReviewStepState`, `createMockQueueState`, `mockExportScenarios` (11 canonical fixtures), `mockExportComplexityProfiles`. 70 unit tests (6 files) covering lifecycle transitions, confidence computation, naming, governance audit, registry, and module request creation. Coverage: 99% lines, 95% branches, 100% functions. Per L-01 through L-06.
**SF24-T09 closure:** **Complete 2026-03-23. `@hbc/export-runtime` v0.1.0 (feature-complete).** Full SF24 delivered: T01 scaffold, T02 contracts (30+ types/interfaces, 8 constants), T03 lifecycle state machine + storage adapter, T04 hooks (3 + query keys), T05 ui-kit menu/picker, T06 ui-kit progress/receipt, T07 registry + reference adapters, T08 testing (70 tests, 99%/95%/100% coverage). ADR-0119, adoption guide, and API reference authored. 6 locked decisions (L-01–L-06) satisfied.
**Status: 5.2 Export Runtime — COMPLETE.**

**5.3 — Record Form runtime (`@hbc/record-form`, SF23)**
Deliver the shared record-form package: create/edit/duplicate/template lifecycle, draft recovery, review/handoff orchestration, offline replay, and module adapter scaffold for all 9 Phase 3 modules. Each module adapter owns its domain schema; the primitive owns lifecycle, offline resilience, and telemetry. Module-level adapter wiring is Stage 7 scope.
Governing: `docs/architecture/plans/shared-features/SF23-Record-Form.md`
**SF23-T01 scaffold:** **Implemented 2026-03-23 in `@hbc/record-form` v0.0.1.** Package scaffold with types (trust-state vocabulary, recovery states, review steps, next recommended action, draft persistence, failure/retry diagnostics, telemetry), model, api, hooks, components, adapters barrels, testing subpath, and README per SF23 locked decisions L-01/L-03/L-04/L-06. Layer 8 shared-feature primitive. Feature adapter seams (with profiles directory) in `@hbc/features-business-development` and `@hbc/features-estimating`.
**SF23-T02 contracts:** **Implemented 2026-03-23 in `@hbc/record-form` v0.0.2.** Canonical TypeScript contracts: `RecordFormMode` (5 modes), expanded `RecordFormStatus` (8 states), `RecordSyncState` (6 states), `RecordStateConfidence` (5 levels), generic `IRecordFormDefinition<TRecord>`, `IRecordFormExplanationState`, `IRecordValidationState`/`IRecordValidationWarning`/`IRecordBlockedReason`, `IRecordDraftComparisonState`, `IRecordSubmitGuardState`, `IRecordConflictState`, `IRecordBicStepConfig`, `IRecordNextRecommendedAction` (with side + category), `IRecordSyncStateInfo`, `IRecordStateConfidenceInfo`. T02 reason codes (`RecordDeferReasonCode`, `RecordRetryReasonCode`) and constants locked. Per L-01 through L-06.
**SF23-T03 lifecycle+storage:** **Implemented 2026-03-23 in `@hbc/record-form` v0.0.3.** Record lifecycle state machine (`createRecordFormSession`, `transitionRecordFormStatus`, `VALID_RECORD_TRANSITIONS` with monotonic enforcement), confidence computation (`computeRecordConfidence`, `detectDraftConflict`), draft management (`createDraft`, `markDraftDirty`, `compareDrafts`), governance audit trail (`createRecordFormAuditEntry`, `IRecordFormAuditEntry`), storage adapter interface (`IRecordFormStorageAdapter`, `IRecordFormStorageRecord`), and `InMemoryRecordFormStorageAdapter` (dev/test). Per L-01, L-02, L-04, L-06.
**SF23-T04 hooks:** **Implemented 2026-03-23 in `@hbc/record-form` v0.0.4.** Three primitive hooks: `useRecordFormState` (query orchestration, derived explanation/validation/confidence, top recommended action, pending count), `useRecordDraftPersistence` (draft save/restore/compare, dirty tracking, optimistic statuses), `useRecordSubmission` (review gate validation, submit/retry transitions via model-layer lifecycle). Query key factory `recordFormKeys`. Stable return shapes, model-layer composition. Per L-01 through L-06.
**SF23-T05 form+submitbar:** **Implemented 2026-03-23 in `@hbc/ui-kit` v2.2.77 + `@hbc/record-form` v0.0.5.** `HbcRecordForm` in `@hbc/ui-kit` (complexity-tier field rendering, validation display, recovery banner, AI placeholders, draft save, sync status badge). `HbcRecordSubmitBar` in `@hbc/ui-kit` (submit status, validation gates, BIC avatars, queue/sync indicators, blocked/warning/deferred explainability, duplicate guard). Composition shells `RecordFormShell` and `RecordSubmitBarShell` in `@hbc/record-form`. Per L-02, L-03, L-05, L-06.
**SF23-T06 review+recovery:** **Implemented 2026-03-23 in `@hbc/ui-kit` v2.2.78 + `@hbc/record-form` v0.0.6.** `HbcRecordReviewPanel` in `@hbc/ui-kit` (read-only review, BIC avatars, review-step explainability with blocking/phase/owner/reason/next-action, approve/reject actions, related deep-links). `HbcRecordRecoveryBanner` in `@hbc/ui-kit` (recovery state with local/server/restored/stale-restored source, trust warnings, compare/restore/discard/retry/resume actions, sync status, conflict count). Composition shells `RecordReviewPanelShell` and `RecordRecoveryBannerShell` in `@hbc/record-form`. Per L-03, L-04, L-05, L-06.
**SF23-T07 integrations:** **Implemented 2026-03-23 in `@hbc/record-form` v0.0.7.** `RecordFormModuleRegistry` singleton (additive, freeze-on-write, duplicate rejection), `IRecordFormModuleRegistration` and `IRecordFormSchemaProvider` contracts, `createModuleRecordFormSession` helper. Reference adapters in `@hbc/features-business-development` (standard tier, create/edit/duplicate, pursuit/scorecard) and `@hbc/features-estimating` (essential tier, create/edit, bid-readiness). Per L-01 through L-06.
**SF23-T08 testing:** **Implemented 2026-03-23 in `@hbc/record-form` v0.0.8.** `@hbc/record-form/testing` subpath with `createMockRecordFormState`, `createMockRecordRecoveryState`, `createMockRecordReviewStepState`, `createMockRecordFormQueueState`, `mockRecordFormScenarios` (11 canonical fixtures), `mockRecordFormComplexityProfiles`. 61 unit tests (6 files) covering lifecycle transitions, confidence computation, draft management, governance audit, registry, and module session creation. Coverage: 100% lines, 96% branches, 100% functions. Per L-01 through L-06.
**SF23-T09 closure:** **Complete 2026-03-23. `@hbc/record-form` v0.1.0 (feature-complete).** Full SF23 delivered: T01 scaffold, T02 contracts (30+ types/interfaces, 7 constants), T03 lifecycle state machine + storage adapter, T04 hooks (3 + query keys), T05 ui-kit form/submit bar, T06 ui-kit review/recovery, T07 registry + reference adapters, T08 testing (61 tests, 100%/96%/100% coverage). ADR-0120, adoption guide, and API reference authored. 6 locked decisions (L-01–L-06) satisfied.
**Status: 5.3 Record Form — COMPLETE.**

**5.4 — Saved Views (`@hbc/saved-views`, SF26)**
Deliver the shared saved-views package: view lifecycle, personal/team/role/system scope model, schema compatibility/reconciliation, `ISavedViewStateMapper<TState>` module adapter interface, SharePoint MVP persistence, and `@hbc/ui-kit` view picker and dialog surfaces. Establishes the `ISavedViewContext` handoff contract consumed by 5.6 (Bulk Actions) and by Stage 5.2 (Export Runtime). Module-level mapper wiring is Stage 7 scope.
Governing: `docs/architecture/plans/shared-features/SF26-Saved-Views.md`
**SF26-T01 scaffold:** **Implemented 2026-03-23 in `@hbc/saved-views` v0.0.1.** Package scaffold with types (scope model, `ISavedViewDefinition`, `ISavedViewStateMapper<TState>`, filter/sort/group/presentation contracts, schema compatibility/reconciliation, `ISavedViewContext` handoff, telemetry events), model, storage, hooks, adapters, telemetry barrels, testing subpath, and README per SF26 locked decisions L-01/L-04/L-06. Layer 8 shared-feature primitive.
**SF26-T02 contracts:** **Implemented 2026-03-23 in `@hbc/saved-views` v0.0.2.** Canonical contracts: `ISavedViewDefinition` (workspaceKey, description, ownerUserId, schemaVersion as number), `ISavedViewStateMapper<TState>` (serialize/deserialize/currentSchemaVersion/currentSchema), `ISavedViewSchemaDescriptor`, `SavedViewCompatibilityStatus` (compatible/degraded-compatible/incompatible), `ISavedViewCompatibilityResult`, `ISavedViewScopePermissions`, `ISavedViewOwnershipCheck`, `ISavedViewContext` (activeViewId/Title), `SavedViewAction` (8-variant discriminated union), expanded `IFilterClause` (11 operators). Per L-01 through L-05.
**SF26-T03 state+storage:** **Implemented 2026-03-23 in `@hbc/saved-views` v0.0.3.** View lifecycle states (8 states: active/default/saved/unsaved-changes/degraded-compatible/incompatible/pending-share/deleted), `createSavedView` factory, schema compatibility `reconcile` function (identifies removedColumns/Filters/Groups, returns compatible/degraded-compatible/incompatible with user-facing explanations), `ISavedViewsStorageAdapter` interface (loadViews/saveView/deleteView/setDefault/clearDefault), `InMemorySavedViewsStorageAdapter` (dev/test). Per L-01, L-02, L-03, L-04.
**SF26-T04 hooks:** **Implemented 2026-03-23 in `@hbc/saved-views` v0.0.4.** Three hooks: `useSavedViews` (lifecycle operations, activeView/defaultView derivation, activeViewContext handoff for SF24/SF27), `useViewCompatibility` (reconciliation check with canApply/requiresUserConfirmation), `useWorkspaceStateMapper` (serialize/deserialize/currentSchema composition). Query key factory `savedViewsKeys`. Per L-01 through L-05.
**SF26-T05 picker+chip:** **Implemented 2026-03-23 in `@hbc/ui-kit` v2.2.79 + `@hbc/saved-views` v0.0.5.** `SavedViewPicker` in `@hbc/ui-kit` (scope-grouped dropdown with personal/team/role/system sections, active/default markers, save/save-as actions, accessibility). `SavedViewChip` in `@hbc/ui-kit` (compact active view indicator with unsaved-changes dot). Composition shells `SavedViewPickerShell` and `SavedViewChipShell` in `@hbc/saved-views`. Per L-02, L-06.
**SF26-T06 dialog+badge+toggle+banner:** **Implemented 2026-03-23 in `@hbc/ui-kit` v2.2.80 + `@hbc/saved-views` v0.0.6.** `SaveViewDialog` in `@hbc/ui-kit` (save/save-as/rename/duplicate modes, scope selector with permission gating, share intent warnings, default toggle). `SavedViewScopeBadge` (compact scope indicator). `DefaultViewToggle` (set/unset default). `ViewCompatibilityBanner` (degraded-compatible warning with removed field list, apply-anyway/cancel). Composition shells `SaveViewDialogShell` and `ViewCompatibilityBannerShell`. Per L-02, L-03, L-06.
**SF26-T07 integrations:** **Implemented 2026-03-23 in `@hbc/saved-views` v0.0.7.** `SavedViewModuleRegistry` singleton (additive, freeze-on-write, module+workspace keyed), `createTanStackTableMapper` (canonical TanStack Table ISavedViewStateMapper with serialize/deserialize for column visibility, sorting, grouping, filters, column order, density), `IComplexityViewDefault` (complexity-based default view resolution). Integration contracts documented for SF24 export-runtime and SF27 bulk-actions via ISavedViewContext handoff. Per L-05, L-06.
**SF26-T08 testing:** **Implemented 2026-03-23 in `@hbc/saved-views` v0.0.8.** `@hbc/saved-views/testing` subpath with `createMockSavedViewDefinition` factory, 6 view fixtures (personal/team/role/system/degraded/incompatible), 2 permission fixtures, 2 schema fixtures. 29 unit tests (4 files) covering lifecycle creation, compatibility reconciliation (compatible/degraded/incompatible), registry, and TanStack Table mapper (serialize/deserialize). Coverage: 100% lines, 97% branches, 100% functions. Per L-01 through L-05.
**SF26-T09 closure:** **Complete 2026-03-23. `@hbc/saved-views` v0.1.0 (feature-complete).** Full SF26 delivered: T01 scaffold, T02 contracts (ISavedViewDefinition, ISavedViewStateMapper, compatibility/scope/context types), T03 lifecycle + reconciliation + storage, T04 hooks (3 + query keys), T05 ui-kit picker/chip, T06 ui-kit dialog/badge/toggle/banner, T07 registry + TanStack mapper + integration contracts, T08 testing (29 tests, 100%/97%/100% coverage). ADR-0121, adoption guide, and API reference authored. 6 locked decisions satisfied.
**Status: 5.4 Saved Views — COMPLETE.**

**5.5 — Publish Workflow (`@hbc/publish-workflow`, SF25)**
Deliver the shared publish-workflow package: publication state machine, readiness/approval flow, supersession/revocation, receipt traceability, and module adapter scaffold. Required by Reports (publication lifecycle governed by P3-F1, Stage 9) and Subcontract Execution Readiness (governed precedent-publication lifecycle and receipt traceability). Module-level adapter wiring is Stage 7 scope.
Governing: `docs/architecture/plans/shared-features/SF25-Publish-Workflow.md`
**SF25-T01 scaffold:** **Implemented 2026-03-23 in `@hbc/publish-workflow` v0.0.1.** Package scaffold with types (PublicationStatus 8 states, IReadinessState, IApprovalState, ISupersessionState, IRevocationState, IPublicationRecord, IPublicationReceipt, IReadinessRule, IApprovalRule), model, api, hooks, components, rules, adapters barrels, testing subpath, and README per SF25 locked decisions L-01/L-03/L-04/L-06. Layer 8 shared-feature primitive. Feature adapter seams in `@hbc/features-business-development` and `@hbc/features-estimating`.
**SF25-T02 contracts:** **Implemented 2026-03-23 in `@hbc/publish-workflow` v0.0.2.** Canonical contracts: `PublishState` (8 states including approved-for-publish/publishing), `IPublishRequest` (top-level orchestration with targets, approval rules, BIC steps, telemetry, context stamp), `IPublishTarget` (4 target types), `IPublishApprovalRule` (deadline + escalation), `IPublishBicStepConfig`, `IPublishTelemetryState` (5 KPIs), `IPublishReceiptContextStamp` (provenance with supersedes chain). T02 constants locked (SYNC_QUEUE_KEY, SYNC_STATUSES, VISIBILITY_POLICY). Per L-01 through L-06.
**SF25-T03 lifecycle+storage:** **Implemented 2026-03-23 in `@hbc/publish-workflow` v0.0.3.** Publish lifecycle state machine (`createPublishRequest`, `transitionPublishState`, `VALID_PUBLISH_TRANSITIONS`, `evaluateReadiness`), governance audit trail (`createPublishAuditEntry`, 10 `PublishAuditAction` types), storage adapter interface (`IPublishStorageAdapter`, `IPublishStorageRecord`), and `InMemoryPublishStorageAdapter` (dev/test). Per L-01, L-02, L-04, L-06.
**SF25-T04 hooks:** **Implemented 2026-03-23 in `@hbc/publish-workflow` v0.0.4.** Three hooks: `usePublishWorkflowState` (request loading, pending count), `usePublishReadinessState` (readiness evaluation via model-layer evaluateReadiness), `usePublishQueue` (pending list, startPublish transition). Query key factory `publishWorkflowKeys`. Per L-01 through L-06.
**SF25-T05 panel+targets:** **Implemented 2026-03-23 in `@hbc/ui-kit` v2.2.81 + `@hbc/publish-workflow` v0.0.5.** `HbcPublishPanel` in `@hbc/ui-kit` (always-visible readiness/approval/supersession/revocation/receipt sections, BIC avatars, publish/approve/reject/supersede/revoke actions). `PublishTargetSelector` in `@hbc/ui-kit` (target chips with compatibility, offline status). Composition shells `PublishPanelShell` and `PublishTargetSelectorShell`. Per L-02, L-03, L-05, L-06.
**SF25-T06 checklist+receipt:** **Implemented 2026-03-23 in `@hbc/ui-kit` v2.2.82 + `@hbc/publish-workflow` v0.0.6.** `PublishApprovalChecklist` in `@hbc/ui-kit` (pass/fail gates, BIC avatars, due status, acknowledge actions, supersession/revocation warnings). `PublishReceiptCard` in `@hbc/ui-kit` (publish ID, issue label, targets, supersession chain, BIC owners, deep-links, sync status). Composition shells `PublishApprovalChecklistShell` and `PublishReceiptCardShell`. Per L-02, L-04, L-05, L-06.
**SF25-T07 integrations:** **Implemented 2026-03-23 in `@hbc/publish-workflow` v0.0.7.** `PublishModuleRegistry` singleton (additive, freeze-on-write), `IPublishModuleRegistration` (defaultTargets, approvalRules, supportsSupersession/Revocation), `createModulePublishRequest` helper. Reference adapters in BD (PM approval, supersession+revocation) and Estimating (no approval, no supersession). Per L-01 through L-06.
**SF25-T08 testing:** **Implemented 2026-03-23 in `@hbc/publish-workflow` v0.0.8.** `@hbc/publish-workflow/testing` with `createMockPublishRequest` factory, `mockPublishScenarios` (8 fixtures). 31 unit tests (3 files) covering lifecycle transitions, readiness evaluation, governance audit, and registry. Coverage: 100% lines, 97% branches, 100% functions. Per L-01 through L-06.
**SF25-T09 closure:** **Complete 2026-03-23. `@hbc/publish-workflow` v0.1.0 (feature-complete).** Full SF25 delivered: T01–T09 covering scaffold, contracts, lifecycle+storage, hooks, panel/targets UI, checklist/receipt UI, registry+adapters, testing (31 tests, 100%/97%/100%), and closure documentation. ADR-0122, adoption guide, and API reference authored.
**Status: 5.5 Publish Workflow — COMPLETE.**

**5.6 — Bulk Actions (`@hbc/bulk-actions`, SF27)**
Deliver the shared bulk-actions package: selection semantics, per-item eligibility evaluation, execution lifecycle, mixed-result reporting, and `@hbc/ui-kit` bulk-action surfaces. Depends on 5.4 (Saved Views) for the `ISavedViewContext` execution scope handoff. Module action registrations are scaffolded here and wired into module surfaces in Stage 7.
Governing: `docs/architecture/plans/shared-features/SF27-Bulk-Actions.md`
**SF27-T01 scaffold:** **Implemented 2026-03-23 in `@hbc/bulk-actions` v0.0.1 + `@hbc/ui-kit` v2.2.83.** Package scaffold with types (SelectionScope, IBulkSelectionState, IBulkAction, IEligibilityResult, IChunkPlan, IBulkExecutionResult, ISelectionSourceAdapter, IBulkActionExecutionContext with ISavedViewContext handoff), model, selection, execution, adapters, hooks, telemetry barrels, testing subpath, and README per SF27 decisions. 6 ui-kit components: BulkSelectionBar, BulkActionMenu, BulkActionConfirmDialog, BulkActionInputDialog, BulkActionResultsPanel, SelectAllFilteredBanner. Layer 8 shared-feature primitive.
**SF27-T02 contracts:** **Implemented 2026-03-23 in `@hbc/bulk-actions` v0.0.2.** Canonical contracts: `BulkSelectionScope` (page/visible/filtered), `BulkExecutionPhase` (7 states), `BulkResultKind` (4 kinds), `IBulkActionDefinition<TInput>` (generic with destructive/permission/transactional), `IBulkSelectionSnapshot`, `IBulkEligibilityResult`, `IBulkActionContext`, `IBulkExecutionPlan`/`Progress`/`Result`, `IBulkItemExecutionResult`, `IBulkGroupedFailureReason`, `IBulkPermissionGate`, `IBulkDestructiveActionMetadata`, `IBulkAuditEmission`, `IBulkSelectionAdapter`, `IBulkResultsSummary`. 6 reason-code enums + 4 constants. Per L-01 through L-10.
**SF27-T03 selection+execution:** **Implemented 2026-03-23 in `@hbc/bulk-actions` v0.0.3.** Selection management (`createSelectionSnapshot`, `escalateScope`, `validateScopeIntegrity`), eligibility evaluation (`evaluateEligibility`, `filterEligible`), chunked execution engine (`planExecution` with configurable chunk size, `aggregateResults` with grouped failure reasons and phase derivation). Per L-03 through L-10.
**SF27-T04 hooks:** **Implemented 2026-03-23 in `@hbc/bulk-actions` v0.0.4.** `useBulkSelection` (selection state, scope escalation, validation), `useBulkActionExecution` (chunk planning, running state, async executor, result aggregation), query key factory `bulkActionKeys`. Per L-01 through L-10.
**SF27-T05 bar+menu:** **Implemented 2026-03-23 in `@hbc/bulk-actions` v0.0.5.** Composition shells `BulkSelectionBarShell` (wires useBulkSelection to BulkSelectionBar) and `BulkActionMenuShell` (maps IBulkActionDefinition to BulkActionMenuItem). Added @hbc/ui-kit dep. UI components created in T01.
**SF27-T06 confirm+input+results+banner:** **Implemented 2026-03-23 in `@hbc/bulk-actions` v0.0.6.** 4 composition shells: `BulkActionConfirmDialogShell`, `BulkActionInputDialogShell` (managed input state), `BulkActionResultsPanelShell` (maps IBulkExecutionResult to result items), `SelectAllFilteredBannerShell`. UI components from T01.
**SF27-T07 integrations:** **Implemented 2026-03-23 in `@hbc/bulk-actions` v0.0.7.** `BulkActionModuleRegistry` singleton (additive, freeze-on-write, per-module action list), `IBulkActionModuleRegistration`, `getActionsForModule` helper. Per L-01 through L-10.
**SF27-T08 testing:** **Implemented 2026-03-23 in `@hbc/bulk-actions` v0.0.8.** `@hbc/bulk-actions/testing` with `createMockBulkSelectionSnapshot` factory, `mockBulkActionScenarios` (5 fixtures). 25 unit tests (4 files) covering selection management, execution engine, eligibility, and registry. Coverage: 100% lines, 96% branches, 100% functions. Per L-01 through L-10.
**SF27-T09 closure:** **Complete 2026-03-23. `@hbc/bulk-actions` v0.1.0 (feature-complete).** Full SF27 delivered: T01 scaffold + 6 ui-kit components, T02 contracts (25+ types, 6 reason-code enums, 4 constants), T03 selection + execution + eligibility, T04 hooks, T05/T06 composition shells, T07 registry, T08 testing (25 tests, 100%/96%/100%). ADR-0123, adoption guide, and API reference authored.
**Status: 5.6 Bulk Actions — COMPLETE.**

**Stage 5 — Shared Feature Infrastructure Completion — ALL 6 ITEMS COMPLETE.**

---

### Stage 6 — Project Canvas and Home

Must follow Stage 2 (role resolution) and Stage 4 (routing). Can proceed in parallel with Stages 3 and 5.

**6.1 — Canvas governance model**
Implement the three governance tiers: locked (mandatory, immovable), role-default (visible by role, movable), and optional (catalog-selectable). Locked tiles cannot be removed or repositioned. The governance model is enforced in both lanes.
Governing: P3-C1
Status: **Implemented 2026-03-23 in `@hbc/project-canvas` v0.1.0.** Added `project-work-queue` and `project-activity` mandatory tile definitions. Upgraded `related-items` to mandatory+lockable. Added `PROJECT_ROLE_DEFAULT_TILES` mapping 7 P3-A2 project roles. 14 total reference tiles (was 12). Governance infrastructure (TileRegistry, useCanvasEditor, CanvasApi) already mature.

**6.2 — Mandatory core tile family**
Implement all five mandatory operational core tiles: Health Summary, Activity Feed, Work Queue, Financial Summary, and Schedule Summary. These tiles must be present in both lanes. Role-based visibility rules apply — tiles are hidden when the role has no access, no empty placeholders shown.
Governing: P3-C2
Status: **Implemented 2026-03-23 in `@hbc/project-canvas` v0.1.1.** `ProjectWorkQueueTile` and `ProjectActivityTile` with essential/standard/expert variants wired into tile definitions. Health tile fully implemented via SF21. Related Items tile fully implemented via SF14. Identity header is canvas shell surface per P3-C2 §2.

**6.3 — Adaptive composition and personalization**
Implement governed adaptive composition: user preferences persist (PWA: IndexedDB + server-side; SPFx: localStorage + SharePoint). Reset-to-role-default must restore mandatory tiles. Complexity tiers (essential / standard / expert) render per user preference.
Governing: P3-C3
Status: **Implemented 2026-03-23 in `@hbc/project-canvas` v0.2.0.** `useCanvasComplexity` bridge hook connecting `@hbc/complexity` context to canvas tile variant rendering. Personalization infrastructure (editor, API, role defaults, mandatory enforcement, reset-to-default) already mature from SF13/ADR-0102.

**6.4 — SPFx canvas capability**
Implement the SPFx lane canvas. Capability must match the P3-C3 lane-aware matrix. Personalization persistence uses the SPFx-appropriate storage tier.
Governing: P3-C3 §6
Status: **Implemented 2026-03-23 in `@hbc/project-canvas` v0.2.1.** `createSpfxCanvasStorageAdapter` for localStorage-based canvas persistence in SPFx per P3-C3 §6.2. Canvas foundation is lane-agnostic — same package, same governance in both lanes. SharePoint list online sync is runtime integration scope. Stage 6 (Project Canvas and Home) complete.

---

### Stage 7 — Core Module Implementation

Must follow Stage 2 (auth), Stage 3 (spine adapters), Stage 4 (routing), and Stage 5 (shared feature infrastructure). Each module must implement its P3-A3 spine adapters and consume the shared feature packages delivered in Stage 5 per P3-E1 §13. Modules may be developed in parallel.

For each module, the implementation contract is defined by P3-E2 (source-of-truth and action boundaries) and P3-E1 (classification and capability tier). Spreadsheet/document replacement guidance is in P3-E3.

**7.1 — Financial module**
Implement the governed project-financial operating surface: versioned forecast ledger (Working / ConfirmedInternal / PublishedMonthly / Superseded version lifecycle; no unlock-in-place; derivation-based editing); budget line identity model (stable `canonicalBudgetLineId` across CSV re-imports; composite fallback matching; reconciliation conditions for ambiguous matches); separated cost model (`jobToDateActualCost` distinct from `committedCosts`, both distinct from `forecastToComplete`); Financial Forecast Summary (project metadata, schedule fields, financial summary, contingency); GC/GR working model (version-scoped, division-level); Cash Flow working model (13 actuals + 18 forecast months; A/R aging read-only display); Buyout sub-domain (procurement-control surface; dollar-weighted completion metric; savings tracking and explicit disposition workflow with three destinations; `ContractExecuted` gate enforced via P3-E13 Subcontract Execution Readiness). Implement report-candidate designation and P3-F1 publication handoff. Implement field-level PER annotation scope on confirmed versions (not working versions) via `@hbc/field-annotations` with version-aware, `canonicalBudgetLineId`-anchored annotation carry-forward on version derivation. Implement spine adapters for all four spines per P3-A3. The Financial module owns its own budget/cost records as source-of-truth; no other module may write to Financial domain fields.
Governing: P3-E1 §4.1, P3-E2 §3, P3-E3 §2, **P3-E4** (field-level specification — master index + T01–T09 detail files)
**T01 Module Doctrine:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.1.** Financial module foundation: `FinancialVersionState` (4 states), `FinancialAuthorityRole` (3 roles), `FinancialAccessAction` (5 actions), `resolveFinancialVersionAccess` (12-cell access matrix per T01 §1.4 — PER hidden from Working versions, no write on confirmed, Leadership read-only throughout), 5 integration boundary declarations (T01 §1.5), governance scope, testing fixtures (12 scenarios), contract stability tests. T02–T08 build on this foundation.
**T02 Budget Line Identity:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.2.** Budget line data model (`IBudgetLineItem` — 26 fields per §3.1), `CostType` enum (5 values), identity model (`canonicalBudgetLineId`, `fallbackCompositeMatchKey`, `externalSourceLineId`), `IBudgetLineReconciliationCondition` (§2.5), 9 pure derived-field computors (§3.1 formulas), CSV import validation pipeline (required fields, cost-code dictionary, atomic batch), identity resolution (§2.3 — external match / composite unique / composite new / composite ambiguous), cost-code dictionary parser (both XX-XX-XXX and XX XX XX formats), Procore CSV column mapping and cost type extraction. Testing fixtures (4 factories + 6 scenario sets), contract stability and behavioral tests.
**T03 Forecast Versioning:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.3.** Forecast version ledger (`IForecastVersion` — 15 fields per §3.3), `ForecastDerivationReason` (5 values), `ForecastChecklistGroup` (4 groups), `IForecastChecklistItem` (10 fields per §4.2), canonical checklist template (19 items from §4.1 — 16 required + 3 conditional), version lifecycle operations (createInitialVersion, deriveWorkingVersion, transitionToSuperseded, confirmVersion, designateReportCandidate, promoteToPublished per §3.4–§3.6), confirmation gate validation (§4.3 — required items + stale budget line check), checklist generation from template. Testing fixtures and behavioral tests.
**T05 Cash Flow Model:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.4.** Cash flow actual record (`ICashFlowActualRecord` — 21 fields per §7.1), forecast record (`ICashFlowForecastRecord` — 14 fields per §7.2), summary aggregate (`ICashFlowSummary` — 14 fields per §7.3 including peakCashRequirement and cashFlowAtRisk), retention calculation model (`IRetentionConfig` per §7.4 — configurable retainage rate), A/R aging display model (`IARAgingRecord` — 14 fields per §7.5, read-only ERP sync), version-scoped records (§7.6). Pure computors for all derived fields (totalInflows, totalOutflows, netCashFlow, cumulative series, forecast accuracy, projected net/cumulative, summary aggregation, retention). Testing fixtures (3 factories + 3 scenario sets) and behavioral tests.
**T06 Buyout Sub-Domain:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.5.** Buyout line item (`IBuyoutLineItem` — 19 fields per §8.1), `BuyoutLineStatus` (7 procurement statuses per §8.2), ContractExecuted gate rule (§8.3 — P3-E13 subcontract execution readiness integration), `IBuyoutSummaryMetrics` (dollar-weighted completion per §8.4), `IBuyoutSavingsDisposition` and `IBuyoutSavingsDispositionItem` (three-destination savings workflow per §8.5–§8.6: AppliedToForecast, HeldInContingency, ReleasedToGoverned), buyout-to-budget reconciliation (§8.7 — 5% tolerance). Testing fixtures and behavioral tests.
**T07 Business Rules:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.6.** Sign convention rules (§9.1–§9.5 — budget-minus-cost for forecast/profit/cashFlow, cost-minus-budget for GC/GR and buyout), display precision constants (§9.6), FTC validation with 10% over-budget warning (§10.2), profit/margin assessment with 5% warning and 0% critical thresholds (§9.4, §10.3), GC/GR variance computors (§10.4), forecast summary calculations (§10.3 — revisedContractCompletion, estimatedCostAtCompletion, currentProfit, profitMargin, expectedContingencyUse), consolidated field summary alignment. Testing fixtures and behavioral tests.
**T08 Platform Integration:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.7.** 10 activity spine event types (§14.1 — BudgetImported through ReconciliationConditionResolved), 10 health spine metric definitions (§14.2), 8 work queue item types (§14.3), `IFinancialAnnotationAnchor` (§15.4 — version-aware field/section/block anchors using canonicalBudgetLineId), `ICarriedForwardAnnotation` (§15.5 — inheritance with value-change flag and PM disposition), annotatable field/section key registries (§15.2), PER permission matrix alignment (§15.6). Testing fixtures and behavioral tests.
**T09 Implementation Closure:** **Assessed 2026-03-23 in `@hbc/features-project-hub` v0.1.8.** P3-E4 Financial module contract-level implementation: T01–T03, T05–T08 complete (7 of 8 T-files). 34 source files, 19 test files, 233 financial tests across 12 subdirectories. Acceptance gate: 37 of 48 items satisfied at contract level; 4 items pending T04 data model (`IFinancialForecastSummary`, `IGCGRLine`); 7 items are runtime/integration/UAT scope. Blocker B-FIN-03 (P3-F1 handoff) stub-ready via `promoteToPublished()`. All implementation stages (FIN.0–FIN.8) complete except FIN.4 (pending T04) and FIN.9 (partial — runtime integration scope).

**7.2 — Schedule module**
Implement the full three-layer schedule operating model: governed master-schedule layer (canonical source ingestion, frozen import snapshots, versioned baselines, dual-truth commitment reconciliation); published forecast layer (stage-gated publication, executive review and health spine consumer); and field execution layer (work packages, commitment management, blocker tracking, look-ahead planning with PPC, three-tier progress verification). Implement schedule file ingestion (PWA Required, SPFx Launch-to-PWA); scenario branch management; analytics and confidence grading; offline-first sync with intent-log; cross-platform workflow integration via shared packages. Implement spine adapters for all four spines per P3-A3. All thresholds, grading rules, roll-up rules, and governed taxonomies are configured exclusively by Manager of Operational Excellence.
Governing: P3-E1 §4.2, P3-E2 §4, P3-E3 §3, **P3-E5** (field-level specification — master index + T01–T11 detail files)
**T01 Source Identity and Versioning:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.9.** Schedule module foundation: `ICanonicalScheduleSource` (12 fields per §1.1), `IScheduleVersionRecord` (20 fields per §1.2, 6 version statuses), `IBaselineRecord` (13 fields per §1.3, 4 baseline types), `IImportedActivitySnapshot` (full P6 field mapping per §1.4 with classification fields), `IActivityContinuityLink` (§1.5 — durable cross-version identity via `externalActivityKey`), `ICalendarRule` (§17.2 — Source and Operating calendar types), `ScheduleConstraintType` (8 constraint types per §1.4.1), 10 import validation rules (§1.6), `resolveScheduleLayerAccess` (6 roles × 4 layers authority matrix), `deriveExternalActivityKey` key derivation, `detectCalendarAssumptionDivergence` business rule, 5 integration boundary declarations, testing fixtures (6 mock factories + scenario sets), contract stability tests. T02–T11 build on this foundation.
**T02 Dual-Truth Commitments and Milestones:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.10.** Managed operating layer: `IManagedCommitmentRecord` (24 fields per §2.1, 3 commitment types, 7 reconciliation statuses), `IReconciliationRecord` (13 fields per §2.2, 5 trigger types — forensic audit trail), `IMilestoneRecord` (24 fields per §4.2, 8 milestone types, 7 calculated statuses per §4.3 algorithm), `resolveReconciliationStatus` (7-state reconciliation logic), `calculateMilestoneStatus` (threshold-based OnTrack/AtRisk/Delayed/Critical with governed defaults 14d/30d), `calculateVarianceDays`, `isApprovalRequired` governed threshold check, `isMilestoneActivity` (§4.1 identification rule), `createReconciliationEntry` (audit factory), testing fixtures (4 mock factories + scenario sets), contract stability tests. Builds on T01 source truth layer.
**T03 Publication Layer:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.11.** Published forecast layer: `IPublicationRecord` (21 fields per §3.1, 6 publication types, 4-state lifecycle — Draft/ReadyForReview/Published/Superseded), `IPublishBlocker` (§3.2 — Hard/Soft governed conditions), `IPublishedActivitySnapshot` (14 fields per §3.3 — frozen point-in-time combining source + commitment), `IScheduleSummaryProjection` (§19.1 — 15 fields, health spine consumer with MilestoneSummary and NextMilestoneRef), `validatePublicationAdvance` (stage-gate with blocker enforcement), `transitionPublicationStatus` (lifecycle state machine), `createPublishedActivitySnapshot` (source+commitment merge), `calculateScheduleOverallStatus` (§19.2 governed thresholds — default 7d/21d), `buildMilestoneSummary`, `findNextMilestone`, testing fixtures (4 mock factories + scenario sets), contract stability tests. Builds on T01 source truth + T02 commitment layer.
**T04 Scenario Branch Model:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.12.** Scenario branching: `IScenarioBranch` (16 fields per §5.1, 6 scenario types, 7-state lifecycle, 4 promotion dispositions), `IScenarioActivityRecord` (8 fields per §5.2 — per-activity date/float overrides with null-inherit from source), `IScenarioLogicRecord` (8 fields per §5.3 — alternative FS/SS/FF/SF relationships with promotion eligibility), `validateScenarioPromotion` (§5.4 — governed promotion to commitment/publication/baseline with PE gate), `transitionScenarioStatus` (lifecycle state machine), `getEffectiveActivityDate` (inherit-or-override resolution), `countActivityOverrides`, testing fixtures (3 mock factories + scenario sets), contract stability tests. Builds on T01–T03.
**T05 Field Execution Layer:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.13.** Full field execution layer: `IFieldWorkPackage` (30 fields per §6.1, 7 statuses, 7 progress basis types), `ILocationNode` (§6.2 — governed hierarchical location model, 7 levels), `IFieldCommitmentRecord` (22 fields per §6.3, 4 commitment types, 9 statuses — PPC tracking unit), `IBlockerRecord` (18 fields per §6.4, 14 blocker types, 4 severities, 6 statuses), `IReadinessRecord` (§6.5 — per-dimension readiness with governed dimensions), `ILookAheadPlan` (16 fields per §6.6 — weekly planning artifact with PPC metrics), `IAcknowledgementRecord` (17 fields per §7.1, 5 subject types, 8 statuses), `IProgressClaimRecord` (16 fields per §8.1), `IProgressVerificationRecord` (13 fields per §8.2, 6 verification methods, 4 outcomes — three-tier authority chain), `calculatePPC`, roll-up rules (§9 — blocker severity, readiness, commitment status, weighted progress), `isVerifiedAndAccepted` (§8.2 authority chain), `isWorkPackageInParentWindow` (§6.1 validation), testing fixtures (5 mock factories + scenario sets), contract stability tests. Builds on T01–T04.
**T06 Logic Dependencies and Propagation:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.14.** Three-layer dependency model: `IImportedRelationshipRecord` (7 fields per §10.2 — immutable CPM logic per version, FS/SS/FF/SF), `IWorkPackageLinkRecord` (9 fields per §10.3 — field execution-sequencing links with promotion eligibility), `LogicLayer` enumeration (§10.1 — SourceCPM/Scenario/WorkPackage), `PropagationType` (§10.4 — SourceSchedulePropagated/OperatingLayerProjected/ScenarioLayerProjected with governed authority rules), `getLogicLayerAuthority`, `classifyPropagationAuthority`, testing fixtures (2 mock factories), contract stability tests. Builds on T01–T05.
**T07 Analytics Intelligence and Grading:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.15.** Analytics and intelligence layer: `IScheduleQualityGrade` (§11.1 — composite grading with 10 governed controls, A/B/C/D/F letter grades), `IFloatPathSnapshot` (§11.2 — criticality index per activity, critical/near-critical/negative float counts), `IMilestoneSlippageTrend` (§11.3 — version-over-version slippage history), `IConfidenceRecord` (§11.4 — 8-factor governed confidence with High/Moderate/Low/VeryLow labels and narrative), `IRecommendationRecord` (§12.1 — 9 recommendation types, 6 dispositions, 5 promotion paths — never silently mutates schedule truth), `ICausationCode` (§13.1 — hierarchical governed taxonomy, 13 root categories, 9 applicable record types), `calculateCriticalityIndex` (§11.2.1 formula), `calculateOverallGrade`, `calculateConfidenceLabel`, `calculateSlippageSinceLastUpdate`, `isRecommendationPromotable`, testing fixtures (3 mock factories), contract stability tests. Builds on T01–T06.
**T09 Platform Integration and Governance:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.16.** Cross-platform integration and governance: 11 `@hbc/related-items` relationship type mappings (§18.1), 5 `@hbc/workflow-handoff` types (§18.2), 10 `@hbc/my-work-feed` work item types with trigger/assignee configs (§18.3), 6 `@hbc/notification-intelligence` event types (§18.4), 3 `@hbc/complexity` progressive disclosure tiers (§18.5 — Essential/Standard/Expert), `@hbc/field-annotations` annotation scope with 5 annotatable surfaces and 4 annotation restrictions (§18.6/§23), `IGovernedPolicySet` with 20 governed policy areas (§20.1), `getComplexityTierFeatures`, `isAnnotatableSurface`, `getAnnotatableFields`, `getDefaultAssigneeForWorkItem`, testing fixtures (1 mock factory), contract stability tests. Builds on T01–T07.
**T10 Business Rules, Capabilities, and Reference:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.17.** Business rules and reference: 10 codified business rules (§21 — commitment approval thresholds per type with defaults ActivityForecast=5d/MilestoneCommitment=3d/CompletionForecast=1d, duration conversion at 8hr/day, auto-publish criteria with 6 governed conditions, variance sign convention, identity derivation, staleness detection at 30d, delete flag handling, manual milestones, offline precedence, PPC calculation with 4-window rolling default), 12 required capabilities (§22 — ingestion through work feed registration with dependency mapping), complete status enumeration reference (§27 — 18 enumerations cross-referenced), field summary index (§28 — 33 record types with primary keys and 10 authority layers), `getApprovalThresholdForType`, `evaluateAutoPublishEligibility`, `convertHoursToDays`/`convertDaysToHours`, `applyVarianceSignConvention`, `calculateDaysSinceLastImport`, `getFieldSummaryForRecord`, `getAuthorityLayerForRecord`, contract stability tests. Builds on T01–T09.
**T08 Classification, Metadata, Offline, and Sync:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.18.** Classification and offline layer: governed classification framework (§14 — 8 dimensions with source-to-governed mapping, 6 classification usage contexts), `IIntentRecord` (12 fields per §15.1, 4 operation types, 7 offline-capable record types — full intent queue model), sync states (§15.2), conflict resolution rules (§15.3 — 4 conflict types with governed resolution), visibility policy model (§16.1 — 6 policy dimensions, 4 hard rules), `IExternalParticipantRecord` (12 fields per §16.2, 4 permitted workflows), `isExternalParticipantVisible`, `resolveConflictResolution`, `isOfflineCapableRecordType`, testing fixtures (2 mock factories), contract stability tests. Builds on T01–T10.
**T11 Implementation Closure:** **Assessed 2026-03-23 in `@hbc/features-project-hub` v0.1.18.** P3-E5 Schedule module contract-level implementation: T01–T10 complete (10 of 10 data model T-files). 52 source files, 20 test files, 380+ schedule tests across 10 subdirectories. Acceptance gate: 40 of 49 items satisfied at contract level; 9 items are runtime/integration/UAT scope (parser with reference files, integration tests with sample projects, performance benchmarks, SPFx lane parity, UI surfaces). 5 shared package blockers (B-SCH-01 through B-SCH-05) stub-ready — `@hbc/related-items` schedule anchor types, `@hbc/workflow-handoff` publication/scenario types, `@hbc/complexity` Expert tier, `@hbc/field-annotations` publication-activity anchor. All implementation stages (7.SCH.0–7.SCH.11) complete at contract level except 7.SCH.8 (partial — offline replay runtime scope), 7.SCH.9 (partial — runtime integration with shared packages), 7.SCH.10 (UI surfaces scope), 7.SCH.11 (UAT scope).

**7.3 — Constraints module**
Implement the four-ledger project-controls workspace: Risk Ledger (forward-looking risk identification, probability/impact assessment, mitigation tracking, risk-to-constraint spawn); Constraint Ledger (active blockers and issues, peer of other ledgers — not a parent record type); Delay Ledger (contemporaneous delay event records with schedule reference model, evidence gates, separation of time impact from commercial/cost impact, B+/C-ready claims orientation); Change Ledger (manual-native in Phase 3; Procore-integration-ready data model with canonical HB Intel identity preserved through future integration). Implement governed cross-ledger lineage (spawn/promotion model with immutable `LineageRecord`). Implement live vs published state split: operational surfaces use live state; executive review consumes published snapshots and review packages. Implement spine adapters for all four spines per P3-A3. All taxonomies, thresholds, BIC registries, and escalation rules configurable by Manager of Operational Excellence.
Governing: P3-E1 §4.3, P3-E2 §5, P3-E3 §4, **P3-E6** (field-level specification — master index + T01–T08 detail files)
**T01 Risk Ledger:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.19.** Risk record model (`IRiskRecord`, 24 fields per §1.2), 8-state lifecycle with state machine validation, 16 governed risk categories, 5-level probability/impact scoring (`riskScore` = probability × impact, range 1–25), 6 business rules (R-01 immutability, R-02 no hard delete, R-03 owner accountability, R-04 score recalculation, R-05 spawn accountability, R-06 governed thresholds), 5 health spine metric helpers, testing fixtures with scenario coverage for all lifecycle states, contract stability tests. Constraints module Stage 7.CON.1 data foundation (Risk portion) and Stage 7.CON.2 Risk Ledger contract complete.
**T02 Constraint Ledger:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.20.** Constraint record model (`IConstraintRecord`, 26 fields per §2.2), `ICommentEntry` append-only comment type, 7-state lifecycle with state machine validation (Resolved requires `dateClosed`, Superseded requires `successorConstraintId`, Resolved without `closureDocumentUri` produces warning per C-06), 26 governed constraint categories, 4-level priority model (Critical/High/Medium/Low), 8 business rules (C-01 immutability, C-02 owner accountability, C-03 no hard delete, C-04 append-only comments, C-05 overdue detection, C-06 closure documentation, C-07 spawned record tracking, C-08 governed thresholds), `calculateDaysOpen` calendar day computation, 6 health spine metric helpers including `avgDaysOpen` and `maxDaysOpen`, testing fixtures with scenario coverage for all lifecycle states and lineage, contract stability tests. Constraints module Stage 7.CON.1 data foundation (Constraint portion) and Stage 7.CON.3 Constraint Ledger contract complete.
**T03 Delay Ledger:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.21.** Delay record model (`IDelayRecord`, ~35 fields per §3.2), nested `ITimeImpactRecord` (11 fields — estimatedCalendarDays, analysisMethod, analysisBasis, fragnet/TIA future-ready seams, quantificationConfidence), nested `ICommercialImpactRecord` (7 fields — hasCostImpact, costImpactEstimate, costImpactBreakdown, separationConfirmed gate), `IDelayEvidenceAttachment` with governed evidence types, `ICostBreakdownItem`, 7-state lifecycle with evidence gate enforcement (Quantified gate: timeImpact + separationConfirmed + criticalPathImpact; Dispositioned gate: dispositionOutcome + dispositionNotes + notificationDate), 9 governed enumerations (DelayEventType ×12, ResponsibleParty ×7, CriticalPathImpact ×4, AnalysisMethod ×7, EvidenceType ×9, QuantificationConfidence ×3, DispositionOutcome ×5, ScheduleReferenceMode ×2), dual schedule reference model (Integrated/ManualFallback with D-03 consistency validation), 7 business rules (D-01 contemporaneous evidence, D-02 notification date integrity, D-03 schedule reference consistency, D-04 impact separation gate, D-05 no hard delete, D-06 governed gate requirements, D-07 change event linking), 5 health spine metric helpers (`totalQuantifiedDelayDays` sum, `pendingNotificationCount` threshold), testing fixtures with nested record factories and 12 scenario variants, contract stability tests. Constraints module Stage 7.CON.1 data foundation (Delay portion) and Stage 7.CON.4 Delay Ledger contract complete.
**T04 Change Ledger:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.22.** Change event record model (`IChangeEventRecord`, ~25 fields per §4.2), `IChangeLineItem` (10 fields — Procore PCO line item model), `IProcoreMappingRecord` (12 fields — full Procore integration state, null in ManualNative Phase 3), 9-state lifecycle with approval gate enforcement (CE-03: Approved requires `approvedDate` + `approvedBy` + `totalCostImpact`; Superseded requires `successorChangeEventId`; Rejected → UnderAnalysis resubmission allowed), 5 governed enumerations (ChangeEventOrigin ×10, ChangeLineItemType ×5, ChangeIntegrationMode ×2, ProcoreSyncState ×4, CostConfidence ×3), `calculateTotalCostFromLineItems` cost rollup, 7 business rules (CE-01 canonical identity permanent, CE-02 no hard delete, CE-03 approval cost finalization, CE-04 line items preferred, CE-05 write path governance, CE-06 one-way promotion, CE-07 governed taxonomies), 5 health spine metric helpers (`totalPendingCostImpact` and `totalApprovedCostImpact` USD sums), testing fixtures with nested line item and Procore mapping factories and 12 scenario variants, contract stability tests. Constraints module Stage 7.CON.1 data foundation (Change portion) and Stage 7.CON.5 Change Ledger ManualNative contract complete.
**T05 Cross-Ledger Lineage:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.23.** Immutable `ILineageRecord` (13 fields per §5.4 — audit trail for every spawn), `ICrossLedgerLink` (bidirectional peer link), 3 governed spawn paths (Risk→Constraint, Constraint→Delay, Constraint→Change per §5.2) with seed data extraction and inheritance snapshots, `ISpawnPathConfig` with governed inherited field maps, spawn eligibility validators (`canSpawnFromRisk`, `canSpawnFromConstraint`), peer link factory with L-05 bidirectional consistency validation, `@hbc/related-items` registration contracts (4 object types, 9 relationship type mappings per §5.7), 5 lineage business rules (L-01 spawn preserves parent, L-02 lineage immutability, L-03 no status inheritance, L-04 multiple spawns allowed, L-05 bidirectional links), testing fixtures and scenario variants, contract stability tests. Constraints module Stage 7.CON.6 lineage contract complete.
**T06 Publication, Review, and Governance:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.24.** `ILedgerRecordSnapshot` (10 fields — immutable record-level publication), `IReviewPackage` (9 fields per §6.3 — multi-ledger review packages with RP-### numbering), `ReviewPackageStatus` (Active/Superseded/Archived), publication authority matrix (8 rules mapping 6 roles to 8 actions per §6.4 — PM publishes snapshots, PM+DesignatedApprover publish packages, PER annotates published only), state consumption map (9 consumer rules per §6.1 — Live/Published/Configurable), master BIC team registry (32 governed teams per §6.7 shared across all four ledgers), governance configurability framework (13 governed taxonomy areas per §6.6 with source references), 7 locked structural elements (schema, state machines, lineage, no-hard-delete, annotation isolation, canonical identity, live/published split), authority check helpers, testing fixtures and scenario variants, contract stability tests. Constraints module Stage 7.CON.7 publication contract complete.
**T07 Platform Integration:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.25.** 11 shared package integration declarations (§7.1 — `@hbc/field-annotations`, `@hbc/related-items`, `@hbc/acknowledgement`, `@hbc/workflow-handoff`, `@hbc/notification-intelligence`, `@hbc/bic-next-move`, `@hbc/versioned-record`, `@hbc/my-work-feed`, `@hbc/complexity`, `@hbc/export-runtime`, `@hbc/session-state`), 5 workflow handoff types (§7.2), 8 notification types with governed routing (§7.3), 9 work item types via `ConstraintsWorkAdapter` (§7.4), 14 activity spine event types with typed payload field declarations (§7.5), 11 health spine metric configs with ledger and update frequency (§7.5), 7 work queue item configs with governed priority (§7.5), 6 report type configs with state source (§7.6 — live operational + published review package), contract stability tests. Constraints module Stage 7.CON.8 integration contract complete.
**T08 Implementation Closure:** **Assessed 2026-03-23 in `@hbc/features-project-hub` v0.1.26.** P3-E6 Constraints module contract-level implementation: T01–T07 complete (7 of 7 data model T-files). 44 source files, 17 test files, 331 constraints tests across 7 subdirectories (risk-ledger, constraint-ledger, delay-ledger, change-ledger, lineage, publication, integration). Acceptance gate: 44 of 53 items satisfied at contract level; 9 items are runtime/integration/UAT scope (UI lineage display, runtime work adapter wiring, runtime notification delivery, UI relationship panel, UI audit history, UI BIC indicator, report generation runtime, integration tests with runtime, performance benchmarks, UAT). 3 shared package blockers (B-CON-01 through B-CON-03) stub-ready — `@hbc/related-items` object types, `@hbc/workflow-handoff` handoff types, `@hbc/field-annotations` snapshot anchors. All implementation stages (7.CON.0–7.CON.10) complete at contract level except 7.CON.8 (partial — runtime wiring with shared packages), 7.CON.9 (export/report generation scope), 7.CON.10 (UAT scope).

**7.4 — Permits module**
Implement the multi-record governed ledger architecture per P3-E7. Seven first-class record families: `PermitApplication` (pre-issuance lifecycle); `IssuedPermit` (operative post-approval record with thread model); `RequiredInspectionCheckpoint` (governed template library per permit type, auto-generated on activation); `InspectionVisit` (first-class inspection event records); `InspectionDeficiency` (first-class deficiency with resolution workflow and severity model); `PermitLifecycleAction` (immutable audit event record — sole mechanism for status change; 20 action types); `PermitEvidenceRecord` (reference-only document and photo attachments). Implement permit thread model connecting master permits, subpermits, phased releases, revisions, and closeout paths. Derived compliance health only — `derivedHealthTier` computed from deficiencies, expiration proximity, and blocking inspection outcomes; no `complianceScore` field anywhere. Responsibility envelopes on `IssuedPermit` and `InspectionVisit`. All 15 work queue rules (WQ-PRM-01 through WQ-PRM-15). PER annotation layer on `IssuedPermit` and `InspectionVisit` via `@hbc/field-annotations`. Six shared package blockers (B-PRM-01 through B-PRM-06) must be resolved before module completes. One-time migration from prior flat `IPermit` model per T07. Full acceptance gate: 52 items (AC-PRM-01 through AC-PRM-52) in P3-E7-T08 §4.
Governing: P3-E1 §3.4, P3-E2 §6, P3-E3 §5, **P3-E7** (field-level specification — master index + T01–T08 detail files)
**T01 Product Shape and Doctrine:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.27.** Permits module foundation: `PermitThreadRelationship` (7 thread types per §4.2 — THREAD_ROOT, SUBPERMIT, PHASED_RELEASE, REVISION, TEMPORARY_APPROVAL, CLOSEOUT_PATH, STANDALONE), `DerivedHealthTier` (CRITICAL/AT_RISK/NORMAL/CLOSED — no manual complianceScore per §8), `PermitRecordType` (7 first-class record families per §3), `PermitAuthorityRole` (4 roles), authority matrix (16 rules per §7.1 — PM create/update, SiteSupervisor create inspections/deficiencies, Executive annotate IssuedPermit/InspectionVisit only), `IPermitThreadNode` thread model with `isThreadRoot`/`isStandalone`/`isThreadChild` checks, `deriveHealthTier` (5 compliance health signals → derived tier, priority CRITICAL > AT_RISK > NORMAL), `deriveThreadHealthTier` (worst-child propagation per §4.3), 15 locked decisions from master index, 6 shared package requirements, 10 cross-contract references, immutable field declarations for IssuedPermit (6 fields per §7.2), testing fixtures with 7 thread node scenarios, contract stability tests. Permits module Stage 7.PRM foundation complete.
**T02 Record Architecture:** **Implemented 2026-03-23 in `@hbc/features-project-hub` v0.1.28.** Complete 7-family record architecture: `IPermitApplication` (21 fields per §2), `IIssuedPermit` (~33 fields per §3 including thread model, responsibility envelope, derived health), `IRequiredInspectionCheckpoint` (17 fields per §4 — governed template library), `IInspectionVisit` (22 fields per §5 with responsibility envelope), `IInspectionDeficiency` (20 fields per §6 with severity/resolution workflow), `IPermitLifecycleAction` (18 immutable fields per §7 — sole mechanism for status change; 20 action types), `IPermitEvidenceRecord` (15 fields per §8). Supporting types: `IJurisdictionContact` (7 fields), `IInspectorContact` (3 fields). 15 governed enumerations: `PermitApplicationStatus` ×7, `IssuedPermitStatus` ×12, `CheckpointStatus` ×8, `InspectionVisitResult` ×7, `DeficiencySeverity` ×3, `DeficiencyResolutionStatus` ×7, `PermitLifecycleActionType` ×20, `PermitEvidenceType` ×10, `PermitType` ×12, `ExpirationRiskTier` ×4, `PermitHealthTier` ×4, `PermitAccountableRole` ×5, `RequiredInspectionResult` ×4, `SubmissionMethod` ×4, `PartyType` ×4. Testing fixtures with mock factories, contract stability tests.
**T03 Lifecycle and Governance:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.29.** Dual lifecycle state machines: `PermitApplication` (7-state lifecycle per §2 with DRAFT→SUBMITTED→UNDER_REVIEW→APPROVED/REJECTED/WITHDRAWN and wildcard WITHDRAWN), `IssuedPermit` via `PermitLifecycleAction` (20-rule transition table per §3.2 — all status changes flow through immutable action records; direct status mutation rejected). Governance: terminal status immutability (§4.1), previousStatus match enforcement (§4.2), notes-required and ack-required action type classification, 3 system-driven transitions (§3.3 — daily expiration sweep, auto-close on all checkpoints pass), application editing rules per status (§5.1), required fields per transition (§5.2), stop-work/violation blocking detection (§4.4), expiration warning logic (§3.3), `resolveNewStatus` action→status resolver, API contract types (`ICreatePermitLifecycleActionRequest`/`Response`). Testing fixtures, contract stability and behavioral tests.
**T04 Inspection and Compliance:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.30.** Checkpoint template library (`ICheckpointTemplate`, 12 fields per §1.2) with 14 `MASTER_BUILDING` templates per §1.3 (IBC/NEC/IPC/IMC code references, blocking-closeout flags, predecessor dependencies), deficiency-to-health severity mapping (9 rules per §3.3 — HIGH+OPEN→CRITICAL, HIGH+REMEDIATION→AT_RISK, MEDIUM+OPEN→AT_RISK), 4 deficiency work queue rules per §3.4, `calcExpirationRiskTier` (CRITICAL/HIGH/MEDIUM/LOW per §4.2), `calculateDaysToExpiration`, `getDeficiencyHealthImpact` severity×status→tier resolver, `isComplianceCloseoutGateMet` (4-condition gate per §6 — blocking checkpoints passed, no open deficiencies, valid permit status, not expired), `validateTemplateImportRow` xlsx import validation per §5.2, testing fixtures, contract stability and behavioral tests.
**T05 Workflow and Downstream:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.31.** 21 activity spine event types with payload declarations (§1 — application lifecycle, permit lifecycle actions, inspection events, deficiency events, expiration events, evidence upload), 11 per-permit health metrics + 7 project-level aggregate metrics (§2), 15 work queue rules (WQ-PRM-01 through WQ-PRM-15 per §3 — expiration, inspection, deficiency, lifecycle categories with resolution triggers), 5 related-item relationship types (§4 — permit→milestone, permit→constraint, permit→financial, inspection→milestone, checkpoint→milestone), 6 workflow handoff scenarios (§5), 7 BIC next-move prompts (§6), PER annotation scopes for 4 record types (§7 — IssuedPermit wildcard, InspectionVisit/Deficiency/Checkpoint field-level), contract stability tests.
**T06 UX Surface and Reporting:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.32.** 5 view surface declarations (§1 — Permit List, Permit Detail, Inspection Log, Compliance Dashboard, Executive Review), 12 list view column configs with sort/filter/visibility per §2.1, 4 health status indicators with color/icon/row-treatment per §2.2, thread view toggle per §2.3, 5 quick action configs per §2.4, 11 detail view sections per §3.1 (collapsible; including Annotations PER-surface-only), 8 edit constraints per §3.4 (status via lifecycle action only), 6 inspection log columns per §4.1, 5 inline deficiency actions per §4.3 (Acknowledge/StartRemediation/MarkResolved/Dispute/AttachEvidence), 6 compliance dashboard tiles per §5.1, 6 report type configs with audience/cadence per §7.1, 11 export fields per §7.2, 3 PER annotation type UX configs (Note/Flag/Recommendation with color indicators per §6.2), contract stability tests.
**T07 Migration and Import:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.33.** Full flat-to-multi-record migration contracts: IPermit→IssuedPermit field mapping (23 entries per §1.2), IInspection→InspectionVisit mapping (16 entries), IInspectionIssue→InspectionDeficiency mapping (7 entries), IRequiredInspectionRecord→RequiredInspectionCheckpoint mapping (10 entries per §1.5), 5 status mappings (§1.3), 4+4 result mappings (§1.4), 8 migration defaults (STANDALONE thread, PM accountable, conservative blocking), synthetic ISSUED lifecycle action template, 8-item migration validation checklist (§1.6 — complianceScore absence confirmed, health recalculation required), import idempotency choices (Append/Replace/Cancel per §2.2), evidence upload config (50MB max, PDF/JPG/PNG/DOCX/XLSX per §3), 8 versioned record tracked fields (§4.1), 4 future jurisdiction API integration points (§5.1), contract stability tests.
**T08 Implementation Closure:** **Assessed 2026-03-24 in `@hbc/features-project-hub` v0.1.34.** P3-E7 Permits module contract-level implementation: T01–T07 complete (7 of 7 data model T-files). 30 source files, 10 test files, 214 permits tests across 7 subdirectories (foundation, records, lifecycle, inspection, workflow, views, migration). Acceptance gate: 42 of 52 items satisfied at contract level; 10 items are runtime/integration/UAT/migration scope (runtime acknowledgment workflow, checkpoint-visit propagation, health spine live updates, work queue resolution, BIC/handoff runtime wiring, UI surface rendering, migration execution). 6 shared package blockers (B-PRM-01 through B-PRM-06) stub-ready — `@hbc/workflow-handoff`, `@hbc/acknowledgment`, `@hbc/field-annotations`, `@hbc/versioned-record`, `@hbc/bic-next-move`, `@hbc/related-items`. All implementation stages (7.PRM.0–7.PRM.10) complete at contract level except 7.PRM.7 (partial — runtime work queue wiring), 7.PRM.8 (runtime related-items), 7.PRM.9 (PER annotation runtime), 7.PRM.10 (migration execution).

**7.5 — Safety module**
Implement the governed multi-record workspace per P3-E8. 15 first-class record families: SSSP Base Plan and Addenda (hybrid structured + rendered PDF; joint approval — Safety Manager + PM + Superintendent; governed sections Safety Manager-only); Inspection Checklist Template (versioned; Safety Manager governed); Completed Weekly Inspection (Safety Manager only; template version pinned; normalized score over applicable sections only); Safety Corrective Action (centralized ledger regardless of source — inspection, incident, JHA, observation, or external); Incident/Case (tiered privacy model — STANDARD/SENSITIVE/RESTRICTED; `personsInvolved` always Safety Manager/Officer only); Job Hazard Analysis (Safety Manager approval; competent-person designation pre-condition); Daily Pre-Task Plan (must reference APPROVED JHA; project team creates); Toolbox Talk Prompt (governed library; schedule-driven intelligence; governed closure model); Weekly Toolbox Talk Record (governed record; hybrid proof model; named attendees for high-risk); Worker Orientation Record (governed; `@hbc/acknowledgment` integration); Subcontractor Safety Submission (governed review lifecycle per submission type); Certification/Qualification Record (daily expiration sweep; EXPIRING_SOON at 30 days); HazCom/SDS Record; Competent-Person Designation (project-scoped; competency area; backs JHA pre-condition). Readiness decision surface at project/subcontractor/activity levels (Ready / Ready with Exception / Not Ready) using governed blocker-and-exception matrix — not a weighted score — not a hard technical stop in v1. Composite safety scorecard published to Project Hub (inspection trend + corrective action health + readiness posture + blockers + compliance completion); sanitized score band (not raw score) to PER. Safety explicitly excluded from PER annotation layer. No annotation affordance, no push-to-team for Safety. 25 work queue rules (WQ-SAF-01 through WQ-SAF-25). 6 shared package blockers (B-SAF-01 through B-SAF-06). Full acceptance gate: 60 items (AC-SAF-01 through AC-SAF-60) in P3-E8-T10 §4.
Governing: P3-E1 §4.5, P3-E2 §7, P3-E3 §6, **P3-E8** (field-level specification — master index + T01–T10 detail files)
**T01 Module Scope and Operating Model:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.35.** Safety module foundation: `SafetyRecordFamily` (15 record families per §3), `SafetyAuthorityRole` (6 roles), `SafetyAuthorityAction` (6 actions), `SafetyLaneOwner` (2 lanes), `IncidentPrivacyTier` (3 tiers per §5.4), `PERVisibilityTier` (3 tiers per §5.3), `CompositeScorecardDimension` (5 dimensions per §5.2), authority matrix (role × record family × action per §4.1–4.2), `safetyManagerOnly` field declarations (§4.3), `canSafetyRolePerformAction` authority check, `isSafetyManagerOnlyField` enforcement check, `isExcludedFromAnnotation` (always true — P3-E1 §9.3), `isSafetyPushToTeamAllowed` (always false), lane ownership split across 15 record families (§4.1–4.2), 6 shared package requirements, 14 cross-contract references, 6 operating model principles (§7), 7 locked decisions from §8 reinforced. T02–T10 build on this foundation.
**T02 Workspace Architecture and Record-Family Map:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.36.** All 16 record family interfaces: `ISiteSpecificSafetyPlan` (§2.1), `ISSSPAddendum` (§2.2), `IInspectionChecklistTemplate` (§2.3), `ICompletedInspection` (§2.5), `ISafetyCorrectiveAction` (§2.6 — centralized ledger), `IIncidentRecord` (§2.7 — tiered privacy), `IJhaRecord` (§2.8 — step-hazard-control), `IDailyPreTaskPlan` (§2.9), `IToolboxTalkPrompt` (§2.10), `IWeeklyToolboxTalkRecord` (§2.11 — hybrid proof), `IWorkerOrientationRecord` (§2.12), `ISubcontractorSafetySubmission` (§2.13), `ICertificationRecord` (§2.14), `IHazComSdsRecord` (§2.15), `ICompetentPersonDesignation` (§2.16), `ISafetyEvidenceRecord` (§2.17 — governed with sensitivity/retention). ~32 type unions for lifecycle states and governed taxonomies. 10 lifecycle state transition maps. Embedded sub-interfaces for inspection sections/items, JHA steps/hazards, toolbox attendees, incident persons. Identity model (§4.2 — hybrid governed + provisional). Governance layer (§4.1 — field-level authority matrix). T03–T10 build on these record shapes.
**T03 SSSP Base Plan, Addenda, and Approval Governance:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.37.** SSSP governed section interfaces (7): `ISSSPGoverned_HazardSection`, `ISSSPGoverned_EmergencySection`, `ISSSPGoverned_ProgramSection`, `ISSSPGoverned_RegulatorySection`, `ISSSPGoverned_CompetentPersonSection`, `ISSSPGoverned_SubcontractorSection`, `ISSSPGoverned_IncidentSection`. Instance section interfaces (5): `ISSSPInstance_Contacts`, `ISSSPInstance_SubcontractorList`, `ISSSPInstance_Location`, `ISSSPInstance_SiteLayout`, `ISSSPInstance_OrientationSchedule`. Joint approval model: `ISSSPApproval` (Decision 20 — 3-party sign-off), `ISSSPAddendumApproval` (Decision 21 — Safety Manager always + PM/Super when `operationallyAffected`), `ISSSPApprovalSignature`, `ISSSPApprovalRejection`. State machine validation: `transitionSSSPStatus`, `transitionAddendumStatus`. Business rules: `isSSSPApprovalComplete`, `isAddendumApprovalComplete`, `canEditSSSPSection` (status/role matrix), `isMaterialChange` (§4.3 — governed sections trigger reapproval), `canDraftAddendum` (governed sections require Safety Manager), `getRequiredApproversForAddendum`. Rendered document config (§5.3 — EXTENDED_REGULATORY retention). 5 work queue triggers (§6). T02 stubs replaced with full interfaces. T04–T10 build on this governance layer.
**T04 Inspection Program, Template Governance, and Scorecard:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.38.** 12-section standard template (`STANDARD_TEMPLATE_SECTIONS` — weights sum to 100 per §2.2). Normalized scoring algorithm (`calculateNormalizedScore` — §4.1: applicable-sections-only reweighting, per-section pass ratio × renormalized weight, rounded 1dp). Score band derivation (`deriveScoreBand` — §5.2: HIGH≥90, MED 70–89, LOW<70; PER never receives raw score). Score trend computation (`computeTrendDirection` — §5.1: 4-week rolling window, IMPROVING/STABLE/DECLINING/INSUFFICIENT_DATA with ±5pt threshold). Template governance: version pinning at inspection creation (`isTemplatePinningValid` — only ACTIVE templates), `validateWeightsSum`. Corrective action due dates (`getCADueDateDays` — §6: CRITICAL=0d, MAJOR=3d, MINOR=7d). T02 interfaces expanded: `IApplicabilityRule` (governance conditions), `IInspectionSectionResponse` (section scores, N/A reason), `IInspectionItemResponse` (typed PASS/FAIL/N_A, numericValue, correctiveActionId), `IInspectionScorecardSnapshot` (15 fields including section summaries). New types: `IInspectionScoreTrend`, `IInspectionTrendDataPoint`, `ISectionScoreSummary`. 4 work queue triggers. T05–T10 build on this inspection program.
**T05 Corrective Actions, Incidents, Evidence, and Privacy:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.39.** Centralized CA ledger governance: `isCAOverdue` (§1.3 — dueDate < today + non-terminal status), `getCAHealthTierImpact` (CRITICAL→CRITICAL tier, MAJOR→AT_RISK), `shouldEscalateCriticalCA` (§1.4 — OPEN >4h triggers escalation), `shouldEscalatePendingVerification` (>2 business days). 4 CA work queue triggers. Incident three-tier privacy model: 15 visibility rules (3 tiers × 5 roles per §2.3), `getIncidentVisibility`, `canViewIncident`, `canEscalatePrivacyTier` (escalation only — demotion requires audit). Evidence governance: `getEvidenceSensitivityDefault` (9 source type defaults per §3.2 — INCIDENT defaults SENSITIVE), `EVIDENCE_RETENTION_RULES` (3 categories per §3.3), `isLitigationHoldActive`, `escalateToLitigationHold`. `IIncidentPersonRecord` expanded (§2.4): `IncidentPersonRole` enum (4 values), `injuryBodyPart`, `medicalAttentionRequired`, `medicalFacility`, `returnToWorkDate`, `lostDays`. New types: `ICorrectiveActionSourceLink`, `ICompositeCAHealthSignal` (5 signals per §4). T06–T10 build on this governance layer.
**T06 JHA, Pre-Task, Toolbox Prompt, and Toolbox Talk:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.40.** JHA approval governance: `canApproveJha` (§2.2 — PENDING_APPROVAL + competent person pre-condition per §2.4), `isCompetentPersonRequirementMet`, JHA supersession model. Daily pre-task validation: `canCreateDailyPreTask` (§3.1 — only APPROVED JHA), `validatePreTaskCompletion` (§3.3 — controlsConfirmed + ppeVerified + attendeeCount > 0). Toolbox prompt intelligence: `IScheduleRiskMapping` (§4.2 — governed + AI-suggested mappings, 14-day schedule lookahead), `PromptClosureType` enum (STANDARD/HIGH_RISK/CRITICAL per §4.3). Prompt closure model: `validatePromptClosure` (§4.3 — STANDARD: attendee count; HIGH_RISK: named attendees + proof; CRITICAL: + Safety Manager verification), `isHighRiskProofSatisfied`. `IToolboxPromptIssuance` expanded with `closureType` and `closureVerifiedById`. 3 work queue triggers (§5.5). `@hbc/acknowledgment` integration pattern for toolbox talk attendance. T07–T10 build on this.
**T07 Orientation, Subcontractor Submissions, Qualifications, SDS, Competent Person:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.41.** Orientation governance: `IOrientationTopicDefinition` (8 topics per §1.2 — isRequired + regulatoryRef), `isOrientationComplete` (COMPLETE + acknowledged), `isAcknowledgmentEvidenceRequired` (PHYSICAL_SIGNATURE only per §1). Submission review: `areRequiredSubmissionsApproved` (all required types APPROVED per §2), `isSubmissionResubmissionRequired` (REJECTED/REVISION_REQUESTED). Certification expiration sweep: `computeCertificationStatus` (§3 — 30-day EXPIRING_SOON window, null=no expiration, REVOKED overrides), designation cascade `shouldCascadeDesignation` (EXPIRED/REVOKED → cascade to linked designations + JHA re-evaluation per §5.4). SDS compliance: `hasSdsComplianceGap` (§4 — active products without ACTIVE SDS). Workforce identity: `canUpgradeWorkerIdentity` (§6 — provisional→governed upgrade). 3 orientation + 3 certification work queue triggers. T08–T10 build on this compliance layer.
**T08 Readiness Evaluation, Blocker Matrix, and Override Workflow:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.42.** Three-level readiness evaluation engine: `ReadinessEvaluationLevel` (PROJECT/SUBCONTRACTOR/ACTIVITY), `ReadinessDecision` (READY/READY_WITH_EXCEPTION/NOT_READY per Decision 22–23). 25 blocker definitions across 3 matrices: 8 project (BLK-PRJ-01–08), 9 subcontractor (BLK-SUB-01–09), 8 activity (BLK-ACT-01–07 + BLK-ACT-04-HR) with HARD/SOFT classification (Decision 25 — HARD cannot be excepted). `evaluateReadiness` (§9 — HARD→NOT_READY, SOFT only→READY_WITH_EXCEPTION, none→READY). Exception model: `IReadinessException` (§7 — Safety Manager-only grant, min 20-char rationale, auto-lapse on expiration per `shouldAutoLapseException`). Override workflow: `IReadinessOverride` (§8 — joint governance: PROJECT/SUBCONTRACTOR=SM+PM, ACTIVITY=SM+PM+Super per `isOverrideComplete`). `IReadinessSummaryProjection` (§9.3 — project readiness + blocker/exception counts + subcontractor/activity summaries for Project Hub). 5 work queue triggers (§10). T09–T10 build on this readiness engine.
**T09 Publication Contracts, Project Hub, PER, and Reports:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.43.** Composite safety scorecard: `ISafetyCompositeScorecard` (§2.1 — 5 dimensions: inspection trend, corrective actions, readiness, blockers, compliance + `SafetyPosture` overall posture per §2.2: CRITICAL/AT_RISK/ATTENTION/NORMAL/INSUFFICIENT_DATA). `deriveSafetyPosture` (priority-tiered posture derivation from scorecard signals). Sanitized PER projection: `ISafetyScorecardPERProjection` (§2.3 — score bands not raw scores, anonymized incident counts, no person details) via `createPERProjection`. 18 activity spine event types (§3). 25 work queue rules WQ-SAF-01 through WQ-SAF-25 (§4 — complete trigger/resolution catalog). 8 related-items relationship types (§5 — ORIGINATED_FROM, GENERATED, GOVERNS, FULFILLS, QUALIFIES_FOR, AMENDS). 7 report definitions (§6 — privacy-enforced incident register). 6 workflow handoff scenarios (§7). 7 BIC next-move prompts (§8). T10 builds on all T01–T09 for implementation closure.
**T10 Implementation Closure:** **Assessed 2026-03-24 in `@hbc/features-project-hub` v0.1.44.** P3-E8 Safety module contract-level implementation: T01–T09 complete (9 of 9 data model T-files). 45 source files, 17 test files, 507 safety tests across 9 subdirectories (foundation, records, lifecycle, inspection, corrective-actions, jha-toolbox, compliance, readiness, publication). Acceptance gate: 54 of 60 items satisfied at contract level; 6 items are runtime/integration/UAT scope (runtime acknowledgment workflow wiring, daily sweep job execution, work queue rule firing with timing windows, activity spine event publication on state transitions, AI-assisted toolbox gap detection, runtime blocker condition evaluation across 25 conditions). 6 shared package blockers (B-SAF-01 through B-SAF-06) stub-ready — `@hbc/acknowledgment` (3 config types defined), `@hbc/workflow-handoff` (6 handoff scenarios defined), `@hbc/bic-next-move` (7 prompts defined), `@hbc/my-work-feed` (25 WQ rules defined), `@hbc/related-items` (8 relationships defined), `@hbc/versioned-record` (audit trail fields present). All implementation stages (8.SAF.0–8.SAF.10) complete at contract level except 8.SAF.0 (partial — shared package tier verification runtime scope), 8.SAF.6 (partial — schedule integration API and AI gap detection runtime scope), 8.SAF.9 (partial — privacy enforcement runtime scope), 8.SAF.10 (partial — scorecard computation runtime, registration adapter wiring).

**7.6 — QC module (baseline-visible lifecycle)**
Implement the QC module at baseline-visible lifecycle depth only. Phase 3 does not include deeper field-first QC execution. Do not scope beyond the baseline-visible classification.
Governing: P3-E1 §3.7

**7.7 — Warranty module**
Implement the Warranty module as a governed **two-layer operating model**: Project Hub internal control surface (Layer 1, Phase 3 scope) plus deferred external collaborative workspace seams (Layer 2, future scope). Layer 1 delivers the Coverage Registry (`WarrantyCoverageItem` — scope, dates, responsible subcontractor), the Case Workspace (`WarrantyCase` — full open/assigned/in-progress/resolved/closed/voided lifecycle with SLA timers and escalation), Subcontractor Coordination (`SubcontractorAcknowledgment` — acknowledgment, scope-acceptance/dispute, resolution declarations), Owner Intake Log (PM-entered only; no owner-facing portal in Phase 3), and Resolution Records with back-charge advisory publication to Financial. Layer 2 (owner portal, direct owner intake, shared PM + owner + sub workspace) is explicitly deferred. Canonical warranty records are owned by `@hbc/features-project-hub` and must not be duplicated in a parallel system when Layer 2 is built. Implement spine adapters for all four spines (Activity, Health, Work Queue, Related Items) per P3-A3. Canvas tile surfaces open/overdue case counts and expiring coverage advisory. SPFx delivers read-only coverage and case list with Launch-to-PWA for all mutating actions. Deeper field-first Warranty execution beyond the Layer 1 operating model is explicitly not Phase 3 scope.
Governing: P3-E14 T01–T10; P3-E1 §3.8; P3-E2 §17; P3-E4 (back-charge advisory); P3-E10 (Closeout continuity seam); P3-E11 (Startup turnover handoff)
**P3-E14 Readiness Review:** **Completed 2026-03-24.** Pre-implementation blocker assessment per T10 §1–§2: Stages 1–2 (record model + state machine) are **unblocked** — no hard-blocker packages required. Stage 3 hard blockers (`@hbc/my-work-feed`, `@hbc/activity-timeline` SF28, P3-D2 Health Spine, `@hbc/record-form` SF23, `@hbc/saved-views` SF26) require adapter verification when reached — documented as pre-Stage-3 gate check. Package enhancement dependencies (SLA indicator and Next Move card for `@hbc/ui-kit`, Advisory priority tier for `@hbc/my-work-feed`, module adapter scaffolding for `@hbc/activity-timeline`, `@hbc/acknowledgment` Phase 3 readiness) are Stage 3–4 scope and not blocking Stages 1–2. T10 §8 cross-reference update checklist completed: P3-E1 §3.8 updated from baseline-visible to always-on lifecycle with full Layer 1 spec; P3-E1 row 16 reclassified; P3-E1 §4.2 updated; P3-E1 spine table updated; P3-E2 §17 Warranty SoT section added; P3-H1 §6.7 Warranty acceptance gate added (AC-WAR-01 through AC-WAR-46); P3-G1 §4.8.4 Warranty lane depth added. P3-E14 master index + T01–T10 committed. **Warranty implementation may begin at Stage 1.**

**7.8 — Project Closeout module**
Implement: Closeout Checklist (70 items, 7 sections including jurisdiction-configurable Section 7) with tri-state results and date tracking; Subcontractor Scorecard entry with weighted 6-section evaluation (Safety 20%, Quality 20%, Schedule 20%, CostMgmt 15%, Communication 15%, Workforce 10%) and aggregation dashboard; Lessons Learned form entry and organization-wide knowledge database. Section 6 completion events must trigger snapshot publication to the Reports module. Reports module assembles snapshots into release artifacts — it does NOT own Closeout data. Item 4.14 (80-day lien deadline) is auto-calculated from item 4.13.
Governing: P3-E1 §3.9, P3-E2 §14, **P3-E10** (field-level specification)
**T01 Operating Model, Scope, Surface Map, SoT Boundaries:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.45.** Closeout module foundation: `CloseoutRecordFamily` (16 record families per §3.1), `CloseoutSurfaceClass` (3 classes per §2.1 — ProjectScoped/OrgDerived/ProjectHubConsumption), `CloseoutOperationalSurface` (4 surfaces per §2.2), `CloseoutDerivedIndex` (3 org-wide indexes per §2.2), `CloseoutConsumptionPoint` (3 Project Hub panels per §2.2), `CloseoutAuthorityRole` (6 roles), `CloseoutAuthorityAction` (5 actions), `CloseoutLifecyclePhase` (5 phases per §4 — always-on activation model), `CloseoutFunction` (2 — ProjectScopedOperations vs. IntelligencePublication per §1), `CloseoutCrossModuleSource` (5 read-only sources per §3.2), SoT boundary matrix (11 rows per §5), cross-contract positioning (12 references per §6), 14 locked architecture decisions from master index, 7 shared package requirements (B-CLO-01 through B-CLO-07), 5 operating principles, surface class business rules (`isClass1Surface`/`isClass2Index`/`isClass3ConsumptionPoint`/`getSurfaceClass`), always-on activation rules (`isAlwaysOnSurface`/`isCloseoutPhaseRequired`), cross-module immutability enforcements (`canCloseoutMutate` always false, `canClass2IndexBeDirectlyWritten` always false), testing fixtures, contract stability tests. Closeout module Stage 7.8 T01 foundation complete.
**T02 Record Families, Identity, Field Architecture, Publication States:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.46.** Record-level data model: `CloseoutPublicationState` (8-state model per §2 — DRAFT/SUBMITTED/PE_REVIEW/REVISION_REQUIRED/PE_APPROVED/PUBLISHED/SUPERSEDED/ARCHIVED), publication state details with editable and org-eligible flags, publication state applicability matrix (10 record families per §2.1), `CloseoutChecklistItemResult` (4 values per §4.2), `CloseoutChecklistResponsibleRole` (8 roles per §4.2), `CloseoutChecklistLifecycleStageTrigger` (6 triggers per §4.2), `CloseoutChecklistJurisdiction` (2 values per §4.1), `CloseoutScorecardEvaluationType` (2 types per §4.3), `CloseoutReBidRecommendation` (3 values per §4.3), record family interfaces (`ICloseoutChecklist` 16 fields per §4.1, `ICloseoutChecklistItem` 25 fields per §4.2, `ISubcontractorScorecard` 29 fields per §4.3, `ILessonEntry` 19 fields per §4.4, `ILessonsLearningReport` 22 fields per §4.5), org intelligence provenance contracts (`ISubIntelligenceIndexEntry` per §3.1, `ILessonsIntelligenceIndexEntry` per §3.2, `ILearningLegacyFeedEntry` per §3.3), record family hierarchy (15 relationships per §1), autopsy invariants (6 per §5.1), autopsy relationships (5 per §5.2), immutability rules (10 per §6), publication state business rules (`isEditableState`/`isOrgEligibleState`/`isTerminalState`/`canTransitionTo`), publication applicability rules (`usesPublicationStates`/`isOrgPublishable`), checklist item rules (`requiresNaJustification`/`isGovernedItemDescriptionImmutable`), scorecard rules (`isInterimOrgEligible`/`isFinalCloseoutOrgEligible`), lesson entry validation (`isRecommendationValid`), testing fixtures (`createMockCloseoutChecklist`/`createMockSubcontractorScorecard`/`createMockLessonEntry`), contract stability tests (29 tests), business rule tests (50 tests). Closeout module Stage 7.8 T02 records complete.
**T03 Closeout Execution Checklist, Template Library, Overlay Model:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.47.** 70-item governed baseline checklist: `CloseoutChecklistSectionKey` (7 sections per §3 — Tasks/DocumentTracking/Inspections/Turnover/PostTurnover/CompleteProjectCloseoutDocuments/PBCJurisdiction), `CloseoutMilestoneKey` (9 milestone gates per §3 section gate rules), `CloseoutSpineEventKey` (7 Activity Spine events per §3/§5), `CloseoutTemplateAuthority` (4 roles per §1.1), 7 section definitions with governed item counts and gate rules, template authority matrix (MOE-controlled per §1.1), jurisdiction variants (PBC = 16 Section 7 items; Other = empty scaffold per §1.3), overlay rules (max 5 per section, `{section}.OL-{n}` format, not deletable, 500 char max, audit-logged per §4.1), 9-step instantiation sequence per §5, 8 section gate rules, 5 calculated item rules (items 4.14/6.1/6.3/6.4/6.5), 5 integration-driven items (Section 6), complete governed item catalog by section (Section 1: 5 items, Section 2: 13 items, Section 3: 11 items, Section 4: 15 items, Section 5: 5 items, Section 6: 5 items, Section 7 PBC: 16 items = 70 total), all items with full metadata (isGoverned, isRequired, responsibleRole, lifecycleStageTrigger, hasDateField, hasEvidenceRequirement, evidenceHint, isCalculated, calculationSource, linkedModuleHint, spineEventOnCompletion, milestoneGateKey), completion business rules (`isItemComplete`/`calculateSectionCompletionPct`/`calculateOverallCompletionPct` per §6), overlay rules (`canAddOverlayItem`/`formatOverlayItemNumber`/`isOverlayItemDeletable`), jurisdiction logic (`getJurisdictionSection7Behavior`/`getGovernedItemsForJurisdiction` — 70 for PBC, 54 for Other), stage activation (`isItemActiveForStage`), calculated item logic (`calculateLienDeadline` — item 4.13 + 80 days), Section 6 integration (`isSection6Item63Complete` — all subs FinalCloseout PE_APPROVED; `isSection6Item65Complete` — report PE_APPROVED), gate satisfaction (`isSectionGateSatisfied`), testing fixtures (`createMockGovernedChecklistItem`/`createMockChecklistSectionDefinition`), contract stability tests (46 tests), business rule tests (43 tests). Closeout module Stage 7.8 T03 checklist complete.
**T04 Lifecycle State Machine, Milestones, Evidence Gates, Approval Rules:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.48.** Project-level closeout lifecycle state machine: `CloseoutLifecycleState` (9 states per §2.1 — NOT_STARTED/ACTIVATED/IN_PROGRESS/INSPECTIONS_CLEARED/TURNOVER_COMPLETE/OWNER_ACCEPTANCE/FINAL_COMPLETION/ARCHIVE_READY/ARCHIVED), `CloseoutMilestoneStatus` (5 statuses per §4.1 — PENDING/EVIDENCE_PENDING/READY_FOR_APPROVAL/APPROVED/BYPASSED), `CloseoutMilestoneEvidenceType` (6 types per §4.1 — System/ChecklistItem/ChecklistSection/DocumentAttachment/CrossModuleSignal/SubsurfaceApproved), `CloseoutFullMilestoneKey` (13 milestones per §4.2 — including CHECKLIST_ACTIVATED/AUTOPSY_COMPLETE/ARCHIVE_READY beyond T03's 9-key subset), `CloseoutLifecycleLayer` (2 layers per §1), `CloseoutTransitionTriggerType` (3 types), 9 lifecycle state definitions, 8 state transitions with trigger types and PE approval flags (3 PE-gated: TURNOVER→OWNER_ACCEPTANCE, FINAL_COMPLETION→ARCHIVE_READY, ARCHIVE_READY→ARCHIVED), 13 milestone definitions with evidence types and external dependency flags (2 external: CO_OBTAINED/AHJ, OWNER_ACCEPTANCE/Owner; 2 PE-gated: OWNER_ACCEPTANCE, ARCHIVE_READY), `ICloseoutMilestoneRecord` (14 fields per §4.1), 8 archive-ready criteria per §4.3 (completion, Section 6, C.O., liens, scorecards, lessons, autopsy, financial payment), 12-row PE approval authority matrix per §5, 8 work queue trigger rules per §7, state machine business rules (`canTransitionState`/`getTransitionForStates`/`requiresPEApprovalForTransition`/`isTerminalLifecycleState`/`isNoRollbackState`), milestone business rules (`getMilestoneForState`/`isExternalDependency`/`requiresPEApprovalForMilestone`), archive-ready gate evaluation (`evaluateArchiveReadyCriteria`/`areAllArchiveReadyCriteriaMet` — per-criterion pass/fail), work queue trigger lookup (`getWorkQueueTriggersForState`), testing fixtures (`createMockCloseoutMilestoneRecord`/`createMockArchiveReadyContext`), contract stability tests (27 tests), business rule tests (40 tests). Closeout module Stage 7.8 T04 lifecycle complete.
**T05 Lessons Learned Operating Model and Intelligence Publication:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.49.** Lessons learned domain model: `LessonCategory` (15 categories per §2.1 — PreConstruction through Other), `DeliveryMethod` (7 methods per §2.2), `MarketSector` (13 sectors per §2.2), `ProjectSizeBand` (6 bands per §2.2), `ImpactMagnitude` (4 levels per §2.2 — Minor/Moderate/Significant/Critical), 3-layer architecture definitions per §1 (raw ledger/synthesis container/institutional synthesis), impact magnitude thresholds (4 rows per §3.2 with cost and schedule conditions), system-derived impact magnitude engine (`extractCostSignal`/`extractScheduleSignal`/`getMagnitudeForCost`/`getMagnitudeForSchedule`/`resolveMultiSignalMagnitude`/`deriveImpactMagnitude` per §3 — multi-signal: higher governs; no signal: returns null for 422), approved action verb set (32 verbs per §4), recommendation validation (`isRecommendationActionVerbValid`/`getRecommendationValidationError` — positive allowlist supersedes T02 negative list), 4-step workflow (rolling capture/closeout synthesis/PE review/org publication per §5), 9 business rules per §6 (rolling capture at any state, one report per project, minimum one entry, all entries linked, sequential numbering, PE gates item 6.5, immutability after PE_APPROVED, keyword deduplication, frozen snapshot for Reports), Reports module snapshot contract (`ILessonsLearnedPublicationSnapshot` with reportHeader/entries/aggregateStats per §7), snapshot aggregation (`calculateAggregateStats` — categoryCounts/magnitudeCounts/highApplicabilityCount/criticalAndSignificantCount), workflow rules (`canCreateLessonEntry`/`canSubmitLessonsReport`/`normalizeKeywords`), 7-row autopsy relationship comparison per §8, testing fixtures (`createMockLessonsLearnedSnapshot`), contract stability tests (25 tests), business rule tests (41 tests). Closeout module Stage 7.8 T05 lessons complete.
**T06 Subcontractor Scorecard Model and Intelligence Publication:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.50.** Scorecard evaluation model: `ScorecardSectionKey` (6 sections per §2.2 — Safety 20%/Quality 20%/Schedule 20%/CostMgmt 15%/Communication 15%/Workforce 10%), `PerformanceRating` (5 ratings per §3.3 — Exceptional/AboveAverage/Satisfactory/BelowAverage/Unsatisfactory), `ScorecardScoreValue` (1–5 + NA per §2.1), 6 section definitions with weights summing to 1.00 and per-criterion weights, 6 score definitions with labels and behavioral definitions, 5 performance rating ranges per §3.3, complete 29-criterion catalog (Safety 5 + Quality 5 + Schedule 5 + CostMgmt 5 + Communication 5 + Workforce 4) each with evidence guidance per §2.3, scoring formulas (`calculateSectionAverage` per §3.1 — weighted average excluding NA, rounded 2 dec; `calculateOverallWeightedScore` per §3.2 — sum of sectionAvg × sectionWeight; `calculateOverallFromSections` convenience; `derivePerformanceRating` per §3.3 — threshold-derived, not user-selectable), submission validation (`canSubmitScorecard` — non-null score + reBid + date ≤ today per §4.2; `isFinalCloseoutDuplicate` — 409 Conflict per §4.1), amendment rules (`canCreateFinalCloseoutAmendment` — requires existing approved + PE allowAmendment per §4.5), interim publication exception (`isInterimPublicationException` — PE explicit grant per §5.2), 8-row project visibility matrix per §5.1, 3-row org access rules per §5.3 (PE/PER/MOE full; SUB_INTELLIGENCE_VIEWER limited; others none), testing fixtures (`createMockScorecardCriterionScores`), contract stability tests (22 tests), business rule tests (28 tests). Closeout module Stage 7.8 T06 scorecard complete.
**T07 Project Autopsy and Learning Legacy:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.51.** Institutional synthesis engine: `AutopsyTheme` (12 sections per §9.1 — 10 always-applicable + OccupancyUserExperience conditional + DeveloperAssetOutcomes conditional), `AutopsyFindingType` (5 types per §11.1 — Strength/Gap/Risk/Opportunity/SystemicPattern), `AutopsyRecurrenceRisk` (3 levels), `AutopsyFindingSeverity` (4 levels), `AutopsyActionType` (8 types per §13.2), `AutopsyActionStatus` (5 statuses per §13.1), `LearningLegacyOutputType` (8 output types per §14.3), `AutopsyWorkshopFormat` (4 formats per §10), `RootCauseCategory` (14 categories per §12.3), `PreSurveyResponseType` (5 types per §6.2), `FindingEvidenceRefType` (8 ref types per §11.2), `IAutopsyRecord` (31 fields per §10), `IAutopsyFinding` (18 fields per §11), `IAutopsyAction` (14 fields per §13.1), `ILearningLegacyOutputRecord` (18 fields per §14.2), pre-survey model (`IPreSurveyQuestion`/`IPreSurveyResponseRecord` per §6), 12 section definitions with applicability rules, 5 finding type definitions, 8 action type definitions with typical owners, 4 root-cause levels per §12.2, 10-block workshop agenda per §8.2, 10 pre-briefing data sources per §7.1, 3-layer comparison per §2, business rules (`isAutopsyWaiverValid`/`isSectionApplicable`/`getApplicableSections` — 10/11/12 based on flags; `canPublishOutput`/`getOutputPublicationBlockers` — three-condition gate per §14.4; `isActionTitleValid` — action verb per §13.1), testing fixtures (`createMockAutopsyRecord`), contract stability tests (28 tests), business rule tests (21 tests). Closeout module Stage 7.8 T07 autopsy complete.
**T08 Project Hub Consumption, Derived Intelligence Indexes, and Reporting:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.52.** Org intelligence consumption layer: `CloseoutDataClass` (3 classes per §1 — ProjectOperational/PublishedIntelligence/ReadOnlyAggregation), `CloseoutOrgIndex` (3 indexes per §2 — LessonsIntelligence/SubIntelligence/LearningLegacy), `CloseoutHealthSpineDimension` (4 metrics per §5.2 — closeoutCompletionPct/scorecardCoverage/lessonsReadiness/autopsyReadiness), 3 data class definitions with write/read path rules, 3 org index definitions with population triggers and unit of entry, 3 search dimension configurations (full-text, filter, sort fields per index), contextual relevance scoring formula per §2.1 (`calculateLessonsRelevanceScore` — sector×3 + method×2 + size×1 + applicability×0.5), 3 Project Hub consumption surface definitions (Contextual Lessons Panel, Sub Vetting Intelligence Panel, Learning Legacy Feed per §3), 17 activity spine events per §5.1 (checklist through archived), 4 health spine metrics per §5.2, 2 report artifact families per §6.1 (sub-scorecard, lessons-learned), 2 snapshot API preconditions per §6.2 (PE_APPROVED + PE role), 7 UI data class rules per §4 (operational vs. published intelligence mutability and navigation), role-gated SubIntelligence query enforcement (`isSubIntelligenceQueryAllowed` — PE/PER/MOE/SUB_INTELLIGENCE_VIEWER per §2.2), snapshot generation validation (`canGenerateSnapshot`), read-only enforcement (`isProjectHubSurfaceReadOnly`), testing fixtures (`createMockProjectProfile`/`createMockIndexEntryProfile`), contract stability tests (22 tests), business rule tests (15 tests). Closeout module Stage 7.8 T08 consumption complete.
**T09 Permissions, Visibility, Executive Review, Role Matrix:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.53.** Complete role-based access model: `CloseoutRole` (6 roles per §1 — PM/SUPT/PE/PER/MOE/SUB_INTELLIGENCE_VIEWER), `CloseoutIntelligenceVisibility` (2 regimes per §3.1 — BroadlyAvailable/Restricted), `CloseoutAnnotationSource` (2 per §4.3 — PE/PER), `SubIntelligenceAccessLevel` (3 tiers per §3.3), 6 role definitions with scope and authority per §1, 31-row master role matrix across 7 categories (Checklist/Scorecards/Lessons/Autopsy/Lifecycle/Annotations/Templates per §2), 4-regime intelligence visibility model per §3.1, 16-field SubIntelligence field visibility matrix per §3.3 (narratives and financials restricted from SUB_INTELLIGENCE_VIEWER), 2 annotation visibility rules per §4.3 (PER annotations not visible to SUPT), 7-dimension PE approval vs. annotation comparison per §5 (formal contract: annotations never write to operational records, never advance publication status), 7 PE formal review surfaces per §6.1, 6 PE non-triggering routine actions per §6.2, 5 PE work queue items per §6.3, 6-section SUPT checklist mutation scope per §7 (Sections 1/3/4 mutable; 2/5/6 read-only), business rules (`canPerformAction` — matrix lookup; `isSubIntelligenceFieldVisible` — per access level; `isSuptChecklistSectionMutable`; `isPEWorkQueueTrigger`; `isAnnotationVisibleToRole`), testing fixtures (`createMockCloseoutRoleAction`), contract stability tests (21 tests), business rule tests (25 tests). Closeout module Stage 7.8 T09 permissions complete.
**T10 Lane Ownership and Shared Package Reuse:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.54.** Package identity and integration contracts: `@hbc/project-closeout` at L5 Feature layer (§1.1), 5-surface PWA/SPFx classification (checklist and scorecard full on both; lessons PWA-authoring/SPFx-read-only; autopsy PWA-only/SPFx-summary; health summary full/widget per §2.1), 6 SPFx constraints per §2.2, 4 PWA features per §2.3, 7 shared package integration contracts (4 related-items pairs per §3.1 with auto-write permanently blocked, 6 versioned records, 8 annotation attachment points, 5 workflow handoff triggers, 2 acknowledgment use cases, 5 BIC prompts, 5 notifications), 3 spine publication contracts per §4 (Activity 17 events, Health 4 dimensions, Work Queue 7 items), 11 allowed dependencies + 6 prohibited feature-to-feature imports per §5.1 (@hbc/financial, @hbc/permits, @hbc/safety, @hbc/schedule, @hbc/reports, org-intelligence), 7 lane capability entries per §6 (ProjectRecords, OrgIntelligenceWrite, LifecycleGating, WorkQueue, HealthSignal, ActivitySignal, ReportsSnapshot), 6 external ownership items per §1.3, business rules (`isAllowedDependency`/`getSurfaceCapability`/`isRelatedItemAutoWriteBlocked`), testing fixtures (`createMockRelatedItemsPair`), contract stability tests (23 tests), business rule tests (6 tests). Closeout module Stage 7.8 T10 integration complete.

**7.9 — Project Startup module**
Implement Project Startup as a **top-level readiness program** that is active from project creation, not as five peer forms. The module must deliver six governed subordinate surfaces: (1) Startup Program Checklist — a governed template-instance library, not a flat checklist, with 55 project-scoped readiness tasks across 4 sections and per-task category, severity, gating, owner, due-trigger, dependency, and evidence metadata; (2) Startup Safety Readiness — a 32-item, 2-section remediation-capable startup-phase safety assessment in which `Fail` creates a first-class `SafetyRemediationRecord` with assignment, due-date, evidence, escalation, and blocker linkage; safety certification allows documented open remediations at submission when they are assigned and due-dated, but blocks on PX-escalated remediations or active blocker conditions unless waived; (3) Permit Posting Verification — Section 4 evidence-and-cross-reference verification, distinct from the Permits module lifecycle ledger, with one companion `PermitVerificationDetail` per Section 4 task, photo evidence for `Yes`, discrepancy reasons and blocker routing for `No`, and no write path to Permits; (4) Contract Obligations Register — an active monitoring ledger, not a one-time Owner Contract Review form, with `OPEN` / `IN_PROGRESS` / `SATISFIED` / `WAIVED` / `NOT_APPLICABLE` lifecycle states, `flagForMonitoring`-driven routing, due-date and recurrence handling, and certification based on documented review/routing/acknowledgment of monitored and near-due obligations rather than blanket closure; (5) Responsibility Routing Engine — a role-accountability routing engine, not a spreadsheet artifact, with 7 PM assignment columns covering 71 assignment-bearing rows plus 11 reminder-only rows, 5 Field assignment columns covering 27 assignment-bearing rows, category-level named-`Primary` coverage gates, critical-category `acknowledgedAt` gates, and PM Plan Section VII read-through via `responsibilityMatrixRef`; (6) Project Execution Baseline — the 11-section PM Plan as a typed launch-commitment record, with structured baseline fields, categorized `ExecutionAssumption` records, PM/PX signatures plus PX approval before `EXECUTION_BASELINE` certification, and baseline lock producing an immutable `StartupBaseline` that Closeout reads instead of live PM Plan records. T02 fixes the record layer as 28 Startup-owned record families across four tiers: program-core governance, governed template/task-library, project-scoped operational surfaces, and immutable continuity. T03 fixes the checklist surface as MOE-governed `StartupTaskTemplate` records instantiating project `StartupTaskInstance` records plus first-class `TaskBlocker` handling, with task completion separated from PE readiness approval, due dates anchored to planned launch data in the Project Execution Baseline, and Section 4 permit items remaining governed by T07 and read-only toward Permits. T07 fixes Startup Safety as a review-capable remediation/escalation surface despite the Safety module's executive-review exclusion, and fixes Permit Posting as a Section 4 companion-record verification surface rather than an independent permit ledger. T08 fixes Startup publication and consumption as a shared-spine and canvas contract only: Startup publishes governed Activity Spine events, Health Spine metrics, Work Queue items, Related Items relationships, and a project-canvas mobilization summary tile; it does **not** publish to Reports, does **not** create org-intelligence indexes, and exposes Closeout continuity only through the read-only `StartupBaseline` API. T09 fixes Startup governance, lane ownership, and package-boundary posture: the Project Startup planning family is owned by `@hbc/features-project-hub`, not a standalone `@hbc/project-startup` package; PX retains exclusive authority for certification acceptance, waivers, PE mobilization authorization, and baseline lock; PWA remains the full-depth lane while SPFx keeps broad direct parity with explicit Launch-to-PWA depth deferrals; shared-package reuse is limited to the current repo-truth integration set; and no direct cross-feature imports or parallel local substitutes are permitted. Executive review annotations use `@hbc/field-annotations` and remain formally distinct from certification review, PE mobilization authorization, and baseline lock. Startup must treat PE mobilization authorization as a separate gate after all six certifications are accepted or waived, preserve the stabilization window before baseline lock, and create an immutable `StartupBaseline` handoff record for Closeout delta analysis. Financial and Schedule integrations for Startup remain read-only advisory context, while Permits Section 4 boundary rules, Safety non-interference, annotation isolation, Closeout read-only continuity, and `@hbc/features-project-hub` feature-boundary enforcement must be preserved throughout.
Governing: P3-E1 §3.10, P3-E2 §15, P3-E3 §8.4, **P3-E11-T01 through P3-E11-T10**
**T10 Stage 1 — Program Core and State Machine:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.64.** Complete program-core governance contracts: `StartupReadinessStateCode` (8-state machine per T01 §4.1 — DRAFT → ACTIVE_PLANNING → READINESS_REVIEW → READY_FOR_MOBILIZATION → MOBILIZED → STABILIZING → BASELINE_LOCKED → ARCHIVED), `StartupSubSurface` (6 surfaces per T01 §3.2), `StartupCertificationStatus` (7 values per T02 §3.4), `GateOutcome` (3 per T02 §3.5), `GateCriterionResult` (4 per T02 §3.6), `WaiverStatus` (5 per T02 §3.9), `ProgramBlockerScope` (2 per T02 §3.10), `ProgramBlockerType` (6 per T02 §3.10), `ProgramBlockerStatus` (3 per T02 §3.10), `MobilizationAuthStatus` (2 per T02 §3.11), `StartupAuthorityRole` (10 per T09 §1), `PXExclusiveAction` (6 per T09/T10); 9 Tier 1 record interfaces (`IStartupProgram` 14 fields per T02 §3.1, `IStartupProgramVersion` 9 fields per T02 §3.2, `IStartupReadinessState` 6 fields per T02 §3.3, `IReadinessCertification` 16 fields per T02 §3.4, `IReadinessGateRecord` 10 fields per T02 §3.5, `IReadinessGateCriterion` 6 fields per T02 §3.6, `IExceptionWaiverRecord` 18 fields per T02 §3.9, `IProgramBlocker` 17 fields per T02 §3.10, `IPEMobilizationAuthorization` 14 fields per T02 §3.11); 9 valid state transitions per T01 §4.2 with from/to/trigger/condition/requiresPE; 6 sub-surface definitions per T01 §3.2; 28 record family definitions across 4 tiers per T02 §1 (9 Tier 1 + 3 Tier 2 + 15 Tier 3 + 1 Tier 4); 18 SoT boundary rows per T01 §8.2; 16 governed gate criteria across 6 surfaces per T02 §3.7 (3 STARTUP_TASK_LIBRARY + 3 SAFETY_READINESS + 2 PERMIT_POSTING + 3 CONTRACT_OBLIGATIONS + 2 RESPONSIBILITY_MATRIX + 3 EXECUTION_BASELINE); 6 role-scoped certification ownership entries per T02 §3.8; 10 locked architecture decisions per P3-E11 master index; 12 cross-contract references per T01 §9; 6 hard blocker shared package requirements per T10 §1; 3 operating principles per T01 §1.1; 6 Stage 1 Activity Spine events (`StartupProgramCreated`, `StartupStateTransitioned`, `ProgramBlockerCreated`, `ProgramBlockerResolved`, `StartupMobilizationAuthorized`, `StartupBaselineLocked`); 3 Health Spine metrics (`startupReadinessState`, `startupCertificationProgress`, `startupOpenProgramBlockerCount`); canvas tile registration (`StartupCanvasTileAdapter`); business rules (`isValidStateTransition` — T01 §4.2 transition validation, `requiresPEForTransition`, `getTransitionTriggerType`, `canAdvanceToReadyForMobilization` — all 6 ACCEPTED/WAIVED, `canIssueMobilizationAuth` — certs + no open PROGRAM blockers, `isStartupOwnedRecord` — 28 families, `isPXExclusiveAction`, `isWaiverLapsed` — T02 §3.9 lapse rule, `canStartupMutateCrossModuleData` — always false); label maps for all enums; testing fixtures (`createMockStartupProgram`); contract stability tests (55 tests), business rule tests (45 tests). Startup module Stage 7.9 T10 Stage 1 foundation complete.
**T10 Stage 2 — Task Library:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.65.** Complete governed task library contracts per T03: `StartupTaskSectionCode` (4 sections per T03 §2 — REVIEW_OWNER_CONTRACT, JOB_STARTUP, ORDER_SERVICES, PERMIT_POSTING), `StartupTaskCategory` (12 categories per T03 §3), `StartupTaskSeverity` (3 levels per T03 §4 — CRITICAL/HIGH/STANDARD), `StartupTaskGatingImpact` (3 per T03 §4 — BLOCKS_CERTIFICATION/REQUIRES_BLOCKER_IF_OPEN/ADVISORY), `StartupTaskOwnerRole` (6 roles per T03 §4 — PM/PA/PROJ_ACCT/PX/SUPERINTENDENT/SAFETY_MANAGER), `StartupTaskDueTrigger` (5 triggers per T03 §6 — ON_PROJECT_CREATION/ON_CONTRACT_EXECUTION/ON_NTP_ISSUED/DAYS_BEFORE_MOBILIZATION/NONE), `StartupTaskResult` (3 per T03 §2 — YES/NO/NA + null), `TaskBlockerType` (8 per T03 §8), `TaskBlockerStatus` (3 per T03 §8), `StartupTaskEvidenceType` (9 per T03 §7); 3 record interfaces (`IStartupTaskTemplate` 19 fields per T03 §2 — org-governed with immutable taskNumber/title, `IStartupTaskInstance` 24 fields per T03 §2 — project-scoped with 8 immutable template-inherited fields and PATCH → HTTP 409 contract, `ITaskBlocker` 15 fields per T03 §8 — auto-creation on CRITICAL NO, linkedWaiverId for waiver path); full 55-task governed template catalog per T03 §12 (Section 1: 4 tasks — contractual obligation review, Section 2: 33 tasks — job startup covering admin/financial/sub/owner/legal/safety/schedule/team/community, Section 3: 6 tasks — site services and equipment, Section 4: 12 tasks — permit posting with all `activeDuringStabilization = true` and `category = PERMIT_POSTING` and `dueOffsetDays = 0`); 5 key dependency chains per T03 §5.2 (DAG-validated — 2.6→2.3, 2.7→2.4+2.6, 2.17→2.4, 2.24→2.4, 2.28→2.13); 8 immutable template fields per T03 §2; certification eligibility rules per T03 §13 (all reviewed or blocked, CRITICAL NO requires blocker/waiver, HIGH NO requires blocker, no empty auto-stubs); completion percentage formula per T03 §13.2 (YES/non-NA × 100); 4 Stage 2 Activity Spine events (`StartupTaskLibraryActivated`, `StartupTaskInstanceUpdated`, `TaskBlockerCreated`, `TaskBlockerResolved`); 2 Health Spine metrics (`startupTaskCompletionRate`, `startupOpenTaskBlockerCount`); 2 Work Queue items (`CriticalTaskUnstarted`, `TaskBlockerOpen`); business rules (`isImmutableTemplateField` — 8-field guard, `canSetTaskResult` — T03 §5 dependency validation, `isTaskOverdue` — T03 §6 overdue logic, `shouldAutoCreateBlocker` — T03 §8.3 CRITICAL+NO rule, `isTaskEditableDuringStabilization` — T03 §11 stabilization editability, `canSubmitTaskLibraryCertification` — T03 §13 4-rule eligibility, `computeCompletionPercentage` — T03 §13.2 formula, `getTaskGatingEffect` — T03 §4 enforcement); label maps for all enums; testing fixtures (`createMockStartupTaskInstance`, `createMockTaskBlocker`); contract stability tests (46 tests), business rule tests (62 tests). Startup module Stage 7.9 T10 Stage 2 task library complete.
**T10 Stage 3 — Safety Readiness:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.66.** Complete safety readiness contracts per T07: `SafetyReadinessSectionTitle` (2 per T07 §3 — AreasOfHighestRisk/OtherRisks), `SafetyReadinessResult` (3 per T07 §4 — Pass/Fail/NA + null), `RemediationStatus` (4 per T07 §5 — PENDING/IN_PROGRESS/RESOLVED/ESCALATED), `EscalationLevel` (3 per T07 §5.2 — NONE/PM/PX); 4 record interfaces (`IJobsiteSafetyChecklist` 14 fields per T07 §3 — header with calculated pass/fail/na/openRemediation/escalatedRemediation counts and certificationStatus mirror, `ISafetyReadinessSection` 5 fields per T07 §3 — 2 per checklist with immutable sectionNumber/sectionTitle/itemCount, `ISafetyReadinessItem` 10 fields per T07 §4 — immutable itemNumber/description with versioned result via `@hbc/versioned-record`, `ISafetyRemediationRecord` 20 fields per T07 §5 — auto-created on Fail with full remediation lifecycle including escalation and ProgramBlocker linkage); full 32-item governed template catalog per T07 §6 (Section 1: 4 items — Fall Exposures/Electrical Shocks/Struck by Risks/Crushed by Risks, Section 2: 28 items — Blasting through Welding & Cutting); remediation state machine (7 valid transitions per T07 §5.0 — PENDING→IN_PROGRESS/ESCALATED/RESOLVED, IN_PROGRESS→RESOLVED/ESCALATED, ESCALATED→IN_PROGRESS/RESOLVED); 3 escalation thresholds per T07 §5.2 (unassigned 2 business days→PM, overdue→PM, overdue 3+ days→PX with ProgramBlocker creation per T07 §5.3); certification eligibility per T07 §7 (all 32 assessed, all Fail items have remediationNote + assignedPersonName + dueDate, no PX-escalated or blocker-active without waiver; open remediations permitted when documented); Safety module non-interference per T07 §2.1 (zero writes to `@hbc/safety`); 4 Stage 3 Activity Spine events (`SafetyReadinessItemUpdated`, `SafetyRemediationCreated`, `SafetyRemediationEscalated`, `SafetyRemediationResolved`); 2 Health Spine metrics (`safetyReadinessOpenRemediations`, `safetyReadinessEscalatedRemediations`); 4 Work Queue items (`SafetyRemediationPending`, `SafetyRemediationUnassigned`, `SafetyRemediationOverdue`, `SafetyRemediationEscalated`); business rules (`isValidRemediationTransition` — T07 §5.0 state machine, `shouldAutoCreateRemediation` — Fail auto-creation, `isRemediationDocumented` — 3-field documentation check, `canSubmitSafetyReadinessCertification` — T07 §7 5-rule eligibility, `getEscalationLevel` — T07 §5.2 threshold evaluation, `shouldCreateProgramBlocker` — PX-level trigger, `computeSafetyReadinessCounts` — pass/fail/na counts, `canStartupSafetyWriteToSafetyModule` — always false non-interference); label maps for all enums; testing fixtures (`createMockSafetyReadinessItem`, `createMockSafetyRemediationRecord`); contract stability tests (37 tests), business rule tests (49 tests). Startup module Stage 7.9 T10 Stage 3 safety readiness complete.
**T10 Stage 4 — Permit Posting Verification:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.67.** Complete permit posting verification contracts per T07 §8-§9: `StartupPermitType` (12 types per T07 §9.2 — Master/Roofing/Plumbing/HVAC/Electrical/FireAlarm/FireSprinkler/Elevator/Irrigation/LowVoltage/SiteUtilities/RightOfWay), `PermitVerificationResult` (3 per T07 §9 — Yes/No/NA + null), `AppSurface` (2 — PWA/SPFx); 1 record interface (`IPermitVerificationDetail` 14 fields per T07 §9.1 — one-to-one companion to Section 4 StartupTaskInstance with permitType, physicalEvidenceAttachmentIds for photo evidence, verifiedBy/verifiedAt for confirmation, discrepancyReason for No results, permitModuleRecordRef/permitStatusFromModule/permitExpirationFromModule for read-only P3-E7 cross-reference display); 12 permit type mappings (task numbers 4.01-4.12 per T07 §9.2); verification model (Yes requires verifiedBy + verifiedAt + physicalEvidenceAttachmentIds with HTTP 400 on missing fields per T10 criterion 14; No requires discrepancyReason with PendingPermit TaskBlocker and PermitNotPosted Work Queue routing per T07 §9.4; photo upload PWA-depth only per T09 §7.2 — SPFx supports metadata entry but defers upload); Permits module non-interference per T07 §8.1 (zero P3-E7 writes; permit status does not auto-resolve Section 4 items; verified by integration test per T10 criterion 13); certification eligibility per T07 §9.5 (all 12 assessed, Yes items have evidence, No items have discrepancy, NA excluded); 1 Stage 4 Activity Spine event (`PermitPostingVerified`); 1 Work Queue item (`PermitNotPosted`); business rules (`isPermitVerificationComplete` — result-dependent field validation, `canConfirmPermitPosted` — 3-field Yes check, `requiresDiscrepancyReason` — No trigger, `shouldRaisePermitNotPostedWorkItem` — No trigger, `canSubmitPermitPostingCertification` — T07 §9.5 certification eligibility, `canStartupWriteToPermitsModule` — always false non-interference, `isPhotoUploadAvailable` — PWA/SPFx depth); label maps for all enums; testing fixtures (`createMockPermitVerificationDetail`); contract stability tests (20 tests), business rule tests (33 tests). Startup module Stage 7.9 T10 Stage 4 permit posting verification complete.
**T10 Stage 5 — Contract Obligations Register:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.68.** Complete contract obligations register contracts per T04: `ObligationStatus` (5 per T04 §4 — OPEN/IN_PROGRESS/SATISFIED/WAIVED/NOT_APPLICABLE), `ObligationCategory` (12 per T04 §5 — SPECIAL_TERMS through OTHER with LIQUIDATED_DAMAGES auto-flag), `ObligationTriggerBasis` (10 per T04 §3.1 — PROJECT_START through NONE including RECURRING_MONTHLY/QUARTERLY), `MonitoringPriority` (3 per T04 §6.2 — HIGH/MEDIUM/LOW), `ContractType` (9 per T04 §9), `StartupDeliveryMethod` (8 per T04 §9); 2 record interfaces (`IContractObligationsRegister` 19 fields per T04 §2 — header with calculated total/open/inProgress/flagged/overdue counts and certificationStatus mirror, `IContractObligation` 28 fields per T04 §3 — full obligation row with lifecycle, monitoring, evidence, waiverNote, and versioned obligationStatus via `@hbc/versioned-record`); 9 valid state transitions per T04 §4 with PX guards (WAIVED requires PX+waiverNote; SATISFIED/NOT_APPLICABLE/WAIVED→OPEN reopen requires PX+notes; IN_PROGRESS→SATISFIED requires evidence or notes); 12 obligation category definitions per T04 §5 with routing implications; 5 monitoring trigger rules per T04 §6.1; 3 monitoring priority lead days per T04 §6.2 (HIGH=21/MEDIUM=14/LOW=7); certification eligibility per T04 §7 (≥1 obligation, flagged have notes, near-due OPEN addressed, LIQUIDATED_DAMAGES/INSURANCE_REQUIREMENTS have dueDate — documented review not blanket closure); 2 Stage 5 Activity Spine events (`ContractObligationAdded`, `ContractObligationStatusChanged`); 2 Health Spine metrics (`contractObligationsFlagged`, `contractObligationsOverdue`); 3 Work Queue items (`ObligationOpenFlagged`, `ObligationDueSoon`, `ObligationOverdue`); business rules (`isValidObligationTransition`, `requiresPXForTransition`, `requiresWaiverNote`, `requiresEvidenceForSatisfied`, `isObligationOverdue`, `getMonitoringLeadDays`, `shouldAutoFlagForMonitoring`, `canSubmitContractObligationsCertification`, `computeObligationCounts`); label maps for all enums; testing fixtures (`createMockContractObligation`); contract stability tests (31 tests), business rule tests (53 tests). `StartupDeliveryMethod`/`STARTUP_DELIVERY_METHODS` prefixed to avoid collision with closeout lessons module. Startup module Stage 7.9 T10 Stage 5 contract obligations register complete.
**T10 Stage 6 — Responsibility Matrix:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.69.** Complete responsibility matrix contracts per T05: `MatrixSheet` (2 — PM/Field), `PMRoleCode` (7 per T05 §6), `FieldRoleCode` (5 per T05 §7), `AssignmentValue` (4 per T05 §8 — Primary/Support/SignOff/Review), `PMTaskCategory` (7 per T05 §8), `FieldTaskCategory` (4 per T05 §10); 3 record interfaces (`IResponsibilityMatrix` 13 fields per T05 §3 — header with calculated pmRowsWithPrimaryCount/fieldRowsWithPrimaryCount/unacknowledgedCriticalCount, `IResponsibilityMatrixRow` 10 fields per T05 §4 — governed rows with immutable taskDescription/taskCategory/sheet and isReminderOnly/isCustomRow flags, `IResponsibilityAssignment` 12 fields per T05 §5 — cell with value/assignedPersonName/acknowledgedAt versioned via `@hbc/versioned-record`); PM sheet: 7 role columns, 71 assignment-bearing + 11 reminder-only rows across 7 categories; Field sheet: 5 role columns, 27 assignment-bearing rows across 4 categories; 5 critical categories requiring acknowledgedAt (PM: PX/QAQC/ProjAcct, Field: LeadSuper/QAQC_Field per T05 §9.2); governed row immutability (taskDescription/taskCategory/sheet → HTTP 400; custom rows allow taskDescription edits); certification per T05 §9.1+§9.2 (all PM and Field categories have named Primary, all critical primaries acknowledged; reminder-only and custom rows excluded from gates); PM Plan Section VII `responsibilityMatrixRef` integration; 2 Stage 6 Activity Spine events (`ResponsibilityAssignmentUpdated`, `ResponsibilityAssignmentAcknowledged`); 1 Health Spine metric (`responsibilityMatrixReadiness`); 2 Work Queue items (`MatrixUnassignedCategory`, `MatrixAcknowledgmentPending`); business rules (`isGovernedRowFieldImmutable`, `isCustomRowFieldMutable`, `isCriticalCategory`, `hasPrimaryCoverage`, `canSubmitResponsibilityMatrixCertification`, `isAssignmentAcknowledged`, `computeMatrixReadiness`); label maps for all enums; testing fixtures (`createMockResponsibilityAssignment`); contract stability tests (31 tests), business rule tests (36 tests). Startup module Stage 7.9 T10 Stage 6 responsibility matrix complete.
**T10 Stage 7 — Project Execution Baseline (PM Plan):** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.70.** Complete execution baseline contracts per T06: `BaselineStatus` (4 per T06 §2.1 — Draft/Submitted/Approved/Archived), `BaselineFieldType` (6 per T06 §4), `AssumptionCategory` (9 per T06 §8), `AssumptionRiskLevel` (3 per T06 §7); 5 record interfaces (`IProjectExecutionBaseline` 16 fields per T06 §2 — header with approval flow and certificationStatus, `IExecutionBaselineSection` 9 fields per T06 §3 — 11 immutable sections with content and isComplete, `IBaselineSectionField` 8 fields per T06 §4 — typed commitment records with fieldKey/fieldType/isBaselineQueryable and versioned value via `@hbc/versioned-record`, `IExecutionAssumption` 14 fields per T06 §7 — 9-category assumptions with isSuccessCriterion and successMeasure required-when pattern, `IPlanTeamSignature` 5 fields per T06 §2.2 — PM+PX signatures both required with signedAt); 11-section structured baseline per T06 §3.1 (I Philosophy through XI Attachments with Section VII referencing ResponsibilityMatrix); 4 approval flow transitions per T06 §2.1 (Draft→Submitted→Approved; Submitted→Draft revert for editing); 7 critical certification fields per T06 §2.3 (safetyOfficerName/safetyOfficerRole from Section IV, projectStartDate/substantialCompletionDate/noticeToProceedDate/goalSubstantialCompletionDate/goalFinalCompletionDate from Section VI); 9 assumption category definitions per T06 §8; certification per T06 §2.3 (Approved + 7 critical fields + PM/PX signatures); baseline lock produces immutable `StartupBaseline.executionBaselineFieldsAtLock` snapshot for Closeout delta analysis; 3 Stage 7 Activity Spine events (`PMPlanSubmitted`, `PMPlanApproved`, `ExecutionAssumptionAdded`); 1 Health Spine metric (`pmPlanStatus`); 1 Work Queue item (`PMPlanPendingApproval`); business rules (`isValidBaselineStatusTransition`, `canSubmitPMPlan`, `canApprovePMPlan`, `canEditPMPlan`, `areCriticalFieldsPopulated`, `hasRequiredSignatures`, `canSubmitExecutionBaselineCertification`, `isAssumptionValid`, `computeBaselineSectionCompleteness`); label maps for all enums; testing fixtures (`createMockExecutionBaseline`); contract stability tests (30 tests), business rule tests (48 tests). Startup module Stage 7.9 T10 Stage 7 execution baseline complete.
**T10 Stage 8 — Baseline Lock and Closeout Continuity (FINAL STAGE):** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.71.** Complete baseline lock and Closeout continuity contracts per T02 §7 and T01 §7.5: `BaselineLockActor` (2 — PE/SYSTEM), `BaselineAPIMethod` (4 — GET/PATCH/PUT/DELETE), `BaselineReadRole` (2 — PX/CloseoutService); `IStartupBaseline` Tier 4 immutable snapshot (22 fields per T02 §7.2 — snapshotId, programId, projectId, lockedAt, lockedBy, programStateAtLock, stabilizationWindowDays/ActualDuration, 5 sub-surface snapshots: taskLibrarySnapshotAtLock with total/yes/no/na/unreviewed/openBlockers, safetyReadinessSnapshotAtLock with pass/fail/na/openRemediations/resolvedRemediations, permitPostingSnapshotAtLock per-item array of 12 Section 4 items, contractObligationsSnapshotAtLock with total/flagged/open/satisfied, responsibilitySnapshotAtLock with pmSheetAssigned/fieldSheetAssigned/unassignedCategories/primaryAssignments; executionBaselineFieldsAtLock full field-value map; pmPlanStatusAtLock; certificationSummaryAtLock for all 6 surfaces; approvedWaiversAtLock and lapsedWaiversAtLock arrays; openProgramBlockersAtLock; peFlagsAtLock; authorizingPEUserId and authorizationTimestamp); 10 nested snapshot type interfaces; 5-step atomic lock transaction per T01 §7.5 (create snapshot → set Tier 2 read-only → advance state to BASELINE_LOCKED → emit StartupBaselineLocked → clear Work Queue); Closeout read API contract per T02 §7.3 (GET /api/startup/{projectId}/baseline → 200 authorized/403 unauthorized/404 not locked; PATCH/PUT/DELETE → 405 BASELINE_IMMUTABLE); triple immutability (snapshot HTTP 405 always, all Tier 2 records read-only, governed fields locked); `startup-baseline-feeds-autopsy` Related Items relationship; 9-entry Closeout delta analysis map per T06 §9.2 (schedule/cost/risk/savings/safety/assumptions/success/team/lessons); 1 Stage 8 Activity Spine event (`StartupBaselineLocked`); business rules (`isBaselineSnapshotComplete` — 22-field null check, `isBaselineMutationAllowed` — always false, `isBaselineReadAuthorized` — PX/CloseoutService, `canCreateBaselineSnapshot` — BASELINE_LOCKED only, `getBaselineAPIResponse` — method→status code); label maps; testing fixtures (`createMockStartupBaseline` with full 22-field populated snapshot including 12 permit items and 6 certification summaries); contract stability tests (25 tests), business rule tests (29 tests). **All 8 T10 implementation stages complete. Startup module Stage 7.9 fully implemented: 630 tests across 16 test files, 8 subdirectories, covering all 28 record families across 4 tiers.**
**P3-E11 Acceptance Sweep:** **Completed 2026-03-24 in `@hbc/features-project-hub` v0.1.72.** Final acceptance audit: all 31 T10 §5 acceptance criteria verified against implementation with zero gaps. T10 §11 added with full acceptance verification matrix mapping each criterion to implementing stage, function/constant, test coverage, and contract-complete status. P3-E11 master index status updated with implementation completion. Criteria 1-6 (state machine) verified in Stage 1 foundation; criteria 7-9 (task library) in Stage 2; criteria 10-12 (safety readiness) in Stage 3; criteria 13-14 (permit posting) in Stage 4; criteria 15-16 (contract obligations) in Stage 5; criteria 17-18 (responsibility matrix) in Stage 6; criteria 19-21 (execution baseline) in Stage 7; criteria 22-23 (closeout continuity) in Stage 8; criteria 24-27 (spine/integration) across all stages; criteria 28-31 (canvas/UI) in Stages 1+8 and design contracts. **P3-E11 Project Startup Module — implementation closed.**

**7.10 — Subcontract Execution Readiness module**
Implement the module as a **governed `SubcontractReadinessCase` model**, not as a flat checklist. Each active `project + subcontractor legal entity + governed award / buyout intent` combination gets one active case bound to governed requirement profiles that generate requirement items from award-path facts rather than from a universal fixed row set. Every generated item must carry both artifact state and compliance-evaluation state, and the governing execution gate is a distinct `ExecutionReadinessDecision` issued by Compliance / Risk rather than generic checklist completion. SDI / prequalification must be modeled as a governed requirement family with multiple valid outcomes, not a binary Compass-upload rule. Where a requirement cannot be satisfied through a standard or governed alternate outcome, the module must route the case through immutable `ExceptionSubmissionIteration` submissions with governed approval slots, controlled reassignment that preserves slot identity and audit history, and optional precedent / reference publication for `GlobalPrecedentReference` outcomes without any automatic reuse. Routine evaluation and readiness issuance remain Compliance / Risk-owned; PX is limited to flagged business-risk and governed exception approvals. Material award-path changes must supersede or void the current case and create a successor case rather than mutating the active case in place. Implement timer-driven reminders, overdue handling, and escalation through shared work-intelligence primitives and publish governed Activity, Health, Work Queue, and Related Items outputs per P3-A3. Buyout gate integration: `IBuyoutLineItem.status` must not transition to `ContractExecuted` unless the linked readiness gate projection reports `READY` from an issued readiness decision; the Financial module enforces the gate and the readiness module surfaces the source-of-truth state only.
Governing: P3-E1 §3.11, P3-E2 §16, P3-E3 §8.5, P3-E4 §6.1–§6.2a, **P3-E13-T01 through P3-E13-T08**
**T08 Stage 1 — Module Foundation:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.73.** Complete operating model foundation contracts per T01 and P3-E13 master index: `ReadinessSurfaceCode` (6 surfaces per T01 §4.1 — CASE_REGISTRY/CASE_DETAIL_WORKSPACE/REQUIREMENT_EVALUATION_WORKBENCH/EXCEPTION_PACKET_WORKSPACE/READINESS_DECISION_SURFACE/REVIEW_OVERLAY_SURFACE), `ReadinessOperatingLayer` (3 layers per T01 §4.2 — PRIMARY_OPERATIONAL/EXCEPTION_GOVERNANCE/GATE_AND_PUBLICATION), `ReadinessRecordClass` (3 classes per master index — CLASS_1_PRIMARY_OPERATIONAL with 4 records, CLASS_2_EXCEPTION_GOVERNANCE with 5 records, CLASS_3_GATE_AND_DOWNSTREAM with 3 records), `AdjacentModuleCode` (6 adjacent modules per T01 §5 — FINANCIAL/STARTUP/REPORTS/HEALTH/WORK_QUEUE/FUTURE_VENDOR_MASTER), `ReadinessAuthorityRule` (4 rules per T01 §8 — CANONICAL_SOURCE/SPECIALIST_GOVERNED/CONSUMES_READINESS_OUTPUT/SEPARATE_REVIEW_LAYER), `ReadinessOwnerRole` (7 roles per T01 §6 — PM/APM/PA/COMPLIANCE_RISK/PX/CFO/COMPLIANCE_MANAGER), `CaseActivationTrigger` (4 triggers per T01 §7), `CrossContractRole` (4 roles per T01 §9), `ModuleIdentityExclusion` (6 exclusions per T01 §2), `ReadinessBusinessConcern` (5 concerns per T01 §3); 8 interface contracts (`IReadinessSurface`, `IReadinessOperatingLayerDef`, `IReadinessRecordClassDef`, `IModuleBoundaryDeclaration`, `ISourceOfTruthBoundary`, `IOperatingOwnership`, `ICaseActivationRule`, `ICaseIdentityConstraint`, `ICrossContractPosition`, `ILockedArchitectureDecision`, `IModuleIdentityExclusionDef`, `IBusinessConcernDef`, `IReadinessCrossContractRef`); governed constant maps: 6 surface definitions, 3 operating layer definitions, 3 record class definitions (12 total records across 3 classes), 6 module boundary declarations per T01 §5.1–§5.6, 7 SoT boundary rows per T01 §8, 5 operating ownership rows per T01 §6, 4 case activation rules per T01 §7, 6 module identity exclusion definitions per T01 §2, 5 business concern definitions per T01 §3, 4 cross-contract positions per T01 §9, 13 locked architecture decisions per P3-E13 master index, 10 cross-contract references per master index; 7 label maps (surfaces, layers, adjacent modules, roles, record classes, identity exclusions, business concerns); business rules (`isActiveCaseIdentityMatch` — T01 §7 one-active-case identity, `isValidActivationTrigger` — T01 §7, `getModuleBoundary` — T01 §5, `isFinancialBoundaryRespected` — T01 §5.1 gate-only read rule, `getAuthorityForConcern` — T01 §8 SoT lookup, `isModuleIdentityViolation` — T01 §2 not-a-checklist guard, `canReadinessMutateCrossModuleData` — always false); contract stability tests (39 tests), business rule tests (33 tests). Subcontract Execution Readiness module Stage 7.10 T08 Stage 1 foundation complete.
**T08 Stage 2 — Record Families and Identity:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.74.** Complete record family, identity, lifecycle, and readiness decision contracts per T02: `ReadinessWorkflowStatus` (11 statuses per T02 §4.1 — DRAFT/ASSEMBLING/SUBMITTED_FOR_REVIEW/UNDER_EVALUATION/AWAITING_RESPONSE/AWAITING_EXCEPTION/READY_FOR_ISSUANCE/ISSUED/RENEWAL_DUE/SUPERSEDED/VOID), `ExecutionReadinessOutcome` (6 outcomes per T02 §4.2 — NOT_ISSUED/READY/BLOCKED/READY_WITH_APPROVED_EXCEPTION/SUPERSEDED/VOID), `ReadinessRecordFamily` (15 families per T02 §1 — 10 primary ledger + 1 publication artifact + 4 downstream projection), `ReadinessLedgerType` (3 types), `SupersedeVoidTrigger` (4 triggers per T02 §3.3), `InCaseContinuityReason` (5 reasons per T02 §3.2), `OutcomeReasonCode` (5 reason codes per T02 §5.1); 9 record interfaces (`ISubcontractReadinessCase` 22 fields per T02 §2 — 9 identity/lineage + 9 operational header + 4 audit, `IExecutionReadinessDecision` 10 fields per T02 §5, `IReadinessRequirementItem` 9 fields per T02 §6, `IRequirementArtifact` 8 fields per T02 §6, `IRequirementEvaluation` 8 fields per T02 §6, `IExceptionCase` 5 fields per T02 §7, `IReadinessRecordFamilyDef`, `ISupersedeVoidTriggerDef`, `IInCaseContinuityDef`); governed constant maps: 15 record family definitions with ledger type classification, 4 supersede/void trigger definitions, 5 in-case continuity definitions, 4 immutable case identity fields, 9 active + 2 terminal workflow statuses, 10 primary ledger + 4 downstream projection families; 5 label maps (workflow statuses, outcomes, record families, supersede triggers, reason codes); business rules (`isOneActiveCaseViolated` — T02 §3.1 checks existing active cases against new identity, `isInCaseContinuityEvent` — T02 §3.2, `isSupersedeVoidTrigger` / `shouldSupersedeOrVoid` — T02 §3.3, `isTerminalWorkflowStatus` / `isActiveWorkflowStatus` — T02 §4.1, `isWorkflowOutcomeConsistent` — T02 §4.3 workflow/outcome independence with pre-review and terminal guards, `canFinancialConsumeDecision` — T02 §5.2 formal decision required, `canFinancialReadRawItems` — always false per T02 §5.2, `isRecordFamilyPrimaryLedger` / `isDownstreamProjection` / `getRecordFamilyLedgerType` — T02 §1/§8 classification); contract stability tests (28 tests), business rule tests (52 tests). Subcontract Execution Readiness module Stage 7.10 T08 Stage 2 record families complete.
**T08 Stage 3 — Requirement Profiles and Dual-State Evaluation:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.75.** Complete governed requirement profile, dual-state item, and SDI prequalification contracts per T03: `ArtifactState` (8 states per T03 §4.1 — NOT_PROVIDED/REQUESTED/RECEIVED_PENDING_REVIEW/RECEIVED_ACCEPTED/RECEIVED_DEFICIENT/REPLACED/EXTERNAL_REFERENCE_ONLY/NOT_REQUIRED_BY_RULE), `ComplianceEvaluationState` (8 states per T03 §5.1 — NOT_STARTED/UNDER_REVIEW/SATISFIED/SATISFIED_WITH_CONDITIONS/NOT_REQUIRED_BY_RULE/DEFICIENT/EXCEPTION_REQUIRED/REJECTED), `SDIPrequalificationOutcome` (5 outcomes per T03 §7.1 — Qualified/NotRequiredByRule/AlternateRiskTreatmentApproved/ExceptionRequired/Rejected), `BlockingSeverity` (4 severities per T03 §3.1), `RenewalStatus` (5 statuses per T03 §3.1), `MetadataFieldType` (6 types per T03 §3.2), `ProfileInputDimension` (9 minimum dimensions per T03 §1.2); 10 interface contracts (`IRequirementProfile`, `IProfileRequirementItem` with governed key/family/severity/evaluator/metadata/renewal, `IMetadataFieldSpec`, `IReadinessRequirementItemT03` 16 fields with dual-state artifactState + complianceEvaluationState, `IRequirementArtifactT03` 14 fields with receipt provenance and replacement lineage, `IRequirementEvaluationT03` 12 fields with specialist ruling detail and PM applicability-review audit, `ISDIPrequalificationRecord` 9 fields, `IProfileInputDimensionDef`, `IDualStateExample`); governed constant maps: 9 profile input dimension definitions, 4 dual-state independence examples per T03 §5.2, 4 PM-prohibited override actions per T03 §6, 2 SDI blocking + 3 SDI satisfied outcomes; 6 label maps (artifact states, evaluation states, SDI outcomes, blocking severities, renewal statuses, profile dimensions); business rules (`isDualStateConsistent` — T03 §5.2 artifact/evaluation independence, `canPMSuppressRequirement` — always false per T03 §6, `canSpecialistDeclareNotRequired` — T03 §6 specialist-only, `isItemBlocking` / `isItemConditional` — T03 §3.1, `isRenewalDue` / `isRenewalActionRequired` — T03 renewal, `isSDIOutcomeBlocking` / `isSDIOutcomeSatisfied` — T03 §7.1, `isProfileBindingComplete` / `getMissingProfileInputs` — T03 §1.2, `isEvaluationBlocking` / `isEvaluationSatisfied` — evaluation state classification); contract stability tests (22 tests), business rule tests (53 tests). Subcontract Execution Readiness module Stage 7.10 T08 Stage 3 requirement profiles complete.
**T08 Stage 4 — Exception Governance and Delegation:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.76.** Complete exception governance contracts per T04: `ExceptionIterationStatus` (5 statuses per T04 §2 — DRAFT/SUBMITTED/UNDER_REVIEW/REJECTED/WITHDRAWN), `ApprovalSlotStatus` (5 statuses per T04 §3.1 — PENDING/APPROVED/REJECTED/RETURNED/DELEGATED), `ApprovalActionOutcome` (4 outcomes per T04 §3.2 — APPROVED/REJECTED/RETURNED/DEFERRED), `ApprovalSlotRole` (4 roles per T04 §5.2 — PX/CFO/COMPLIANCE_MANAGER/SPECIALIST), `ApprovalSequencingMode` (2 modes per T04 §5.1 — PARALLEL/ORDERED), `PrecedentPublicationStatus` (3 statuses per T04 §6), `DelegationReason` (4 reasons per T04 §4.1); 11 interface contracts (`IExceptionCaseT04` 10 fields per T04 §1, `IExceptionSubmissionIteration` 8 fields per T04 §2.2 immutable snapshots, `IExceptionApprovalSlot` 7 fields per T04 §3.1 preserved slot model, `IExceptionApprovalAction` 7 fields per T04 §3.2 tied to slot+iteration, `IExceptionDelegationEvent` 8 fields per T04 §4.1 with audit trail, `IGlobalPrecedentReference` 12 fields per T04 §6 with publication/revocation, `IApprovalSlotRoleDef`, `IPrecedentProhibition`, `IAuditPreservationRequirement`); governed constant maps: 4 required approval authorities per T04 §5.2 (3 required-by-default), 4 immutability rules per T04 §2.1, 4 delegation rules per T04 §4.2, 4 precedent prohibitions per T04 §6.2, 6 audit preservation requirements per T04 §7, 4 terminal iteration + 3 terminal slot statuses; 7 label maps; business rules (`isIterationImmutable` / `canEditIteration` — T04 §2.1 DRAFT-only edits, `isSlotRequired` / `isSlotActionComplete` / `isApprovalSequenceComplete` — T04 §3/§5, `isDelegationValid` / `doesDelegationPreserveSlot` — T04 §4.2 original slot preserved, `canPublishAsPrecedent` — T04 §6.1, `doesPrecedentAutoApprove` / `doesPrecedentAutoSatisfy` / `doesPrecedentBypassLocalEvaluation` — all false per T04 §6.2, `getSlotActionForIteration` — T04 §3.2 lookup); contract stability tests (21 tests), business rule tests (34 tests). Subcontract Execution Readiness module Stage 7.10 T08 Stage 4 exception governance complete.
**T08 Stage 5 — Workflow, SLA, and Publication:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.77.** Complete workflow, timer, routed output, publication, and shared package contracts per T05: `ReadinessWorkflowStage` (9 stages per T05 §1), `ReadinessTimerType` (5 types per T05 §2.1 — MISSING_PACKAGE_ITEM/PENDING_EVALUATOR_REVIEW/PENDING_EXCEPTION_APPROVAL/UPCOMING_EXPIRATION_OR_RENEWAL/STALE_DECISION_NEAR_EXECUTION), `ReadinessWorkItemType` (7 per T05 §3.1), `ReadinessNotificationType` (6 per T05 §3.2), `ReadinessEscalationType` (3 per T05 §3.3), `ReadinessActivityEventType` (10 events per T05 §4.1), `ReadinessWorkQueueType` (5 per T05 §4.2), `ReadinessHealthMetricType` (5 per T05 §4.3), `ReadinessRelatedItemType` (3 per T05 §4.4), `RequiredSharedPackage` (10 per T05 §5), `ProhibitedLocalSubstitute` (6 per T05 §5.1); 10 interface contracts; governed constant maps: 5 timer definitions with anchors/outputs, 7 work items, 6 notifications, 3 escalations with targets, 10 activity events, 5 work queue projections, 5 health metrics, 3 related items, 10 shared package requirements, 6 prohibited local substitutes; 4 label maps; business rules (`isTimerAnchoredToSubmission` / `getTimerAnchor` — T05 §2.1, `isRoutedWorkItem` / `isEscalationPolicyDriven` / `isPublicationDownstreamOnly` — always true guards, `isSharedPackageRequired` — T05 §5, `isLocalSubstituteProhibited` — T05 §5.1, `canUseLocalReminderTable` / `canUseLocalNotificationSystem` / `canBuildBespokeApprovalRouting` — always false per T05 §5.1); contract stability tests (25 tests), business rule tests (19 tests). Subcontract Execution Readiness module Stage 7.10 T08 Stage 5 workflow publication complete.
**T08 Stage 6 — Lanes, Permissions, and UX Surfaces:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.78.** Complete authority model, action distinction, annotation surface, lane depth, and visibility contracts per T06: `ReadinessAuthorityRoleT06` (7 roles per T06 §1.1 — PM_APM_PA/COMPLIANCE_RISK/PX/CFO/COMPLIANCE_MANAGER/PER_REVIEW_ONLY/READ_ONLY_CONSUMER), `DistinctActionType` (4 types per T06 §2 — ANNOTATION/APPROVAL/READINESS_ISSUANCE/GATE_ENFORCEMENT), `AnnotationTarget` (6 targets per T06 §3), `ApplicationLane` (2 per T06 §4), `PWADepthCapability` (7 per T06 §4.1), `SPFxDepthCapability` (5 per T06 §4.2), `UXSurfaceExpectation` (8 per T06 §5), `VisibilityTier` (3 per T06 §6); 7 interface contracts; governed constant maps: 7 role behaviors, 4 distinct actions, 6 annotation targets, 4 annotation invariants, 7 PWA capabilities, 5 SPFx capabilities, 8 UX expectations, 3 visibility tiers; 3 label maps; business rules (`isAnnotationNonMutating` / `isAnnotationStoredInFieldAnnotations` — always true per T06 §3, `doesAnnotationFlipFinancialStatus` / `doesApprovalFlipFinancialStatus` — always false per T06 §2, `canRoleMutateReadinessState` — COMPLIANCE_RISK only per T06 §1.2, `isReviewOnlyRole` / `canRoleAuthorOperationalContent` / `isExceptionApprovalRole` — T06 §1.1, `isPWAOnlyCapability` / `shouldSPFxLaunchToPWA` — T06 §4); contract stability tests (19 tests), business rule tests (28 tests). Subcontract Execution Readiness module Stage 7.10 T08 Stage 6 lanes permissions complete.
**T08 Stage 7 — Cross-Module Contracts and Integrations:** **Implemented 2026-03-24 in `@hbc/features-project-hub` v0.1.79.** Complete cross-module contract, Financial gate, Startup boundary, downstream consumer, related-item, activity, and future-input contracts per T07: `GateProjectionField` (7 fields per T07 §1.2), `FinancialProhibition` (4 per T07 §1.3), `StartupAllowedConsumption` (3 per T07 §2) / `StartupProhibitedConsumption` (4 per T07 §2), `DownstreamConsumer` (3 per T07 §3), `ReadinessRelatedItemPair` (4 per T07 §4.1), `CrossModuleActivityEvent` (10 per T07 §4.2), `FutureExternalInputType` (5 per T07 §5), `ExternalDisplacementProhibition` (4 per T07 §5); 8 interface contracts (`IReadinessGateProjection` 7 fields per T07 §1.2, `IFinancialProhibitionDef`, `IStartupBoundaryAllowedDef`/`IStartupBoundaryProhibitedDef`, `IDownstreamConsumerDef`, `IRelatedItemPairDef`, `ICrossModuleActivityEventDef`, `IFutureExternalInputDef`, `IExternalDisplacementProhibitionDef`); governed constant maps: 4 Financial prohibitions, 3 Startup allowed + 4 prohibited, 3 downstream consumers, 4 related-item pairs, 10 activity events, 5 future external inputs, 4 displacement prohibitions; business rules (`canFinancialProgressToContractExecuted` — READY/READY_WITH_APPROVED_EXCEPTION only per T07 §1.3, `canFinancialInferFromRawItems` / `canFinancialWriteReadinessRecords` / `canFinancialMutateExceptionOutcomes` — all false, `canStartupOwnReadinessWorkflow` / `canStartupOwnEvaluationLedgers` — all false per T07 §2, `isDownstreamProjectionOnly` — always true, `isValidRelatedItemPair` — T07 §4.1, `isExternalInputContributorOnly` — always true, `canExternalSystemDisplaceReadinessCase` / `canExternalSystemDisplaceEvaluation` / `canExternalSystemDisplaceDecision` — all false per T07 §5); contract stability tests (17 tests), business rule tests (20 tests). Subcontract Execution Readiness module Stage 7.10 T08 Stage 7 cross-module contracts complete.
**P3-E13 Acceptance Sweep:** **Completed 2026-03-24 in `@hbc/features-project-hub` v0.1.80.** Final acceptance audit: all 16 T08 §4 acceptance criteria verified against implementation with zero gaps. Criteria 1–4 (parent case, one-active-case, in-case continuity, supersede/void) verified in Stages 1–2 (foundation + record-families); criteria 5–8 (governed profiles, dual-state, typed metadata, SDI outcome family) verified in Stage 3 (requirement-profiles); criteria 9–12 (exception case, immutable iterations, slot-tied actions, controlled delegation) verified in Stage 4 (exception-governance); criteria 13–14 (formal decision, Financial gate) verified across Stages 2 + 7 (record-families + cross-module-contracts); criteria 15 (downstream projections only) verified across Stages 2 + 5 + 7; criterion 16 (annotation isolation) verified in Stage 6 (lanes-permissions). T08 §7 cross-reference update checklist verified: `phase-3-deliverables/README.md`, `MASTER/README.md`, `04_Phase-3_...Plan.md`, `P3-E12` supersession note, `P3-E1`, `P3-E2`, `P3-G1` all point to P3-E13 as active authority. P3-E13 master index status updated with implementation completion. **P3-E13 Subcontract Execution Readiness Module — implementation closed.** 7 implementation stages, 7 subdirectories, 14 test files, 410 tests across `src/subcontract-readiness/` (foundation 72, record-families 80, requirement-profiles 75, exception-governance 55, workflow-publication 44, lanes-permissions 47, cross-module-contracts 37).

---

### Stage 8 — Executive Review Layer

Must follow Stage 2 (PER auth model), Stage 3 (work queue spine), Stage 7 (review-capable module surfaces present). Requires Stage 0.1 (`@hbc/field-annotations` gap) to be resolved before beginning.

**8.1 — `@hbc/field-annotations` integration on review-capable surfaces**
Integrate `@hbc/field-annotations` on Financial, Schedule, Constraints, Permits, Project Closeout, Project Startup, and Subcontract Execution Readiness module surfaces. Annotation affordances must target section-level and block-level anchors. PER role must have annotation write access; all other roles must be read-only or have no annotation access (depending on surface policy). Safety must have no annotation affordance — enforce this at both the UI and the auth layer.
Governing: P3-E1 §9, P3-E2 §3.4, §4.4, §5.4, §6.4, §7.4, §14.4, §15.4, §16.4

**8.2 — Annotation isolation enforcement**
Ensure all PER review annotations are stored in the `@hbc/field-annotations` annotation layer, completely separate from the module source-of-truth records. No annotation write path may mutate a PM-owned module record. Implement a test that proves annotation creation produces zero writes to module domain tables.
Governing: P3-E2 §11.2

**8.3 — Reviewer-generated review run**
Implement the `runType: 'reviewer-generated'` report run path. A reviewer-generated run must use only the latest already-confirmed PM-owned report snapshot as its data source. The PM's current draft state must not be read, modified, or touched during a reviewer-generated run. Record `runType` in the run-ledger entry.
Governing: P3-F1 §8.6

**8.4 — Push-to-Project-Team structured work item**
Implement the Push-to-Project-Team action available to PER users on review-capable surfaces. The push creates a structured work item in `@hbc/my-work-feed` with full provenance (originRole: 'portfolio-executive-reviewer', originReviewRunId, originAnnotationId, pushTimestamp). The PM receives the item in their work queue. Implement the closure loop: PM marks responded; PER receives closure notification; PER confirms closure; closure confirmation is stored.
Governing: P3-D3 §5, P3-F1 §8.5

**8.5 — Executive review lane depth enforcement**
Implement lane-depth enforcement for PER posture: PWA provides the full executive review experience (all annotation operations, reviewer-generated runs, push, closure, thread management, multi-run comparison, full history browsing). SPFx provides broad direct interaction (annotation on supported surfaces, single review run, push initiation, closure confirmation) and escalates to PWA for depth operations (thread management, multi-run comparison, full history). Implement the SPFx escalation trigger points for these three cases.
Governing: P3-G1 §4.8, P3-G2 §8.8

---

### Stage 9 — Reporting System

Must follow Stage 2 (authority model), Stage 7 (module data available for assembly), Stage 8 (reviewer-generated run model in place).
Governing: **P3-E9** T-file family (T01–T10) — governed template library architecture, project registration model, draft/snapshot/narrative layer, run ledger, approval/release governance, PER review boundaries, spine publication, E10 integration for sub-scorecard and lessons-learned artifact ingestion

**9.1 — Central project-governance policy record**
Implement the project-governance policy record data model. The record owns: global floor policy (set by Manager of OpEx), project-level overrides (set by PE), per-report-family approval/release policy, PM↔PE internal review chain configuration, and `perReleaseAuthority` per family. The Reports module reads and enforces this record but does not own or write it. The PE sets project-level configuration through a project settings surface, not through the Reports module.
Governing: P3-F1 §14

**9.2 — Report family definitions — PX Review**
Implement the PX Review report family: PM draft lifecycle, PM narrative overrides with provenance, confirmation and generation pipeline, PM↔PE internal review chain enforcement (if configured), PX approval gate, and release. PWA provides the full lifecycle; SPFx provides generate and approve.
Governing: P3-F1 §8.1, §14.5

**9.3 — Report family definitions — Owner Report**
Implement the Owner Report report family: PM draft lifecycle, PM narrative overrides with provenance, non-gated release. PWA provides the full lifecycle; SPFx provides generate and release.
Governing: P3-F1 §8.2

**9.4 — Report draft refresh and staleness detection**
Implement the staleness detection model. When a draft's source data age exceeds the staleness threshold, display a staleness warning cue. Gate export on stale drafts until the user acknowledges or refreshes. Implement the refresh pipeline (pull latest module data, update draft, reset staleness timestamp). Full draft handling in PWA; refresh supported in SPFx.
Governing: P3-F1 §4, §5

**9.5 — Asynchronous generation pipeline**
Implement the queued generation pipeline. Report generation is asynchronous. The UI shows queue state and notifies on completion. The pipeline must handle concurrent generation requests without race conditions on the run ledger.
Governing: P3-F1 §6

**9.6 — Run-ledger tracking**
Implement the run-ledger entry model: runId, runType (`standard` | `reviewer-generated`), generatedAt, generatedBy, snapshotRef, status, and release artifacts. PWA provides full run-ledger browsing; SPFx escalates to PWA for run-ledger history.
Governing: P3-F1 §7

**9.7 — PER report permissions enforcement**
Implement the PER report permission layer: view any report in their scope, annotate report surfaces via `@hbc/field-annotations`, generate reviewer-generated review runs. Enforce prohibitions: no PM draft edits, no source-of-truth writes, no approval gate authority, no standard run generation (only reviewer-generated). Per-family `perReleaseAuthority` must gate PER release participation.
Governing: P3-F1 §8.5

**9.8 — Report export (PDF → SharePoint)**
Implement PDF artifact generation and storage in the project's SharePoint site. Export is available in both lanes. Produced artifacts are stored with the run-ledger entry reference.
Governing: P3-F1 §9

---

### Stage 10 — SPFx Lane Integration

Can begin after Stage 4 (cross-lane routing) and proceeds in parallel with Stages 7–9 for SPFx-tier capabilities. Full SPFx validation requires all module surfaces to be present.

**10.1 — SPFx web part initialization**
Implement `siteUrl`-based project identity resolution for each SPFx web part. Each web part must resolve `projectId` from the registry before rendering. No web part may render project content without a confirmed `projectId`.
Governing: P3-B1 §3, P3-G1 §2

**10.2 — SPFx lane capability per module**
Implement SPFx-tier capabilities for each module per the P3-G1 lane capability matrix. Where a capability is classified as "Launch-to-PWA" in SPFx, implement the escalation affordance and deep-link construction — do not silently skip the feature.
Governing: P3-G1 §4

**10.3 — SPFx-to-PWA escalation affordances**
Implement all Launch-to-PWA escalation entry points defined in P3-G2. Each escalation must construct a deep link with `projectId`, module, action, and optional `reviewArtifactId` context. The 13 escalation scenarios in P3-G2 §3 are the complete set for Phase 3.
Governing: P3-G2 §3, §8

**10.4 — SPFx personalization persistence**
Implement the SPFx storage tier for canvas personalization: localStorage for immediate state, SharePoint list or user profile extension for server-side persistence.
Governing: P3-C3 §6

---

### Stage 11 — UI Conformance Review

Must run after all surfaces are substantively implemented (Stages 6–10 complete for the surface under review). UI conformance review is a pre-acceptance gate — no surface may enter §18 acceptance validation without conformance evidence. Use the `hb-ui-ux-conformance-reviewer` agent for all evaluation tasks in this stage.

Governing: P3-C1 §14; UI-Kit-Mold-Breaker-Principles.md; UI-Kit-Usage-and-Composition-Guide.md; UI-Kit-Adaptive-Data-Surface-Patterns.md; UI-Kit-Field-Readability-Standards.md; P2-F1

**11.1 — Apply `WorkspacePageShell` to all Project Hub page surfaces**
Verify that every Project Hub page (canvas home, each module landing page, each module detail view, reports workspace, executive review surfaces, and spine full-page views) uses `WorkspacePageShell` as the page wrapper. Use the correct layout type per the shell's `layout` prop: `dashboard` for KPI+chart+data zones, `list` for filterable module lists, `detail` for single-item views.
Governing: P3-C1 §14.2; UI-Kit-Wave1-Page-Patterns.md

**11.2 — Data surface type selection per T06**
For each module list/table surface, document the T06 surface type selected and verify the implementation matches the selection. Financial budget lines and scorecard tables → dense analysis table. Constraints log, Permits log, Schedule milestones → responsive hybrid. Work Queue, activity feed → card/list view. Project health / KPI strips → summary strip + `DashboardLayout` + `HbcKpiCard`.
Governing: P3-C1 §14.2; UI-Kit-Adaptive-Data-Surface-Patterns.md

**11.3 — Token compliance verification**
Run the `enforce-hbc-tokens` ESLint rule across all Phase 3 feature packages. Zero violations must be present before any surface enters acceptance. All color, spacing, radius, elevation, and typography values must use `HBC_*` tokens. All Fluent UI primitives must be imported through `@hbc/ui-kit` (D-10).
Governing: P3-C1 §14.2; MB-08

**11.4 — Density and touch target compliance**
Verify all Project Hub surfaces in all three density tiers: compact (desktop), comfortable (tablet), and touch (field). Specifically verify Safety module and any field-primary surface in touch density. Confirm `HBC_DENSITY_TOKENS[tier].touchTargetMin` is met on all interactive elements per tier (44px touch / 36px comfortable / 24px compact).
Governing: P3-C1 §14.3 MB-07; UI-Kit-Field-Readability-Standards.md

**11.5 — Horizontal scroll prohibition verification**
Verify that no module data table produces horizontal scroll as the default at ≥1024px viewport. Verify adaptive column hiding is active on Financial, Schedule, Constraints, and Permits data tables. Verify card fallback renders at <640px on all responsive-hybrid surfaces.
Governing: P3-C1 §14.3 MB-04; UI-Kit-Adaptive-Data-Surface-Patterns.md

**11.6 — Card weight and hierarchy review**
Review all canvas tiles and module KPI/summary card compositions. Verify card weight differentiation is applied (primary/standard/supporting) — no equal-weight card grids. Verify Three-Second Read Standard: on every primary surface, the most important item (health status, next action, critical constraint) is identifiable within 3 seconds without scanning.
Governing: P3-C1 §14.2; UI-Kit-Visual-Hierarchy-and-Depth-Standards.md

**11.7 — Empty state verification**
Verify every data-dependent zone has an `HbcSmartEmptyState` or `HbcEmptyState` configured with actionable next-step guidance. No zone may render a blank area or a null state without guidance. Verify empty state text is context-specific ("No constraints yet. Typical next step: log the first delay impact.").
Governing: P3-C1 §14.2 MB-01; UI-Kit-Usage-and-Composition-Guide.md

**11.8 — Phase 2 UI precedent compliance**
Verify: (a) all Project Hub KPI card surfaces use `DashboardLayout` + `HbcKpiCard`; (b) module detail views with primary content + context panel use the two-column persistent layout established in Phase 2; (c) all primary action buttons use context-sensitive CTA labels (no generic "Submit"/"OK"); (d) project color coding from Phase 2 is preserved in the project context header.
Governing: P3-C1 §14.4; P2-F1

**11.9 — `hb-ui-ux-conformance-reviewer` final sign-off**
Run the `hb-ui-ux-conformance-reviewer` specialist across all Phase 3 surfaces for a final cross-surface UX consistency review. Verify: `@hbc/ui-kit` alignment, no feature-local duplicate primitives, cross-surface UX consistency between Project Hub and Personal Work Hub (Phase 2), mold-breaker MB-01–MB-08 alignment. Record conformance evidence in P3-H1 §13 (Evidence Collection Log). This sign-off is required before any surface can pass its §18 acceptance gate.
Governing: P3-C1 §14.5; P3-H1 §6.7.12

---

### Stage 12 — Acceptance and Validation

Must follow all implementation stages and Stage 11 (UI Conformance Review). Execute in order — staging scenarios build on one another.

**12.1 — Execute all 10 staging scenarios (P3-H1 §9)**
Run each staging scenario defined in P3-H1 §9 in the order listed. Collect evidence artifacts for each passing scenario. The scenarios in dependency order are:
1. §9.1 Activation flow
2. §9.2 Project switching
3. §9.3 Stale draft handling
4. §9.4 Cross-lane launch SPFx→PWA
5. §9.5 Cross-lane launch PWA→SPFx
6. §9.6 Module spine publication
7. §9.7 Canvas governance enforcement
8. §9.8 Report lifecycle (PX Review and Owner Report)
9. §9.9 Push-to-Project-Team
10. §9.10 Executive review loop

**12.2 — §18.1–§18.7 gate validation**
Validate each checklist item in P3-H1 §2–§8 including the §6.7 UI Conformance criteria. Record pass/fail status and evidence artifact reference for every item. All 95 items must pass before Phase 3 can advance to release.
Governing: P3-H1 §2–§8

**12.3 — Cross-lane evidence matrix completion**
Complete the evidence collection matrix in P3-G3 §10: shared evidence, PWA-specific evidence, and SPFx escalation count (13 scenarios). The matrix must be filled with concrete artifact references.
Governing: P3-G3 §10

**12.4 — Release-readiness criteria check**
Verify all 10 release-readiness criteria in P3-H1 §10, including the central project-governance policy record enforcement (10.9) and UI conformance evidence (10.10). Confirm the defer list in P3-H1 §11 is reviewed and no deferred items have leaked into Phase 3 acceptance.
Governing: P3-H1 §10–§11

**12.5 — Documentation alignment review**
Verify that all 19 deliverables (P3-A1 through P3-H1) reflect the implemented state. Update any deliverable that drifted during implementation. Update the parent Phase 3 Plan (§18–§20) to reflect final implementation decisions.
Governing: P3-H1 §10.5, P3-H1 §12

---

### Stage 13 — Documents Enabling Seams

May begin after Stage 4 (PWA routing foundations in place for project-scoped launch contracts) and Stage 5 (`@hbc/related-items` and `@hbc/publish-workflow` available for E4 and E6 seams). Workstreams E1, E2, and E5 (Sequence A) may start in parallel immediately; E3, E4, E7 (Sequence B) must follow Sequence A; E6 and E8 (Sequence C) may start after Sequence B. Must complete before Phase 5 document implementation begins.

Governing: P3-J1; `docs/architecture/plans/MASTER/04_Phase-3_Unified-Documents-Enabling-Plan.md`

**13.1 — Route and launch contracts (E1)**
Define the complete route contract catalog for project-scoped document access: project-scoped document landing, project-zone landing, direct document deep link, raw library fallback, and escape hatch to the future Global Documents shell. Define the launch-state parameter schema (`projectId`, `workspaceId`, `zoneId`, `sourceRoute`, `relatedRecordContext`, `searchScopeDefault`). Define redirect and deep-link handling rules. Project-scoped launches must not use raw SharePoint library URLs as their primary contract. A future shared shell must be mountable without changing Project Hub's route assumptions.
Governing: P3-J1 §3 E1

**13.2 — Project zone model and registry schema (E2)**
Define the initial normalized project-zone schema. Map each zone to document purpose, default location target, fallback raw structure reference, and visibility/governance metadata. Define the governance field matrix distinguishing globally governed vs project-extensible zones. Define project-level supplemental zone request metadata.
Governing: P3-J1 §3 E2

**13.3 — Source, authority, and restriction vocabulary (E5)**
Define and bind the project-context trust vocabulary and visual states: governed project file, restricted item, preview available, raw library location, canonical/authoritative indicator, not-authoritative/linked copy. Bind all states to `@hbc/ui-kit` badge, list-row, preview-card, and empty-state patterns. Define plain-language explanation copy rules. This vocabulary must be reusable into Phase 5 without redefinition.
Governing: P3-J1 §3 E5

**13.4 — Project Hub contextual entry surface (E3)**
Implement or design the Project Hub document entry surface as a contextual launch/entry layer, not a standalone feature. Use shared-shell language and `@hbc/ui-kit` patterns — no feature-local UI primitive duplication. Provide CTA paths for: open project documents, view by zone, raw library fallback, related documents from records, preview when supported. Define empty-state and no-access states.
Governing: P3-J1 §3 E3

**13.5 — Related-items document reference model (E4)**
Define the document reference model for related items: linked project records, record attachments, zone associations, restricted placeholders. Bind document references into `@hbc/related-items` where appropriate. Define minimum metadata for contextual visibility (title, sourceType, authorityState, availabilityState, originatingRecord, lastRelevantActivity). Define the restricted-stub contract so document references appear safely in project context without broad permission leakage.
Governing: P3-J1 §3 E4; P3-D4

**13.6 — Preview and adaptive tablet/field contract (E7)**
Define the project-scoped preview/details surface contract. Define adaptive behavior requirements for desktop, tablet, and field mode. Bind those requirements to `@hbc/ui-kit` adaptive standards. Keep preview-provider choice abstract so Phase 5 can implement without redesigning the Project Hub surface.
Governing: P3-J1 §3 E7

**13.7 — Publish/handoff intent seams (E6)**
Define event and payload shapes for publish requested, destination suggested, handoff requested, publish completed, and publish failed. Identify where `@hbc/workflow-handoff`, `@hbc/publish-workflow`, and `@hbc/notification-intelligence` will bind in Phase 5. Add lightweight UI affordances only where they support current Project Hub clarity. Phase 5 must be able to add guided publish/handoff without changing Project Hub's domain contracts.
Governing: P3-J1 §3 E6

**13.8 — Data, telemetry, and readiness spikes (E8)**
Spike project-scoped registry resolution against real/proposed site-library patterns. Spike auth/token flow for project-context document access. Spike preview feasibility and handoff mechanics. Define telemetry events for launch, preview, raw fallback, restricted-stub exposure, and no-access states. Capture all contradictions discovered during spikes in the contradiction register. Produce a Phase 5 handoff memo summarizing findings and outstanding risks.
Governing: P3-J1 §3 E8; P3-J1 §8

---

### Key Blocker Dependencies Summary

| Dependency | Owner | Impact if missing | Gate |
|---|---|---|---|
| `@hbc/field-annotations` section/block anchor support | `@hbc/field-annotations` team | Executive review annotation layer cannot be built | Stage 0.1 |
| `@hbc/auth` AccessControlOverrideRecord | `@hbc/auth` team | Out-of-scope PER grants cannot be issued or revoked | Stage 0.2 |
| `@hbc/my-work-feed` provenance work item model | `@hbc/my-work-feed` team | Push-to-Project-Team cannot be implemented | Stage 0.3 |
| SPFx shell Launch-to-PWA escalation affordance | SPFx shell team | 13 escalation scenarios blocked | Stage 0.4 |
| `enforce-hbc-tokens` ESLint rule passing in all Phase 3 packages | UI-Kit / tooling team | UI conformance gate blocked | Stage 11.3 |

### Explicit Phase 3 Defer List

The following are explicitly deferred and must not be treated as Phase 3 scope. See P3-H1 §11 for the full list.
- Direct Procore API budget integration (CSV upload is Phase 3)
- Smart toolbox-talk topic generation
- Full CPM authoring
- Full ERP/accounting behavior
- Full claims/legal/contract-admin behavior
- Full jurisdiction-facing permitting package management
- Deeper field-first execution for QC and Warranty beyond baseline-visible lifecycle
- Full unified Documents product: global shell, My Files / Departments / Company roots, global smart views (Recent / Shared With Me / Pinned), platform-wide document search — all Phase 5 scope (see `06_Phase-5_Search-Connected-Records-and-Document-Access-Plan.md`)

---

## Related Documents

- [Phase 3 Plan — Project Hub and Project Context](../04_Phase-3_Project-Hub-and-Project-Context-Plan.md)
- [Phase 3 Unified Documents Enabling Plan](./04_Phase-3_Unified-Documents-Enabling-Plan.md) *(governing background plan for Workstream J)*
- [Phase 5 Plan — Search, Connected Records, and Document Access](../06_Phase-5_Search-Connected-Records-and-Document-Access-Plan.md) *(deferred target for full Documents implementation)*
- [Master Development Summary Plan](../00_HB-Intel_Master-Development-Summary-Plan.md)
- [Phase 2 Deliverables](../phase-2-deliverables/README.md)
- [Phase 1 Deliverables](../phase-1-deliverables/README.md)
- [Phase 0 Deliverables](../phase-0-deliverables/README.md)
- [Current State Map](../../../blueprint/current-state-map.md)
- [Package Relationship Map](../../../blueprint/package-relationship-map.md)
- [PH7 Project Hub Task Plans](../../ph7-project-hub/)

---

**Last Updated:** 2026-03-24 (v32) — P3-E14 Warranty readiness review completed. P3-E1 §3.8 updated from baseline-visible to always-on lifecycle with full P3-E14 T01–T10 field specification; classification row 16 reclassified; §4.2 updated; spine table Warranty row set to Required. P3-E2 §17 Warranty SoT section added with 10 data-concern rows, Financial/Closeout/Startup boundaries, Layer 2 seam doctrine, and annotation boundary. P3-H1 §6.7 Warranty acceptance gate added (8 criteria referencing AC-WAR-01 through AC-WAR-46); defer list items 11.8–11.10 updated. P3-G1 §4.8.4 Warranty lane depth added (PWA full-depth, SPFx read-only with Launch-to-PWA); executive review surface list updated. P3-E14 master index + T01–T10 files committed. Stages 1–2 unblocked; Stage 3 hard-blocker gate documented. Previously (v31): Stage 7.10 P3-E13 acceptance sweep completed in `@hbc/features-project-hub` v0.1.80. All 16 T08 §4 acceptance criteria verified against implementation with zero gaps. T08 §7 cross-reference update checklist verified. P3-E13 master index updated with implementation completion status. P3-E13 Subcontract Execution Readiness Module implementation closed. 7 stages, 14 test files, 410 tests. Previously (v30): Stage 7.10 P3-E13 T08 Stage 7 (Cross-Module Contracts and Integrations) implemented in `@hbc/features-project-hub` v0.1.79. Created `src/subcontract-readiness/cross-module-contracts/` with complete Financial gate, Startup boundary, downstream consumer, related-item, activity, and future-input contracts per T07: 9 enum types, 8 interface contracts including IReadinessGateProjection, governed constants (Financial prohibitions, Startup boundaries, downstream consumers, related-item pairs, activity events, external inputs, displacement prohibitions), 12 business rules, 37 tests (17 contract stability + 20 business rule). Phase 3 plan/package version references refreshed for `@hbc/features-project-hub` v0.1.79. Previously (v29): Stage 7.10 P3-E13 T08 Stage 6 (Lanes, Permissions, and UX Surfaces) implemented in `@hbc/features-project-hub` v0.1.78. Created `src/subcontract-readiness/lanes-permissions/` with complete authority model, annotation, lane depth, and visibility contracts per T06: 9 enum types, 7 interface contracts, governed constants (roles, actions, annotations, PWA/SPFx capabilities, UX expectations, visibility tiers), 3 label maps, 10 business rules, 47 tests (19 contract stability + 28 business rule). Phase 3 plan/package version references refreshed for `@hbc/features-project-hub` v0.1.78. Previously (v28): Stage 7.10 P3-E13 T08 Stage 5 (Workflow, SLA, and Publication) implemented in `@hbc/features-project-hub` v0.1.77. Created `src/subcontract-readiness/workflow-publication/` with complete workflow stage, timer, routed output, publication, and shared package contracts per T05: 11 enum types, 10 interface contracts, governed constants (timers, work items, notifications, escalations, activity events, work queue, health, related items, shared packages, prohibited substitutes), 4 label maps, 9 business rules, 44 tests (25 contract stability + 19 business rule). Phase 3 plan/package version references refreshed for `@hbc/features-project-hub` v0.1.77. Previously (v27): Stage 7.10 P3-E13 T08 Stage 4 (Exception Governance and Delegation) implemented in `@hbc/features-project-hub` v0.1.76. Created `src/subcontract-readiness/exception-governance/` with complete exception packet, approval slot, delegation, and global precedent contracts per T04: 7 enum types, 11 interface contracts, governed constants (required authorities, immutability/delegation rules, precedent prohibitions, audit requirements), 7 label maps, 12 business rules, 55 tests (21 contract stability + 34 business rule). Phase 3 plan/package version references refreshed for `@hbc/features-project-hub` v0.1.76. Previously (v26): Stage 7.10 P3-E13 T08 Stage 3 (Requirement Profiles and Dual-State Evaluation) implemented in `@hbc/features-project-hub` v0.1.75. Created `src/subcontract-readiness/requirement-profiles/` with complete governed profile, dual-state item, and SDI prequalification contracts per T03: 7 enum types (ArtifactState 8, ComplianceEvaluationState 8, SDIPrequalificationOutcome 5, BlockingSeverity 4, RenewalStatus 5, MetadataFieldType 6, ProfileInputDimension 9), 10 interface contracts, governed constants (profile dimensions, dual-state examples, PM-prohibited actions, SDI classification), 6 label maps, 12 business rules, 75 tests (22 contract stability + 53 business rule). Phase 3 plan/package version references refreshed for `@hbc/features-project-hub` v0.1.75. Previously (v25): Stage 7.10 P3-E13 T08 Stage 2 (Record Families and Identity) implemented in `@hbc/features-project-hub` v0.1.74. Created `src/subcontract-readiness/record-families/` with complete record family, identity, lifecycle, and readiness decision contracts per T02: 7 enum types, 9 record interfaces (ISubcontractReadinessCase 22 fields, IExecutionReadinessDecision 10 fields, IReadinessRequirementItem/IRequirementArtifact/IRequirementEvaluation/IExceptionCase scaffolds), 15 record family definitions with ledger classification, 4 supersede/void triggers, 5 in-case continuity rules, 11 business rules including workflow/outcome independence validation, 80 tests (28 contract stability + 52 business rule). Phase 3 plan/package version references refreshed for `@hbc/features-project-hub` v0.1.74. Previously (v24): Stage 7.10 P3-E13 T08 Stage 1 (Module Foundation) implemented in `@hbc/features-project-hub` v0.1.73. Created `src/subcontract-readiness/foundation/` with complete operating model foundation contracts per T01 and P3-E13 master index: 10 enum types (ReadinessSurfaceCode, ReadinessOperatingLayer, ReadinessRecordClass, AdjacentModuleCode, ReadinessAuthorityRule, ReadinessOwnerRole, CaseActivationTrigger, CrossContractRole, ModuleIdentityExclusion, ReadinessBusinessConcern), 13 interface contracts, governed constant maps (6 surfaces, 3 layers, 3 record classes with 12 records, 6 boundary declarations, 7 SoT boundaries, 5 ownership rows, 4 activation rules, 6 identity exclusions, 5 business concerns, 4 cross-contract positions, 13 locked decisions, 10 cross-references), 7 label maps, 7 business rules, 72 tests (39 contract stability + 33 business rule). Phase 3 plan/package version references refreshed for `@hbc/features-project-hub` v0.1.73. Previously (v23): Stage 7.9 P3-E11 acceptance sweep completed in `@hbc/features-project-hub` v0.1.72. All 31 T10 §5 acceptance criteria verified against implementation with zero gaps. T10 §11 acceptance verification matrix added. P3-E11 master index updated with implementation completion status. P3-E11 Project Startup Module implementation closed. Previously (v22): Stage 7.9 T10 Stage 8 (Baseline Lock and Closeout Continuity — FINAL STAGE) implemented in `@hbc/features-project-hub` v0.1.71. Created `src/startup/baseline-lock/` completing the full 8-stage P3-E11-T10 implementation sequence: IStartupBaseline Tier 4 immutable snapshot (22 fields, 10 nested types), 5-step atomic lock transaction, Closeout read API (200/403/404/405), triple immutability, startup-baseline-feeds-autopsy Related Items, 9-entry delta analysis map, 5 business rules, 54 tests (25 contract stability + 29 business rule). All 8 stages complete: 630 tests across 16 test files, 8 subdirectories under src/startup/, covering all 28 Startup-owned record families across 4 tiers. Phase 3 plan/package version references refreshed for `@hbc/features-project-hub` v0.1.71. Previously (v21): Stage 7.9 T10 Stage 7 (Project Execution Baseline / PM Plan) implemented in `@hbc/features-project-hub` v0.1.70. Created `src/startup/execution-baseline/` with complete PM Plan contracts: 5 record interfaces (IProjectExecutionBaseline/IExecutionBaselineSection/IBaselineSectionField/IExecutionAssumption/IPlanTeamSignature), 11-section structured baseline, 4-step approval flow, 7 critical certification fields, 9-category assumptions with success criteria, PM+PX signature model, 9 business rules, 78 tests (30 contract stability + 48 business rule). Phase 3 plan/package version references refreshed for `@hbc/features-project-hub` v0.1.70. Previously (v20): Stage 7.9 T10 Stage 6 (Responsibility Matrix) implemented in `@hbc/features-project-hub` v0.1.69. Created `src/startup/responsibility-matrix/` with two-sheet PM/Field routing engine: 3 record interfaces (IResponsibilityMatrix/IResponsibilityMatrixRow/IResponsibilityAssignment), 7+5 role columns, 71+27 assignment-bearing rows, 5 critical categories with acknowledgment gates, governed row immutability, 7 business rules, 67 tests (31 contract stability + 36 business rule). Phase 3 plan/package version references refreshed for `@hbc/features-project-hub` v0.1.69. Previously (v19): Stage 7.9 T10 Stage 5 (Contract Obligations Register) implemented in `@hbc/features-project-hub` v0.1.68. Created `src/startup/contract-obligations/` with full obligation ledger contracts: 2 record interfaces (IContractObligationsRegister 19 fields / IContractObligation 28 fields), 5-state lifecycle with 9 transitions and PX guards, 12 categories with LIQUIDATED_DAMAGES auto-flag, 5 monitoring trigger rules with 3 priority lead days, 5-rule certification eligibility, 9 business rules, 84 tests (31 contract stability + 53 business rule). `StartupDeliveryMethod`/`STARTUP_DELIVERY_METHODS` prefixed to avoid closeout collision. Phase 3 plan/package version references refreshed for `@hbc/features-project-hub` v0.1.68. Previously (v18): Stage 7.9 T10 Stage 4 (Permit Posting Verification) implemented in `@hbc/features-project-hub` v0.1.67. Created `src/startup/permit-posting/` with PermitVerificationDetail companion record (14 fields, one-to-one with Section 4 tasks), 12 permit type mappings, verification/discrepancy rules, PWA/SPFx depth, Permits module non-interference, 7 business rules, 53 tests (20 contract stability + 33 business rule). `StartupPermitType`/`STARTUP_PERMIT_TYPES`/`STARTUP_PERMIT_TYPE_LABELS` prefixed to avoid collision with P3-E7 permits module. Phase 3 plan/package version references refreshed for `@hbc/features-project-hub` v0.1.67. Previously (v17): Stage 7.9 T10 Stage 3 (Safety Readiness) implemented in `@hbc/features-project-hub` v0.1.66. Created `src/startup/safety-readiness/` with complete safety checklist contracts: 32-item governed template catalog (4 highest-risk + 28 other-risk), 4 record interfaces (IJobsiteSafetyChecklist/ISafetyReadinessSection/ISafetyReadinessItem/ISafetyRemediationRecord), remediation state machine (7 transitions), 3 escalation thresholds with ProgramBlocker creation at PX level, 5-rule certification eligibility, Safety module non-interference invariant, 8 business rule functions, 86 tests (37 contract stability + 49 business rule). Phase 3 plan/package version references refreshed for `@hbc/features-project-hub` v0.1.66. Previously (v16): Stage 7.9 T10 Stage 2 (Task Library) implemented in `@hbc/features-project-hub` v0.1.65. Created `src/startup/task-library/` with complete Tier 2 governed template-instance contracts: full 55-task catalog across 4 sections per T03 §12, 12 categories, 3 severity/gating/result enums, 6 owner roles, 5 due triggers, 8 blocker types, 9 evidence types, 3 record interfaces (IStartupTaskTemplate/IStartupTaskInstance/ITaskBlocker), 5 key dependency chains (DAG-validated), 8 immutable template fields, certification eligibility + completion percentage rules, stabilization editability, auto-blocker creation, 8 business rule functions, 108 tests (46 contract stability + 62 business rule). Phase 3 plan/package version references refreshed for `@hbc/features-project-hub` v0.1.65. Previously (v15): Stage 7.9 T10 Stage 1 (Program Core and State Machine) implemented in `@hbc/features-project-hub` v0.1.64. Created `src/startup/foundation/` with complete Tier 1 governance contracts: 8-state lifecycle machine (9 transitions), 9 record interfaces, 28 record families across 4 tiers, 16 governed gate criteria, 18 SoT boundary rows, 10 locked decisions, state machine business rules, 100 tests (55 contract stability + 45 business rule). Phase 3 plan/package version references refreshed for `@hbc/features-project-hub` v0.1.64. Previously (v14): Stage 7.9 Project Startup summary aligned to the T09 permissions, lane ownership, and shared-package governance model as well as P3-E11-T01 through T10. Expanded the Stage 7.9 description to reflect Startup’s authoritative role matrix and PX-exclusive gate ownership, PWA-first lane depth with explicit SPFx Launch-to-PWA deferrals, repo-truth shared-package reuse, and the requirement that Project Startup remain owned by `@hbc/features-project-hub` rather than a standalone `@hbc/project-startup` feature package. Fixed internal P3-E11 contradictions and repo-truth drift: T09 now uses the current `@hbc/features-project-hub` package identity and T02-aligned waiver terminology, T10 and T08 no longer name the stale `@hbc/project-startup` package, and the package README now reflects Startup planning through the full T01–T10 set with T09 permissions/lane governance called out explicitly. Phase 3 plan/package version references are refreshed for `@hbc/features-project-hub` v0.1.63. Previously (v13): Stage 7.9 Project Startup summary aligned to the T08 spine publication, executive review, and cross-module consumption governance model as well as P3-E11-T01 through T10. Expanded the Stage 7.9 description to reflect Startup publication to Activity Spine, Health Spine, Work Queue, Related Items, and project-canvas only; clarified that Startup does not publish to Reports and does not create org-intelligence indexes; clarified that Closeout consumes only the immutable `StartupBaseline` via read-only API; and clarified that `@hbc/field-annotations` executive review annotations remain distinct from certification review, PE mobilization authorization, and baseline lock. Fixed internal P3-E11 contradictions and repo-truth drift: T08 now uses the actual T02 waiver-field terminology and T07 safety-remediation routing fields, T10 and P3-H1 now include Related Items plus the no-Reports/no-org-intelligence publication boundary in Startup acceptance language, and the package README now restores the full T01–T10 canonical planning set including T08 and T09. Phase 3 plan/package version references are refreshed for `@hbc/features-project-hub` v0.1.62.

**Last Updated:** 2026-03-24 (v13) — Added P3-E13 Subcontract Execution Readiness as the governing replacement for the old P3-E12 checklist-and-waiver model. Updated the Workstream E index, status legend, Financial / Buyout references, review-capable surface references, and Stage 7.10 summary so the active Phase 3 authority now points to `P3-E13-T01` through `P3-E13-T08`. Marked P3-E12 as a Superseded Reference retained for historical context only. Previously (v12): Stage 7.9 Project Startup summary aligned to the T07 Startup Safety Readiness and Permit Posting Verification governance model as well as P3-E11-T01 through T10. Expanded the Stage 7.9 description to reflect Startup Safety as a 32-item, 2-section remediation-capable assessment with first-class `SafetyRemediationRecord` assignment, due-date, escalation, and blocker behavior; clarified that safety certification may be submitted with documented open remediations but not with PX-escalated remediations or active blocker conditions unless waived; and clarified that Permit Posting remains Section 4 of the Startup Task Library with one companion `PermitVerificationDetail` per task, evidence/discrepancy rules, and no write path to Permits. Fixed internal P3-E11 contradictions and repo-truth drift: T07 now foregrounds the review-capable Startup Safety boundary despite Safety-module annotation exclusion, T10 and P3-H1 now mirror the T07 submission and evidence rules instead of implying all remediations must be resolved, P3-E1 and P3-E2 now use the actual `JobsiteSafetyChecklist` / `SafetyReadinessItem` / `SafetyRemediationRecord` naming plus the correct escalation model, and P3-E3 now points startup-safety replacement references to `P3-E11-T07`. Phase 3 plan/package version references are refreshed for `@hbc/features-project-hub` v0.1.61. Previously (v11): Stage 7.9 Project Startup summary aligned to the T06 Project Execution Baseline governance model as well as P3-E11-T01 through T10. Expanded the Stage 7.9 description to reflect the PM Plan as an 11-section structured `ProjectExecutionBaseline`, typed baseline fields plus categorized `ExecutionAssumption` records, PM/PX signatures plus PX approval before `EXECUTION_BASELINE` certification, and `StartupBaseline` lock behavior with Closeout reading only the immutable snapshot rather than live PM Plan records. Fixed internal P3-E11 contradictions and repo-truth drift: T06 now makes `BASELINE_APPROVED`, `BASELINE_CRITICAL_FIELDS_SET`, and `BASELINE_SIGNED` explicit, clarifies that `ExecutionAssumption` uses the current categorized field model rather than stale `assumptionText`/`successCriteria` wording, and aligns `executionBaselineFieldsAtLock` to the full structured field map. T02, T10, P3-H1, and P3-E3 now mirror the corrected T06 certification and continuity contracts. Phase 3 plan/package version references are refreshed for `@hbc/features-project-hub` v0.1.60. Previously (v10): Stage 7.9 Project Startup summary aligned to the T05 Responsibility Routing governance model as well as P3-E11-T01 through T10. Expanded the Stage 7.9 description to reflect Responsibility Matrix as a routing engine rather than a spreadsheet artifact, the verified workbook-derived dimensional model of 7 PM assignment columns with 71 assignment-bearing rows plus 11 reminder-only rows and 5 Field assignment columns with 27 assignment-bearing rows, category-level named-`Primary` coverage gates, critical-category `acknowledgedAt` gates, and PM Plan Section VII read-through via `responsibilityMatrixRef`. Fixed internal P3-E11 contradictions and repo-truth drift: T05 now distinguishes assignment-bearing PM rows from reminder-only rows, removes stale 84×9 / 28×8 language, and T10 plus P3-H1 now summarize both Responsibility Matrix certification gates instead of only the acknowledgment gate. Phase 3 plan/package version references are refreshed for `@hbc/features-project-hub` v0.1.59. Previously (v9): Stage 7.9 Project Startup summary aligned to the T04 Contract Obligations governance model as well as P3-E11-T01 through T10. Expanded the Stage 7.9 description to reflect Contract Obligations as an active monitoring ledger rather than a one-time review form, the canonical `OPEN` / `IN_PROGRESS` / `SATISFIED` / `WAIVED` / `NOT_APPLICABLE` lifecycle, `flagForMonitoring`-driven routing and due-date/recurrence handling, certification based on documented review/routing/acknowledgment of monitored and near-due obligations, and read-only Financial/Schedule advisory context. Fixed internal P3-E11 contradictions and repo-truth drift: T04 now uses canonical monitoring-field wording and explicit PX-only reopen governance, T10 and P3-H1 now mirror the T04 certification model instead of a blanket open-obligation block, and Phase 3 reference matrices now use the T03 template-instance checklist model plus the canonical T04 lifecycle names. Phase 3 plan/package version references are refreshed for `@hbc/features-project-hub` v0.1.58. Previously (v8): Stage 7.9 Project Startup summary aligned to the T03 checklist-library architecture as well as P3-E11-T01 through T10. Expanded the Stage 7.9 description to reflect the MOE-governed template-instance model for the 55-task Startup Program Checklist, first-class `TaskBlocker` handling, per-task category/severity/gating/owner/due-trigger/dependency/evidence metadata, task completion vs. PE readiness approval separation, and Section 4 permit-item continuity with T07. Fixed internal P3-E11 contradictions and repo-truth drift: T03 now anchors `DAYS_BEFORE_MOBILIZATION` to `ProjectExecutionBaseline.section VI.projectStartDate`, removes references to nonexistent `TaskBlocker.gatingImpact` and task-evidence snapshotting in `StartupBaseline`, standardizes owner-role codes, and T10 no longer names a nonexistent `StartupTaskLibrary` record. Phase 3 plan/package version references are refreshed for `@hbc/features-project-hub` v0.1.57. Previously (v7): Stage 7.9 Project Startup summary aligned to the T02 record architecture as well as P3-E11-T01 through T10. Expanded the Stage 7.9 description to reflect the 28-record, four-tier Startup model, split org-governed template vs. project-scoped operational identity rules, six `ReadinessCertification` records with PE gate evaluations, `ExceptionWaiverRecord` / `ProgramBlocker` governance, project-scoped `PublicationState` applicability, and immutable `StartupBaseline` Closeout continuity. Fixed internal P3-E11 contradictions and repo-truth drift: T02 now validates the four-tier count, narrows certification/baseline-lock publication rules away from `StartupTaskTemplate`, and Phase 3 plan/package version references are refreshed for `@hbc/features-project-hub` v0.1.56. Previously (v6): Stage 7.9 Project Startup summary aligned to P3-E11-T01 through T10. Corrected the Stage 7.9 description to reflect the six-surface readiness-program model, certification vs. operational status separation, PE mobilization authorization as a separate gate, stabilization-window baseline lock, and StartupBaseline Closeout continuity. Fixed internal P3-E11 contradictions and repo-truth drift: T01 lifecycle transition wording aligned with the eight-state model, stale `@hbc/activity-spine` / `@hbc/health-spine` references replaced with current Activity Timeline and Health Spine contract wording, and Phase 3 plan/package version references refreshed for `@hbc/features-project-hub` v0.1.55. Previously (v5): Workstream J (Documents Enabling Seams) added. P3-J1 Specification created. Stage 13 added to Implementation Guide. Full unified Documents product added to Explicit Phase 3 Defer List. Related Documents updated with enabling plan and Phase 5 plan references. Previously (v4): Workstream I (Shared Feature Infrastructure) added. SF23 (Record Form), SF24 (Export Runtime), SF25 (Publish Workflow), SF26 (Saved Views), SF27 (Bulk Actions), SF28 (Activity Timeline) incorporated as Phase 3 Stage 5. Stage 5 inserted into Implementation Guide; former Stages 5–11 renumbered Stages 6–12. Stage 7 dependency updated to require Stage 5. SF26 plan file created (was missing). Phase 3 Integration sections added to all 6 SF plan files. P3-E1 §13 governs per-module integration contracts. Previously (v3): — P3-E12 Subcontract Compliance module added (replaces SUBCONTRACT CHECKLIST.xlsx manual workflow; reclassified from Estimating/Cross-cutting). P3-E1 §3.11 and classification table row 14 added. P3-E2 §16 source-of-truth and action-boundary added; §17 Acceptance Gate updated; tail renumbered to §14–§18. P3-E3 §8.5, §9.9, §9.10 added; §9.7 updated. P3-E4 §6.1 `subcontractChecklistId` field and §6.2a ContractExecuted gate rule added. Implementation Guide: Stage 6.10 added; Stage 0.1 and Stage 7.1 updated to include Project Closeout, Project Startup, and Subcontract Compliance as review-capable surfaces. Deliverable count updated to 28. Previously: P3-E11 Project Startup module added (replaces Job Startup Checklist, Jobsite Safety Checklist, Responsibility Matrix templates, Owner Contract Review template, and PROJECT MANAGEMENT PLAN 2019.docx manual workflow). Project_Safety_Checklist.pdf reclassified from Safety domain to Project Startup. Responsibility Matrix files and PM Plan reclassified from cross-cutting to Project Startup. P3-E1 §3.10 and classification table row 13 added. P3-E2 §15 source-of-truth and action-boundary added (tail sections renumbered to §14–§17 to avoid duplication with original §12–§13). P3-E3 §8.4 and §9.8 added; §6.2, §9.5, §9.7 updated. Deliverable count updated to 27. Previously: P3-E10 Project Closeout module added (replaces SubScorecard SOP, Lessons Learned SOP, and Closeout Checklist PDF). SubScorecard and Lessons Learned reclassified from standalone report families to module-generated report artifacts owned by Project Closeout. P3-E9 updated with reclassification notice. P3-E3 inventory updated.

**Governing Plan:** [Phase 3 Plan §14–§15](../04_Phase-3_Project-Hub-and-Project-Context-Plan.md)
