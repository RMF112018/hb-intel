# Wave 1 Validation Matrix

| Prompt | Expected Changes | Required Validation | Must Not Change |
|---|---|---|---|
| Prompt 01 — Repo Truth Audit and Scope Lock | Documentation-only scope lock and audit summary under repo-confirmed Wave 1 docs path | `git status --short`; optionally `pnpm format:check` if docs formatting is expected | No code; no packages; no apps; no backend; no tooling; no versions; no manifests; no CI/CD |
| Prompt 02 — Shared Model Contracts | PCC shared model types under repo-approved model path, likely `packages/models/src/pcc/**` | `git status --short`; `pnpm --filter @hbc/models check-types`; `pnpm --filter @hbc/models test`; `pnpm --filter @hbc/models build`; optionally `pnpm --filter @hbc/models lint` | No shell UI; no backend routes; no provisioning executor; no tenant calls; no `@hbc/project-site-template` changes; no `@hbc/project-site-provisioning` changes; no version bumps |
| Prompt 03 — Role, Work Center, and Status Registries | PCC persona, work center, workflow module, status, priority category, and capability registries | `git status --short`; `pnpm --filter @hbc/models check-types`; `pnpm --filter @hbc/models test`; `pnpm --filter @hbc/models build`; optionally `pnpm --filter @hbc/models lint` | No UI navigation; no routes; no live auth; no permission mutation; no Graph/PnP/Procore |
| Prompt 04 — Workflow, Audit, and Approval Types | PCC workflow transition, audit event, approval checkpoint, reviewer action, comment/history, and assignment types | `git status --short`; `pnpm --filter @hbc/models check-types`; `pnpm --filter @hbc/models test`; `pnpm --filter @hbc/models build`; optionally `pnpm --filter @hbc/models lint` | No workflow API; no SPFx forms; no SharePoint list writes; no backend persistence |
| Prompt 05 — External System and Site Health Types | PCC external system IDs, launch-link types, Document Control source types, Site Health summary/severity, repair request types | `git status --short`; `pnpm --filter @hbc/models check-types`; `pnpm --filter @hbc/models test`; `pnpm --filter @hbc/models build`; optionally `pnpm --filter @hbc/models lint` | No Procore runtime; no Procore secrets; no sync/mirror/write-back; no direct SPFx-to-Procore; no automatic repair |
| Prompt 06 — Fixtures, Feature Flags, and No-Mutation Guards | Deterministic PCC fixtures, model-level feature/module flags, no-mutation/no-secret guard tests if approved | `git status --short`; `pnpm --filter @hbc/models check-types`; `pnpm --filter @hbc/models test`; `pnpm --filter @hbc/models build`; optionally `pnpm --filter @hbc/models lint` | No real tenant URLs unless explicitly dev-safe; no secrets; no Graph/PnP callers; no executable mutation behavior; no runtime feature wiring |
| Prompt 07 — Exports, Tests, and Documentation Closeout | Final PCC exports, tests, docs, and Wave 1 closeout | `git status --short`; `pnpm --filter @hbc/models check-types`; `pnpm --filter @hbc/models test`; `pnpm --filter @hbc/models build`; optionally `pnpm --filter @hbc/models lint`; optionally `pnpm check-types` for broader confidence | No Wave 2 shell work; no Wave 3 backend work; no deployment; no app catalog; no package/SPFx manifest version bumps |
