# Reference Asset Note

The PCC dashboard basis-of-design image has already been saved to the live repo at:

```text
docs/reference/ui-kit/dashboard/dashboard-basis-of-design.png
```

This zip intentionally references that repo path rather than treating the generated image copy as the source of truth.

Future developers should inspect that file directly from the repo before implementing or changing PCC shell UI.
