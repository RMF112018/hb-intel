# Prompt 06 — Closeout Documentation, Validation, and Resolution Closure

## Objective

Finalize the remediation by adding closeout documentation, running complete validation, and preparing the final commit.

This prompt must not introduce new feature scope. It is the closure pass.

## Context

Prompts 01–05 should have:

- instrumented the primitive;
- strengthened visual tier styling;
- explicitly classified all current PCC cards;
- added cross-surface enforcement tests;
- preserved runtime guardrails.

This prompt records closure.

## Required Repo Checks

Start with:

```bash
git status --short
git diff --stat
```

Confirm the worktree only includes files related to this remediation.

## Required Validation

Run the strongest practical validation set:

```bash
pnpm --filter @hbc/project-control-center check-types
pnpm --filter @hbc/project-control-center test
pnpm exec prettier --check \
  apps/project-control-center/src/layout \
  apps/project-control-center/src/shell \
  apps/project-control-center/src/surfaces \
  apps/project-control-center/src/tests
git diff --check
```

If available and not excessively costly, also run:

```bash
pnpm check-types
pnpm test
```

Do not skip targeted validation if full workspace validation fails because of unrelated known issues. If unrelated failures exist, document them with evidence.

## Required Closeout Document

Create a closeout document at the most appropriate canonical path for Wave 15A remediation. Preferred path:

```text
docs/architecture/blueprint/sp-project-control-center/phase-3/wave-15A/card-tier-contract-remediation/CARD_TIER_CONTRACT_REMEDIATION_CLOSEOUT.md
```

If that folder convention conflicts with existing repo structure, use the nearest existing Wave 15A closeout path and explain the choice.

## Closeout Document Required Sections

Use this exact structure:

```markdown
# PCC Card Tier Contract Remediation Closeout

## 1. Objective

## 2. Scope Completed

## 3. Files Changed

## 4. Primitive Contract Changes

## 5. Surface Classification Summary

## 6. Test Coverage Added

## 7. Validation Evidence

## 8. Hosted Evidence Status

## 9. Guardrails Preserved

## 10. Residual Risks / Follow-Up

## 11. Final Closure Statement
```

### Section requirements

#### 1. Objective

State that the remediation closes the card tier/region/footprint contract issue across current PCC cards.

#### 2. Scope Completed

List the areas covered:

- shared primitive;
- Project Home;
- Team & Access;
- Documents;
- Project Readiness and embedded subregions;
- Approvals;
- External Systems;
- Settings;
- Site Health;
- tests.

#### 3. Files Changed

List principal changed files. Do not invent file counts if not certain.

#### 4. Primitive Contract Changes

Describe:

- tier source marker;
- region source marker;
- heading level marker;
- visual hierarchy hardening.

#### 5. Surface Classification Summary

Summarize final classification posture by surface.

#### 6. Test Coverage Added

List new/updated tests and what they enforce.

#### 7. Validation Evidence

Paste exact commands and results.

#### 8. Hosted Evidence Status

State honestly:

- hosted screenshots were not generated by the local code agent unless actually captured;
- source/test closure is complete;
- hosted screenshot matrix remains required for final UI doctrine score closure if not captured.

Required hosted evidence matrix to list:

- desktop edit mode;
- desktop view mode;
- standard laptop;
- small laptop;
- tablet landscape;
- tablet portrait;
- phone portrait;
- phone landscape / short-height;
- ultrawide;
- high zoom / constrained window.

#### 9. Guardrails Preserved

Confirm:

- no live write paths;
- no live external launch links introduced;
- no backend integration changes;
- direct bento child invariant preserved;
- row-span collapse resistance preserved;
- no dense grid behavior;
- no active panel duplication.

#### 10. Residual Risks / Follow-Up

Only include real residual risk.

Examples:

- hosted screenshot matrix pending;
- visual polish may need one more design pass after hosted review;
- if full workspace validation had unrelated failures, document them.

#### 11. Final Closure Statement

State whether the code/test remediation is closed.

Do not claim final hosted UI doctrine closure unless hosted evidence was completed.

## Required Final Diff Review

Run:

```bash
git diff --stat
git diff --check
```

Review changed files for:

- accidental unrelated edits;
- stray comments;
- debug logs;
- unintentional anchors;
- unintentional handlers;
- failed formatting.

## Commit

Commit the remediation.

Use this commit message:

```text
feat(pcc): remediate shared card tier contract across PCC surfaces

Implement the PCC card tier / region / footprint contract remediation across all current Project Control Center route surfaces and embedded subregions.

- Adds shared card contract instrumentation so tests can distinguish explicit tier / region declarations from legacy hierarchy or default fallback.
- Strengthens token-driven Tier 1 / Tier 2 / Tier 3 / state / deferred / detail / rail visual hierarchy.
- Migrates route command, operational, reference, deferred, state, and embedded workbench cards to explicit tier and region declarations.
- Adds cross-surface tests for active-panel ownership, direct bento children, tier / region classification, heading levels, deferred-state integrity, and no dense grid behavior.
- Adds closeout documentation with validation evidence and hosted-evidence follow-up requirements.

Validation:
- <paste exact commands and pass/fail results>
```

If your local workflow requires shorter commit subjects, keep the same meaning.

## Final Agent Response

Return only:

```text
Commit Summary

feat(pcc): remediate shared card tier contract across PCC surfaces

Commit Description

<same structured description with validation evidence>

Files changed:
- <principal paths>

Residual follow-up:
- <hosted evidence pending, if applicable>
```
