# Coverage Matrix

| Original remediation item | Repo-truth finding | Updated prompt file | Closure intent | Validation expectations |
|---|---|---|---|---|
| Normalize resolution read failures | `buildPublishResolutionContext(...)` still emits typed failures only for `articleNotFound` and `templateResolutionFailed`; repository read throws can still escape raw. | `Prompt-01-Normalize-resolution-context-read-failures-and-error-observability.md` | Force all resolution reads into a stable typed contract and make non-preview failures observable in `HB Article Publishing Errors`. | Unit tests for each repository-read failure seam plus orchestrator proof that non-preview logs an error row and preview stays write-free. |
| Close publish binding rollback gap | Late publish failure is broader than binding-only: page can be demoted while binding remains published/in-sync and master/history truth can diverge. | `Prompt-02-Close-late-publish-split-state-windows-across-page-binding-master-and-history.md` | Replace false-live / false-healthy late-publish state with one explicit consistency model across page, binding, master, and history. | Targeted publish-orchestrator tests for create, in-place republish, and regenerate late-failure branches. |
| Close archive/withdraw split-state gaps | After page demotion, binding-update/article-update failures are still split-state risks; history-append compensation can also revert only the master row. | `Prompt-03-Redesign-archive-withdraw-failure-handling-for-fail-truthful-lifecycle-state.md` | Ensure lifecycle transitions converge to internally truthful non-live state after late failures without silently keeping pages public. | Lifecycle tests for binding-update, article-update, and history-append failures for both archive and withdraw. |
| Make draft save atomic or compensating | Child writers are already keyed-sync, but `handleSave()` still writes the master row before child sync finishes, leaving ambiguous partial persistence. | `Prompt-04-Extract-and-harden-draft-save-orchestration-for-truthful-master-child-persistence.md` | Move save into a service-level orchestration model with explicit truthfulness about master/team/media persistence outcome. | Save-path tests for team failure after master write and media failure after master+team write, plus UI/controller outcome proof. |
