# HB Intel My Dashboard — My Projects Source-List Schema Provisioning Audit

Prepared: 2026-05-14  
Repo: `RMF112018/hb-intel`  
Branch audited: `main`  
Prompt basis: `FRESH_SESSION_PROMPT_MY_PROJECTS_LIST_PROVISIONING_METHOD_AUDIT_AND_RESEARCH(1).md`


## Phase 6 — Findings Register

| ID | Title | Severity | Classification | Files / Seams | Evidence Summary | Why It Matters | Recommended Disposition | Proof Required to Close |
|---|---|---|---|---|---|---|---|---|
| F-001 | Complete My Projects role taxonomy is defined in shared models | High | Confirmed repo truth | `packages/models/src/myWork/MyProjectAssignmentRoles.ts` | The repo defines fourteen role IDs, internal fields, display labels, and priority. | Prevents field-name drift across backend, scripts, and UI. | Use this model constant as the only source for provisioning descriptors. | Unit test descriptor fields equal `MY_PROJECT_ASSIGNMENT_INTERNAL_FIELDS`. |
| F-002 | `Projects` requires fourteen target role-array fields | Critical | Schema contract gap | `projects.md`, `projects-list-contract.ts`, readiness helper | Docs mark fields target-only until verifier reports ready. | Missing fields reduce assignment matching; only four legacy fallbacks exist. | Provision missing Note fields via dedicated script. | Readiness JSON `projects.ready === true`. |
| F-003 | Registry requires fourteen role-array fields plus `procoreProject` | Critical | Schema contract gap | `legacy-project-fallback-registry.md`, list descriptor, provider | Registry has no provider-side fallback for canonical roles. | Missing fields create silent zero-match for legacy-only assignments. | Provision missing fields and backfill/mirror values. | Readiness JSON `legacyRegistry.ready === true`. |
| F-004 | Runtime provider already depends on canonical fields | High | Confirmed repo truth | `my-project-links-read-model-provider.ts` | Provider selects canonical fields and matches actor UPN. | Schema readiness is not optional for real My Projects functionality. | Keep provider; close schema gate. | Functional smoke returns expected assigned projects. |
| F-005 | Existing legacy fallback provisioner is useful but too broad | Medium | Provisioning capability gap | `scripts/provision-legacy-fallback-lists.ts` | Can create lists/fields and validate schema but is scoped to legacy fallback descriptors. | Reusing as-is risks unrelated drift/noisy mutation. | Factor shared utilities; create dedicated source-list provisioner. | New script tests and dry-run report. |
| F-006 | Existing provisioner lacks explicit dry-run/apply gate | High | Provisioning capability gap | `scripts/provision-legacy-fallback-lists.ts` | Script mutates by default and only has `--allow-type-drift`. | Not safe enough for existing production list expansion. | New My Projects script must default dry-run and require `--apply`. | Test proves no mutation occurs without `--apply`. |
| F-007 | `FolderWebUrl` descriptor/live type drift remains | High | Descriptor/docs drift | `legacy-project-fallback-registry.md`, `list-descriptors.ts`, compatibility helper | Snapshot says `Text`; descriptor says `URL`. | Full legacy descriptor run may produce unresolved drift. | Isolate My Projects schema delta from this field; remediate separately. | Dedicated provisioner does not evaluate/touch `FolderWebUrl`; separate drift finding remains tracked. |
| F-008 | SPFx `access_as_user` and app-only provisioner identity are separate | High | Permission / identity ambiguity | `package-solution.json`, B05A readiness doc, app registration JSON | SPFx requests delegated API scope; schema mutation needs app-only Graph/SharePoint permission. | Conflating them can lead to false readiness. | Keep separate in docs and script env guidance. | Operator evidence names the execution identity used for provisioner. |
| F-009 | Function App UAMI differs from HB SharePoint Creator app ID | High | Permission / identity ambiguity | Function App ARM JSON, app registration JSON | UAMI client ID `77ad...` differs from app ID `08c...`. | `DefaultAzureCredential` may run as the wrong identity if environment is ambiguous. | Require identity lane declaration and runtime proof. | Token/command evidence proves selected identity can mutate columns. |
| F-010 | HB SharePoint Creator app registration appears properly configured for broad permissions | Medium | Operator proof required | Operator-provided app JSON | App manifest declares Microsoft Graph and SharePoint resource access including broad site roles. | Strong signal, but manifest is not effective-grant proof. | Treat as strong operator evidence; still require runtime/admin-consent proof. | Service-principal grant proof or successful dry-run/apply. |
| F-011 | Graph columnDefinition API is viable but not preferred as primary | Medium | Method recommendation | Microsoft Graph docs | Graph supports list column create/update with `Sites.Manage.All` least-privileged application permission. | Useful fallback if PnP/REST script path fails. | Keep Graph as alternative transport behind repo-native utility. | Spike/proof only if selected. |
| F-012 | PnP PowerShell is viable but weak for repo closure | Low | Method recommendation | PnP PowerShell docs | `Add-PnPField` and `Set-PnPField` can mutate fields. | Manual runbooks are less testable and more drift-prone. | Use only emergency fallback plus verifier. | Operator logs and readiness proof. |
| F-013 | Existing verifier is correct readiness gate | High | Confirmed repo truth | `verify-my-project-role-fields.ts`, readiness helper, readiness docs | Read-only, no apply, exit code reflects both-list readiness. | Provides safe pre/post evidence. | Keep as closure gate. | `ready: true` JSON archived. |
| F-014 | Backfill scripts must run only after schema readiness | High | Confirmed repo truth | backfill scripts | Scripts select and update canonical fields. | Running before columns exist will fail or no-op incorrectly. | Gate backfills after provisioning + verifier. | Backfill dry-run/apply JSON and readiness proof. |
| F-015 | No new app registration is required | Advisory | Concern disproven by audit | Repo docs + operator app JSON | Existing `HB SharePoint Creator` app ID matches repo docs. | Avoids unnecessary security sprawl. | Use existing app path or prove UAMI path. | Operator identity proof. |
