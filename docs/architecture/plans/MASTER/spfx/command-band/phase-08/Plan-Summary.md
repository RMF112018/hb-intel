# Plan Summary — Actions Rail + HB Homepage Enhanced Remediation

## Objective
Bring the `PriorityActionsRail` + `HbHomepage` flagship integration surface into closure-grade compliance by:

- proving hosted parity first,
- correcting the visible-set curation model,
- removing sparse group waste,
- strengthening overflow and mode divergence,
- hardening hosted proof,
- and closing leftover contract honesty gaps.

## Core diagnosis
The prior packages correctly identified that the hosted result is weak, but they underframed the actual repo-truth situation.

The repo already contains a stronger flagship surface path than the screenshot suggests.
So the work must begin by proving whether the hosted tenant is actually rendering the current code and assets.

After that, the real code gaps are concentrated in:

1. primary action curation
2. sparse group rendering
3. secondary command-layer behavior
4. adaptive layout divergence
5. closure proof
6. contract honesty

## Recommended prompt execution order
1. `Prompt-01-Prove-Hosted-Runtime-Parity-and-Rebuild-the-SPPKG.md`
2. `Prompt-02-Replace-Slice-Then-Group-with-Homepage-Flagship-Curation.md`
3. `Prompt-03-Compact-Singleton-Sections-and-Eliminate-Heading-Waste.md`
4. `Prompt-04-Promote-Desktop-and-Tablet-Overflow-into-a-Real-Secondary-Command-Layer.md`
5. `Prompt-05-Make-Layout-Normalization-Materially-Divergent-by-Mode.md`
6. `Prompt-06-Harden-Flagship-Tile-Density-and-Wide-State-Authority.md`
7. `Prompt-07-Add-Closure-Grade-Tests-and-Hosted-Proof.md`
8. `Prompt-08-Resolve-StickyAfterHero-Contract-Honesty.md`

## Non-negotiable working rules
- Do not reopen wrapper ownership.
- Do not move the rail into shell-occupant semantics.
- Do not fork a separate homepage-only rail unless Prompt 01 proves a real export/path defect that cannot be resolved in the shared family.
- Do not re-read files already in active context unless needed to confirm drift, dependencies, or uncertainty after changes.
- Do not treat “it technically renders” as closure.
- Do not defer material in-scope issues into future work.

## Closure standard
The work closes only when:

- the hosted tenant is proven to be running the intended flagship path,
- the visible set is curated intelligently,
- sparse groups no longer waste space,
- overflow is deliberate and device-appropriate,
- the band reads as a premium command layer,
- tests and hosted proof both support closure,
- and no misleading contract surface remains.
