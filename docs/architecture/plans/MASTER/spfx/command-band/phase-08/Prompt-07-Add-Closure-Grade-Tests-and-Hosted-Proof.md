# Prompt 07 — Add Closure-Grade Tests and Hosted Proof

## Objective
Raise the proof standard so this surface cannot close again on plumbing success while still failing as a hosted flagship command band.

## Why this prompt exists
Current tests are useful, but they mostly prove:
- ownership
- context threading
- overflow accessibility basics
- some flagship/default branching

That is not enough to prevent the current failure mode from recurring.

## Current issue
The surface can still be structurally “correct enough” while remaining weak in the actual hosted product read.

## Governing authority
Apply directly:
- benchmark package scoring materials
- benchmark closure checklist
- `UI-Doctrine-SPFx-Governing-Standard.md`
- `UI-Doctrine-SPFx-Homepage-Overlay.md`

## Inspect at minimum
- `packages/ui-kit/src/HbcPriorityRail/__tests__/HbcPriorityRail.test.tsx`
- `apps/hb-webparts/src/webparts/hbHomepage/__tests__/hbHomepageEntryStack.test.tsx`
- any local hosted-proof or validation docs created during this remediation

## Required implementation outcome
Add tests and closure proof that cover at minimum:
- flagship curation rules
- sparse/singleton section compaction or suppression
- layout/presentation divergence by device class
- runtime marker integrity
- overflow strategy expectations where appropriate
- no regression of default/non-flagship consumers

Also produce hosted proof using the checklist in `09-Hosted-Runtime-Proof-Checklist.md`.

## Done state
“Done” means the surface now has both:
- deterministic automated proof,
- and hosted runtime proof,
for the behaviors that actually matter to closure.

## Required proof of closure
Return:
- the new/updated tests
- what each new test is proving
- hosted screenshots by device class / width
- runtime DOM evidence
- package deployment evidence
- a final closure note stating why the hosted output is now trustworthy

## Working rules
- Do not reopen already-correct wrapper ownership unless repo truth proves a real defect.
- Do not migrate the rail into shell-occupant semantics.
- Do not drift into unrelated homepage modules.
- Do not re-read files already in active context unless needed to confirm drift, dependencies, or uncertainty after changes.
- Return concrete proof of closure, not just a description of the changes.
