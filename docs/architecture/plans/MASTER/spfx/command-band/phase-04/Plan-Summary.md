# Plan Summary — Enhanced Wave 02

## Objective

Replace the attached thin `wave-02` package with a stronger audit and execution package grounded in the actual state of `main`.

## Core judgment

The attached package is too shallow and partly mis-sequenced.

It correctly identifies three real themes:

- styling/token closure
- validation/runtime coherence
- hosted proof

But it misses that several earlier structural and product-grade gaps are still open in the live implementation.

## What repo truth says must happen now

### Preserve
- the typed contract layer as the foundation
- the explicit read seams and list descriptors
- the shared/public/admin split
- the proof-first closure posture

### Reopen
- persisted identity and reorder trust
- destructive-action semantics
- permission-aware authoring behavior
- shared/public breakpoint and overflow fidelity
- preview/public parity
- honest closure documentation

## Corrected package logic

### Prompt 01
Repair correctness and persistence truth.

### Prompt 02
Turn the admin into the maintainer product the spec actually requires.

### Prompt 03
Rebuild the shared/public surface into the command band the spec actually describes.

### Prompt 04
Align contracts, validation, queries, normalization, and preview/runtime truth.

### Prompt 05
Close token discipline and premium styling after behavior is correct.

### Prompt 06
Perform final manifest/runtime verification, hosted proof, and stale-doc correction.

## Strongest preserved insight from the old package

It was right to insist that closure must be:

- proof-based
- doctrine-aware
- benchmark-aware
- hostile to vague “future work” language

## Biggest weakness in the old package

It assumed the product model was already trustworthy enough that only hardening remained.

Repo truth says that is not accurate.

## Execution note

This package is intentionally forceful.

A local code agent should not be allowed to:
- paper over structural drift with visual polish
- leave preview/runtime divergence in place
- keep stale closure docs
- declare completion without the proof enumerated in each prompt
