# Prompt-03-Hosted-Package-Closure

## Objective
Close the loop between source intent and hosted/package reality for the redesigned public hbKudos runtime.

## Inspect exactly
- `apps/hb-webparts/src/webparts/hbKudos/HbKudosWebPart.manifest.json`
- packaging/build seams relevant to hb-webparts / homepage
- any mount or deployment contract affected by the redesign
- hosted result in SharePoint

## Current problem
The audit package and doctrine guards are strong, but final acceptance still depends on the packaged, hosted result preserving the intended structural and visual outcome.

## Required implementation outcome
- confirm packaged result matches source intent
- confirm hosted SharePoint screenshots reflect the redesigned public runtime
- confirm no packaging/posture drift was introduced

## Proof of closure
Provide:
- exact build/package commands used
- hosted screenshots
- a short closure note mapping source changes to packaged behavior
