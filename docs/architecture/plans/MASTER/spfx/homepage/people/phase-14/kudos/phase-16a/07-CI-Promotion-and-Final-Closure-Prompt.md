# 07 — CI Promotion and Final Closure Prompt

You are working in the live repo for `RMF112018/hb-intel`.

Your task is to finish the HB Kudos closure effort by promoting the runnable suite into a real regression posture and producing a final honest closure report.

## Mandatory directive

Do not re-read files that are already in your active context unless needed to confirm drift, dependencies, or uncertainty after changes.

## Objective

Move Kudos testing from partial/documented readiness to an actual enforceable regression gate.

## Required CI posture

### Stage 1 — required now

Require on every PR:

- the Kudos-owned Vitest seam suite
- Playwright Kudos discovery sanity if still useful
- any newly added source-level hardening tests

### Stage 2 — required after browser activation

Require on every PR:

- runnable Playwright P0 subset for Kudos

### Stage 3 — nightly / scheduled

Run:

- Playwright P1 subset
- thin live SharePoint integration lane if environment is available and stable

## Required artifact posture

For runnable Playwright lanes, ensure CI preserves:

- HTML report
- traces for failures
- screenshots for key proof captures

For the final closure note, document exact artifact locations.

## Required final closure document

Write a final closure report that states plainly:

1. what is runnable today
2. what is required in CI today
3. what is nightly only
4. what remains intentionally deferred
5. what known exclusions still exist
6. whether the Kudos test posture is now fit to serve as a serious regression gate

## Pass criteria

Only mark this effort closed if all of the following are true:

- dev-harness prerequisites exist in repo truth
- runtime locator contract exists in repo truth
- P0 browser lane executes, not just discovers
- source-level hardening tests are added and passing
- thin live SharePoint lane exists and is documented
- CI has been promoted beyond discovery-only

## Fail criteria

Do not mark this effort closed if any of the following remain true:

- `test.fixme` still blocks core browser coverage
- harness globals/routes are still missing
- locator registry still points to nonexistent runtime nodes
- CI still relies on browser discovery only
- closure report avoids naming unresolved gaps
- artifacts are only described, not generated by real runs

## Prohibited shortcuts

- no soft closure language
- no “structurally complete” language if core execution is still blocked
- no selective omission of remaining blockers
- no weakening of regression assertions simply to get green

## Required output

Commit:

- CI changes
- final closure report
- concise operator/run instructions for future maintainers

Then state explicitly whether the HB Kudos suite is finally:
- not yet closed
- operationally closed
- regression-gate ready
