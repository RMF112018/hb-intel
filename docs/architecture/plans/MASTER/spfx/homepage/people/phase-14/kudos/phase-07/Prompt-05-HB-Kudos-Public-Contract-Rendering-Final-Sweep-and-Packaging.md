# Prompt-05 — HB Kudos Public Contract / Rendering Final Sweep and Packaging

```md
Objective

Conduct the final targeted sweep for the public HB Kudos contract/rendering defect class, perform any truly necessary last-mile fixes, and generate a fresh packaged deliverable with an honest final verdict.

Primary intent

This is the release-gate pass for the targeted contract/rendering work.

It should confirm whether the public featured recognition rendering defect is actually closed and whether the packaged solution reflects that closure.

Critical Constraints

- Use live repo truth.
- Do not begin by re-reading files already in active context or memory.
- Maintain strict focus on the public Kudos contract/rendering path and its directly supporting dependencies.
- Be rigorous, skeptical, and evidence-based.
- Maintain strict compliance with `@hbc/ui-kit`, homepage doctrine, and SPFx packaging discipline.
- Do not claim closure unless both rendered evidence and package state support that conclusion.

What this prompt must do

1. Audit the final public HB Kudos featured/rendered state after Prompts 01–04.
2. Verify that the contract/rendering defect class is actually closed.
3. Identify and fix any remaining small-to-medium issues that still materially weaken closure.
4. Validate manifest/export/mount/package wiring required for the public webpart to ship correctly.
5. Run the fresh build/package flow for the updated application package.
6. Produce a concise final contract/rendering audit-and-packaging report.

Required audit dimensions

Audit the final state for:
- featured recognition rendering integrity
- adapter/shared-surface contract alignment
- sparse-data / fallback safety
- local/shared/ui-kit governance correctness
- regression control across archive/composer/detail
- manifest/export/mount correctness
- package freshness
- packaged inclusion correctness

Required deliverables

1. Implement any final last-mile fixes that are genuinely needed.
2. Build/package the updated deliverable fresh.
3. Produce a final audit report covering:
   - what was checked
   - what was fixed
   - what remains, if anything
4. Produce a packaging report covering:
   - commands run
   - outputs produced
   - whether the resulting package appears fresh and aligned with repo truth
5. Issue a final targeted readiness assessment using:
   - Closed / Ready
   - Closed with Minor Reservations
   - Not Closed
6. State plainly whether the public featured contract/rendering defect class is actually closed.

Acceptance standard

This prompt is successful only if the agent completes a credible final-sweep audit, generates a fresh package, and issues an honest final verdict on whether the public HB Kudos contract/rendering failure has truly been resolved.
```
