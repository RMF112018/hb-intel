# Prompt 06 — Embed HB Kudos into HB Homepage

You are working in the live local HB Intel repo.

## Objective

Embed `HbKudos` into `hb-homepage` as the final and most sophisticated public homepage module in this sequence.

This prompt must preserve Kudos' premium benchmark and workflow richness while making it compatible with the orchestrated homepage host.

## Critical operating instruction

Do not re-read files that are already in your active context unless needed to confirm drift, dependencies, or uncertainty after changes.

## Governing authority

- the two doctrine files
- the `hb-homepage` authority and architecture docs
- all prior `hb-homepage` prompt closure notes

## Required implementation

1. embed `HbKudos` into `hb-homepage`
2. determine whether Kudos needs an explicit embedded-shell mode or host contract
3. preserve its workflow features and premium benchmark
4. remove only the outer standalone layout assumptions that conflict with shell ownership
5. ensure shell layout, spacing, and neighboring-module composition remain stable
6. preserve host-safe behavior and any assistant-safe / layout-safe logic that still applies

## Required closure note

Create:

`docs/architecture/plans/MASTER/spfx/homepage/hb-homepage/phase-03/06-Embed-Kudos.md`

It must include:

- exact files changed
- whether an embedded-shell contract was added
- any Kudos behavior intentionally preserved unchanged
- any Kudos behavior intentionally adapted for shell use
- residual risks before mount/packaging work

## Constraints

- Do not broaden scope into Kudos companion runtime
- Do not perform unrelated redesign outside shell compatibility
- Do not skip proof of how shell ownership and Kudos ownership now differ

## Completion standard

This prompt is complete when `HbKudos` renders inside `hb-homepage` without regressing its product quality and without continuing to own outer page composition.
