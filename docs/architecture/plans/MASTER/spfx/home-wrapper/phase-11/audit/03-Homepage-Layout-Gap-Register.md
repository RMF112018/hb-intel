# Homepage Layout Gap Register

All severities and current-state descriptions are validated against `main`. Occupant citations reference `apps/hb-webparts/src/webparts/hbHomepage/shell/occupantRegistry.ts`; preset citations reference `shell/defaultPreset.ts`; type and ratio citations reference `shell/shellTypes.ts` and `HbHomepageShell.module.css`.

| ID | Severity | Current state (repo truth) | Why it matters | Correction direction | Type |
|---|---|---|---|---|---|
| GAP-01 | Critical | `DEFAULT_PRESET` encodes six bands with Company Pulse duplicated in three slots and Leadership Message duplicated in two; HB Kudos and People & Culture Public are each `stacked-full` alone. | The locked three-row composition is not encoded anywhere as the shell's authoritative target, and the current preset is internally inconsistent on its own terms. | Replace the default preset and band inventory with the requested three live paired rows, eliminating duplicate occupants and dormant bands. | Structural redesign |
| GAP-02 | Critical | `DominanceRule = 'left-dominant' \| 'equal' \| 'single'` (`shellTypes.ts:146`); CSS expresses only left-dominant column templates. | Row 2 must invert to small-left / large-right. The shell type system, validation pipeline, and CSS cannot express this today. | Add `right-dominant` to `DominanceRule`, extend preset schema / validation / conformance, and add mirrored CSS column templates per recipe. | Structural redesign |
| GAP-03 | High | Paired ratios are recipe-specific and container-queried; ~2:1 is reached only at ≥1900px. At 1180–1599px ratios land at 1.5:1–1.75:1. | The intended premium dominance is not delivered at the common laptop/desktop band, which is where most users land. | Tune the 1180px and 1600px container-query tiers so `feature-pair` and `asymmetric-two-up` read closer to 2:1 after gutters without breaking the ≥1900px treatment. | Targeted refinement |
| GAP-04 | Critical | HB Kudos `allowedBandSemantics: ['recognition']`, `prominenceCeiling: 'contextual'`, `firstLaneEligible: false`, `protectedConstraints: ['recognition-cannot-be-primary-anchor']`. Project Portfolio Spotlight `allowedBandSemantics: ['operational-spotlight']`, `reorderDomain: 'locked'`. | Row 1 (PPS large + Kudos small under operational-spotlight) is illegal. Kudos cannot currently live in an operational-spotlight band, and its prominence ceiling + first-lane rules would also need to accommodate the first paired row. | Add `operational-spotlight` to HB Kudos `allowedBandSemantics`, relax first-lane eligibility for the secondary slot only, and revise protected decisions to allow Kudos as a secondary anchor alongside PPS. | Structural redesign |
| GAP-05 | Critical | Safety `allowedBandSemantics: ['operational-spotlight']` only; `communications-newsroom` not included. | Row 2 wants Safety + Company Pulse with Company Pulse as the large (newsroom-semantic) slot. Safety is not legal in `communications-newsroom`, blocking the pairing at validation. | Add `communications-newsroom` to Safety `allowedBandSemantics` or redefine Row 2 under a combined operational-spotlight right-dominant recipe. Either path requires handedness support from GAP-02. | Structural redesign |
| GAP-06 | Critical | People & Culture Public `allowedSlotRoles: ['primary']`, `pairedLayoutEligible: false`, `fallbackWhenUnsafe: 'deny-pairing'`, `protectedConstraints: ['people-culture-must-stack']`, `supportsCompact: false`, `narrowestStablePairedWidth: 640–720`, `allowedBandSemantics: ['people-culture']`. | Row 3 cannot pair Leadership large with PCP small under current governance; every governance dimension blocks it, and the surface itself has no compact mode. | Elevate PCP to secondary-slot eligibility, set `pairedLayoutEligible: true`, add a compatible band semantic that overlaps Leadership, retire `people-culture-must-stack`, and add a real compact / narrow stable mode at the surface level. | Cross-layer redesign |
| GAP-07 | Medium | `people-culture-public.pairingRestrictions: ['hb-kudos']` and `hb-kudos.pairingRestrictions: ['people-culture-public']`. | The bidirectional restriction is not required by the target composition, but retaining it while rewriting PCP/Kudos governance creates hidden pairing failures in future authored presets. | Explicitly re-validate or retire the PCP↔Kudos pairing restriction as part of the governance rewrite. | Targeted refinement |
| GAP-08 | Medium | HB Kudos `supportsCompact: true`, `supportsSummaryCollapse: true`, `narrowestStablePairedWidth: 520`. | Kudos is already comfort-capable; the "narrow slot fit" concern at the surface level is smaller than previously asserted. The real risk is visual quality of Kudos at the small-right slot, not shell eligibility. | Drive Wave-02 Kudos work as validation + premium tuning in the already-supported compact/summary-collapsed modes rather than introducing new modes. | Targeted refinement |
| GAP-09 | Medium | Protected band semantics currently defend the triplicated Company Pulse / stacked-full Kudos / stacked-full PCP arrangement. | Governance is preserving the wrong composition, so a fresh preset cannot land cleanly without updating protected decisions. | Re-baseline `protectedDecisions.ts` around the locked three-row target. | Targeted refinement |
| GAP-10 | Medium | `shellConformance.ts` reports band / slot / recipe metadata, but nothing asserts row-order, handedness, or per-row occupant identity against the target. | Without a target-aware proof, closure is subjective and regressions are invisible. | Extend conformance output and `__tests__` coverage with the locked row sequence, handedness per row, and occupant-per-row membership assertions, plus handheld stack fallback. | Targeted refinement |
| GAP-11 | Medium | HB Kudos `firstLaneEligible: false`, `firstLanePromotionRank: 95`. Row 1 is intended to be the first shell lane. | `firstLaneResolver.ts` will not promote Kudos into the first lane; if the shell's first-lane logic is used to select Row 1, Kudos is silently excluded. | Keep Kudos first-lane-ineligible for primary slot, but confirm the new preset pins Row 1's secondary slot without relying on promotion ranking, or add an explicit secondary-slot first-lane eligibility flag. | Targeted refinement |
| GAP-12 | Low | Launcher / entry-stack layer is already well-bounded and intentionally separated from the shell. | This is not the main problem and should not be destabilized. | Preserve wrapper/launcher structure while refactoring post-launcher shell. | Preserve |
