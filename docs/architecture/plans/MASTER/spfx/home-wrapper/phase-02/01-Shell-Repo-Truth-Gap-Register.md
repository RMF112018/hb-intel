# 01 — Shell Repo-Truth Gap Register

| ID | Gap | Severity | Why it matters | Repo-truth evidence | Earlier package handling | Final package action | Prompt |
|---|---|---|---|---|---|---|---|
| G-01 | `hbHomepage` is still a fixed JSX stack, not a registry-driven shell | Critical | The shell cannot author hierarchy, evolve safely, or prepare for controlled layout administration while composition lives in hard-coded render order | `HbHomepageShell.tsx` renders five zones directly in JSX order | Composition package saw this correctly; basic package only partially | Keep and deepen | Prompt 01, Prompt 02 |
| G-02 | The shell has no typed layout schema, preset model, or validation layer | Critical | `Record<string, unknown>` config is too weak for layout orchestration, persistence, or invalid-config recovery | `hbHomepageContract.ts` exposes broad config typing only | Both packages under-specified this | Add as first-class shell issue | Prompt 01 |
| G-03 | Shell responsiveness is gap/padding-only and viewport-led, not container-aware | Critical | The public shell cannot make pairing, demotion, collapse, or comfort decisions with only a flex-column stack and viewport media queries | `HbHomepageShell.module.css` only changes gap/padding at 768 and 1200 | Composition package was right directionally but too general | Keep and tighten | Prompt 03 |
| G-03A | The shell package did not yet encode the local entry-state breakpoint spec as a governing shell contract | Critical | Without a named entry-state model tied to practical shell design targets, the shell cannot reliably protect first-lane visibility, single-column fallbacks, or device-class-specific hierarchy | No current shell file encodes the spec in `docs/reference/shell/HB-Shell-Entry-Breakpoint-Spec.md` | Missing from the earlier package because the spec did not exist in the package | Add explicitly and make it a governing implementation input | Prompt 01, Prompt 03, Prompt 06, Prompt 07 |
| G-03B | The shell does not yet codify first-lane rules such as conditional two-column pairing, dominant-left behavior, or required single-column fallbacks on portrait and handheld states | High | The entry experience can drift into “branding first, homepage later” unless the first shell lane has explicit policy and closure criteria | The shell today has no first-lane concept at all; it is just a sequential five-zone stack | Only directionally implied before | Add explicitly | Prompt 02, Prompt 03, Prompt 05, Prompt 07 |
| G-04 | The shell has no occupant capability metadata | Critical | Without slot eligibility, comfort width, pairing restrictions, and compact-mode knowledge, the shell cannot reason about layout or future admin safety | No capability registry exists in `hbHomepage` seams | Composition package identified this well | Keep and strengthen | Prompt 01, Prompt 05 |
| G-05 | Zone failure behavior is silent | High | Returning `null` hides failures from authors and collapses shell quality invisibly | `ZoneErrorBoundary.tsx` logs then returns `null` | Basic package identified this well | Keep | Prompt 04 |
| G-06 | Invalid layout / invalid preset / invalid occupant states have no normalized fallback path | High | A future persisted layout or admin-edited arrangement will fail unpredictably without normalization and fallback handling | No schema parser or invalid-state fallback exists in shell seams | Only partially implied before | Add explicitly | Prompt 01, Prompt 04, Prompt 06 |
| G-07 | The shell contract mixes renderer context with layout intent and leaves drift between declared and forwarded props | Medium | Governance clarity is weak when shell props, zone props, and actual zone forwarding do not line up cleanly | `hbHomepageContract.ts` includes broader root props than the shell meaningfully operationalizes for layout | Basic package identified drift; not deep enough | Keep and narrow to shell data design | Prompt 01 |
| G-08 | There is no explicit distinction between protected shell decisions and future-configurable shell decisions | High | Without this boundary, a future control panel will flatten the page into arbitrary arrangement | No preset/persistence contract currently exists | Composition package identified this well | Keep and deepen | Prompt 06 |
| G-09 | Shell closure proof is absent or underdefined | High | The benchmark docs require hosted validation, scoring, and pass/fail closure, but the shell package did not yet make that operational enough | No shell-specific validation artifact set was surfaced in the reachable repo | Both packages recognized this | Keep and strengthen | Prompt 07 |
| G-10 | People & Culture Public is a shell-fit outlier, but the issue is compatibility, not a standalone product redesign | Medium | Its local `maxWidth` and inline-style island reduce shell flexibility for pairing and slot reuse | `PeopleCulturePublicSurface.tsx` keeps a self-contained presentation island and `maxWidth: 1040` | Earlier packages over-expanded the scope | Reframe as shell-fit work only | Prompt 05 |
| G-11 | Hero/top-band work was incorrectly scoped into `hbHomepage` by the earlier basic package | Critical package error, not repo defect | This would send implementation effort in the wrong direction and violate repo-truth shell boundaries | Repo docs and `mount.tsx` state `hbHomepage` is post-hero and `hbSignatureHero` remains independent | Basic package treated this as shell work | Remove from implementation prompts; keep as audit correction | Audit files only |
| G-12 | Extensibility for future occupants is not yet codified | Medium | Future shell growth should not require ad hoc appending of new zones | No active/inactive candidate occupancy model exists | Composition package started this via Safety prompt | Fold into general capability and preset work | Prompt 05, Prompt 06 |

## Notes on what not to do

The final implementation package deliberately avoids:

- moving the hero into `hbHomepage`
- broad module redesign briefs
- freeform drag-resize logic in the public shell renderer
- leaving core shell work for later waves
