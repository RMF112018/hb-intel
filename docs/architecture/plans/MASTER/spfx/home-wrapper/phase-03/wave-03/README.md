# HB Homepage Standalone SPFx App + `.sppkg` Pipeline Prompt Package

## Objective

This package instructs the local code agent to turn **HB Homepage** into a **standalone SPFx solution domain** with its own app host, authoritative `.sppkg` packaging path, clean build flow, and direct package verification.

The implementation must use the **Project Sites** packaging domain and the inspected `hb-intel-project-sites.sppkg` structure as the reference model for:
- app host shape
- Vite bundle → SPFx shell asset staging
- authoritative `tools/build-spfx-package.ts` domain wiring
- shell-entry shim generation
- final `.sppkg` structure
- post-build verification discipline

## Assumption Lock

This package assumes the target is the current **HB Homepage** webpart/runtime that today lives under:

- `apps/hb-webparts/src/webparts/hbHomepage/`
- current webpart manifest id: `e0a11c44-e6d7-45d1-9af5-09ba0b68f5cf`

If the repo already contains partial standalone-homepage work, the agent must reconcile to **one** authoritative package owner and **one** authoritative packaging path. No duplicate ownership of the same webpart id may remain across `hb-webparts` and the new standalone domain.

## Required delivery order

1. `Prompt-01-Create-Standalone-HB-Homepage-App-and-Ownership-Seams.md`
2. `Prompt-02-Wire-Authoritative-SPFx-Packaging-Pipeline.md`
3. `Prompt-03-Run-Clean-Build-Package-and-Verify-Output.md`

## Non-negotiable outcome

At the end of this package, the repo must be able to produce a clean, current, tenant-deployable standalone homepage package such as:

- `dist/sppkg/hb-intel-homepage.sppkg`

with direct proof that:
- the package contains the HB Homepage webpart and no ownership drift,
- the package structure matches the Project Sites reference pattern,
- the staged client-side assets are current,
- the shell-entry shim and manifest wiring are correct,
- the output was generated by the authoritative pipeline rather than by ad hoc manual packaging,
- and the package was actually inspected after build.

## Standing instruction

Do **not** re-read files that are already in your active context unless you need to confirm drift, dependencies, or uncertainty after making changes.
