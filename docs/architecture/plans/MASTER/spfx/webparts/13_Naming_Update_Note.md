# Naming Update Note

This package was updated so all implementation prompts instruct the agent to use the package name `hb-webparts` and avoid titling the package as any kind of homepage package.

Applied rule:
- Package name must be exactly `hb-webparts`
- Do not use `homepage`, `home page`, `homepage-webparts`, or `hb-central-homepage` as the package title/name
