# Plan Summary — Wave 01

Objective:
- correct the first-view launcher row so it satisfies flagship homepage utility expectations

Primary closures:
- remove row heading/count chrome from the homepage path
- make `More tools` a true peer tile
- reduce the boxed-in strip feel of the row

Do not re-read files already in active context unless needed to confirm drift, dependencies, or uncertainty after changes.
