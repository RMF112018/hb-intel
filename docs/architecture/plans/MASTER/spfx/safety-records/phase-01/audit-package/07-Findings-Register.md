# 07-Findings-Register

| Priority | Title | Files | Issue | Consequence | Recommended fix direction |
|---|---|---|---|---|---|
| P0 | Missing real SharePoint repository and upload seams | `packages/features/safety/src/index.ts`, `packages/features/safety/src/factory.ts`, `apps/safety/src/App.tsx` plus missing declared paths under `packages/features/safety/src/adapters/sharepoint/` | The package exports/imports a SharePoint repository and upload helper that were not retrievable at the declared source paths, while the hosted app is wired to use SharePoint mode | Hosted ingestion backend is not operationally proven; main-branch backend cannot be treated as production-ready | Restore/add the concrete SharePoint repository, `spHttp` adapter, and upload helper at the declared boundary, then prove hosted path closure |
| P0 | SharePoint lookup contract incompatible with domain IDs | `packages/features/safety/src/domain/types.ts`, `packages/features/safety/src/lists/fieldSchema.ts`, `packages/features/safety/src/ingestion/runIngestionPipeline.ts` | Domain entities use string IDs, but field schema declares SharePoint Lookup relationships; pipeline writes string business IDs where numeric parent item IDs are required | Real SharePoint writes will fail or produce incorrect relationships | Introduce separate numeric SharePoint item IDs and keep business IDs separate |
| P1 | Weekly rollup derived from same-date inspections, not same-week inspections | `packages/features/safety/src/ingestion/runIngestionPipeline.ts`, `packages/features/safety/src/ports/ISafetyInspectionRepository.ts`, `packages/features/safety/src/adapters/mock/MockSafetyInspectionRepository.ts`, `packages/features/safety/src/scoring/projectWeekRollup.test.ts` | Rollup input is sourced from `findRecentInspectionsForProject(projectNumber, inspectionDate)` rather than all inspections in the reporting week | `InspectionCount` and `AverageInspectionScore` are wrong when multiple inspections occur across different days in one week | Add a week-scoped query and derive rollups from all accepted/duplicate-suspected inspections for the reporting period / week |
| P1 | Ingestion runs lack reporting-period identity | `packages/features/safety/src/domain/types.ts`, `packages/features/safety/src/lists/fieldSchema.ts`, `packages/features/safety/src/ports/ISafetyInspectionRepository.ts`, `packages/features/safety/src/ingestion/runIngestionPipeline.ts`, `packages/features/safety/src/adapters/mock/MockSafetyInspectionRepository.ts` | `IngestionRunFilter` supports `reportingPeriodId`, but the run model and schema do not store it | Period-scoped audit and review filtering are unreliable or impossible | Add `ReportingPeriodId` to the run model/schema and persist it on every run |
| P1 | Ingestion runs do not preserve enough project review context | `packages/features/safety/src/domain/types.ts`, `packages/features/safety/src/lists/fieldSchema.ts`, `apps/safety/src/pages/ReviewQueuePage.tsx` | Unresolved/invalid runs do not durably store attempted project-site text, resolved/attempted project number, or review disposition | Review queue is weak exactly where manual review is needed most | Expand run schema for attempted resolution context and durable review metadata |
| P1 | Reference source list bindings are structurally implicit | `packages/features/safety/README.md`, `packages/features/safety/src/lists/descriptors.ts` | README treats `Projects` and `Legacy Project Fallback Registry` as authoritative dependencies, but the package does not model them as explicit descriptors | Project-resolution backend contract is underdeveloped and hard to verify | Add explicit site-qualified descriptors / resolution bindings for both reference lists |
| P1 | Descriptor binding is fail-closed but not production-wired | `packages/features/safety/src/lists/descriptors.ts`, `packages/features/safety/src/lists/safetyUploadLibrary.ts`, `packages/features/safety/README.md` | All custom/list library descriptors remain on zero GUID placeholders | Main branch is not actually bound to tenant lists | Move GUID binding into a real provisioning/configuration seam and prove resolved values |
| P1 | Retry/replay closure loop incomplete | `packages/features/safety/src/ports/ISafetyInspectionRepository.ts`, `packages/features/safety/src/adapters/mock/MockSafetyInspectionRepository.ts`, `apps/safety/src/pages/ReviewQueuePage.tsx` | Retry exists at the interface but not as a working repo-truth operational path; review UI is read-only | Review-required / commit-failed workbooks are not operationally closable from the app | Implement real replay against retained upload items and surface bounded retry/resolve actions |
| P2 | Date normalization is timezone/locale fragile | `packages/features/safety/src/parser/xlsxWorkbookView.ts`, `packages/features/safety/src/parser/extractMetadata.ts`, `packages/features/safety/src/ingestion/runIngestionPipeline.ts` | Workbook date handling depends on ISO conversion and `Date.parse()` fallback | Duplicate detection and week attribution can drift by day | Normalize workbook dates in a locale-safe, timezone-stable path and add tests |
| P2 | Reporting period/date mismatch not validated | `apps/safety/src/pages/UploadPage.tsx`, `packages/features/safety/src/ingestion/runIngestionPipeline.ts` | User-selected period is trusted without validating that the workbook inspection date falls inside it | Workbooks can be filed into the wrong reporting period | Validate selected period vs parsed inspection date before commit |
| P2 | Parser/data anomalies are flattened into `invalid-template` terminal | `packages/features/safety/src/ingestion/runIngestionPipeline.ts` | `parse-error` exists, but terminal status becomes `invalid-template` | Operator telemetry is less precise than it should be | Add a distinct terminal/audit classification for parser/data failures |
| P2 | Project resolution hint is underused | `packages/features/safety/src/ingestion/runIngestionPipeline.ts`, `packages/features/safety/src/ports/ISafetyInspectionRepository.ts`, `packages/features/safety/src/parser/extractMetadata.ts` | Adapter signature accepts `projectNumberHint`, but pipeline only passes raw project-site text | Unnecessary unresolved-project outcomes and brittle matching | Extract/use project-number hint explicitly |
| P2 | Real `.xlsx` and SharePoint backend paths are unverified | `packages/features/safety/src/test/fixtures.ts`, all `*.test.ts`, `e2e/domain-safety.spec.ts`, `e2e/webparts/safety.spec.ts` | Tests prove synthetic core logic, not hosted backend operations | False confidence in production readiness | Add real workbook and SharePoint contract tests |
| P3 | Design-authority file path references appear drifted | comments in `packages/features/safety/src/*`, `packages/features/safety/README.md` | The referenced design-package authority docs were not directly retrievable at the guessed paths | Harder future auditing and maintenance | Normalize authority paths and add a design-package index/README |
